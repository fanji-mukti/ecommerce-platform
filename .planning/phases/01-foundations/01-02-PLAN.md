---
phase: 01-foundations
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/catalog/ECommerce.Catalog.API/ECommerce.Catalog.API.csproj
  - src/services/catalog/ECommerce.Catalog.API/Program.cs
  - src/services/catalog/Catalog.sln
  - src/services/cart/ECommerce.Cart.API/ECommerce.Cart.API.csproj
  - src/services/cart/ECommerce.Cart.API/Program.cs
  - src/services/cart/Cart.sln
  - src/services/checkout/ECommerce.Checkout.API/ECommerce.Checkout.API.csproj
  - src/services/checkout/ECommerce.Checkout.API/Program.cs
  - src/services/checkout/Checkout.sln
  - src/services/orders/ECommerce.Orders.API/ECommerce.Orders.API.csproj
  - src/services/orders/ECommerce.Orders.API/Program.cs
  - src/services/orders/Orders.sln
  - src/services/identity/ECommerce.Identity.API/ECommerce.Identity.API.csproj
  - src/services/identity/ECommerce.Identity.API/Program.cs
  - src/services/identity/Identity.sln
  - src/services/payments/ECommerce.Payments.API/ECommerce.Payments.API.csproj
  - src/services/payments/ECommerce.Payments.API/Program.cs
  - src/services/payments/Payments.sln
  - src/services/fulfillment/ECommerce.Fulfillment.API/ECommerce.Fulfillment.API.csproj
  - src/services/fulfillment/ECommerce.Fulfillment.API/Program.cs
  - src/services/fulfillment/Fulfillment.sln
  - src/services/notifications/ECommerce.Notifications.API/ECommerce.Notifications.API.csproj
  - src/services/notifications/ECommerce.Notifications.API/Program.cs
  - src/services/notifications/Notifications.sln
autonomous: true
requirements:
  - REPO-01
  - REPO-02
  - INF-03
  - INF-04

user_setup:
  - service: docker-desktop
    why: "Required for Aspire to launch service containers. Not needed for this plan (compile only), but must be installed before Plan 03 execution."
    dashboard_config:
      - task: "Install Docker Desktop"
        location: "https://www.docker.com/products/docker-desktop/"

must_haves:
  truths:
    - "All 8 services compile independently via their respective .sln files"
    - "Each service's Program.cs wires OTel tracing (AddAspNetCoreInstrumentation + AddOtlpExporter) and Serilog (UseSerilog + WriteTo.OpenTelemetry)"
    - "Each service exposes GET /health returning 200 Healthy"
    - "Each service's .csproj references Contracts.csproj via relative ProjectReference path (not NuGet)"
    - "No service .csproj contains business logic, EF Core, or domain dependencies"
  artifacts:
    - path: "src/services/catalog/Catalog.sln"
      provides: "Catalog service standalone solution"
      contains: "ECommerce.Catalog.API.csproj"
    - path: "src/services/catalog/ECommerce.Catalog.API/Program.cs"
      provides: "Catalog stub with OTel + Serilog + /health"
      contains: "MapHealthChecks"
    - path: "src/services/catalog/ECommerce.Catalog.API/ECommerce.Catalog.API.csproj"
      provides: "Catalog project file with Contracts reference"
      contains: "ProjectReference"
  key_links:
    - from: "src/services/catalog/ECommerce.Catalog.API/ECommerce.Catalog.API.csproj"
      to: "src/building-blocks/Contracts/Contracts.csproj"
      via: "ProjectReference relative path"
      pattern: "building-blocks/Contracts/Contracts.csproj"
    - from: "src/services/catalog/ECommerce.Catalog.API/Program.cs"
      to: "AddOtlpExporter"
      via: "OTel OTLP exporter — endpoint injected by Aspire via OTEL_EXPORTER_OTLP_ENDPOINT"
      pattern: "AddOtlpExporter"
---

<objective>
Scaffold all 8 service stubs: each gets a .csproj (Microsoft.NET.Sdk.Web, 4 OTel/Serilog packages, ProjectReference to Contracts), a Program.cs (OTel + Serilog + /health), and a .sln file. Each solution is independently openable in Visual Studio and references Contracts via relative path.

Purpose: Delivers the service skeleton that the Aspire AppHost (Plan 03) will reference. Wires observability from day one so every trace and log carries correlation IDs from the moment the stubs run.

Output: 8 service directories under src/services/, each with a compilable project, a minimal Program.cs, and a standalone .sln. Zero business logic.
</objective>

<execution_context>
@C:/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundations/01-CONTEXT.md
@.planning/phases/01-foundations/01-RESEARCH.md
@.planning/phases/01-foundations/01-PATTERNS.md

<interfaces>
<!-- Service .csproj pattern (from PATTERNS.md Group B, lines ~199–232): -->
<!-- SDK: Microsoft.NET.Sdk.Web -->
<!-- PropertyGroup: net10.0, ImplicitUsings enable, Nullable enable -->
<!-- PackageReferences (pinned versions from RESEARCH.md):
     Serilog.AspNetCore 10.0.0
     Serilog.Sinks.OpenTelemetry 4.2.0
     OpenTelemetry.Extensions.Hosting 1.15.3
     OpenTelemetry.Instrumentation.AspNetCore 1.15.2 -->
<!-- ProjectReference: ../../../building-blocks/Contracts/Contracts.csproj (relative from service project folder) -->

<!-- Program.cs pattern (from PATTERNS.md Group B, lines ~249–309): -->
<!-- Bootstrap logger → WebApplication.CreateBuilder → UseSerilog → AddOpenApi → AddHealthChecks -->
<!-- → AddOpenTelemetry().WithTracing(AddAspNetCoreInstrumentation + AddOtlpExporter) -->
<!-- → UseHttpsRedirection → MapOpenApi → MapHealthChecks("/health") → app.Run() -->
<!-- Wrapped in try/catch(Exception ex) { Log.Fatal } finally { Log.CloseAndFlush } -->

<!-- Service folder/solution name table (from PATTERNS.md, lines ~316–336):
     catalog  → Catalog.sln  → src/services/catalog/ECommerce.Catalog.API/
     cart     → Cart.sln     → src/services/cart/ECommerce.Cart.API/
     checkout → Checkout.sln → src/services/checkout/ECommerce.Checkout.API/
     orders   → Orders.sln   → src/services/orders/ECommerce.Orders.API/
     identity → Identity.sln → src/services/identity/ECommerce.Identity.API/
     payments → Payments.sln → src/services/payments/ECommerce.Payments.API/
     fulfillment → Fulfillment.sln → src/services/fulfillment/ECommerce.Fulfillment.API/
     notifications → Notifications.sln → src/services/notifications/ECommerce.Notifications.API/ -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 8 service .csproj files</name>
  <files>
    src/services/catalog/ECommerce.Catalog.API/ECommerce.Catalog.API.csproj,
    src/services/cart/ECommerce.Cart.API/ECommerce.Cart.API.csproj,
    src/services/checkout/ECommerce.Checkout.API/ECommerce.Checkout.API.csproj,
    src/services/orders/ECommerce.Orders.API/ECommerce.Orders.API.csproj,
    src/services/identity/ECommerce.Identity.API/ECommerce.Identity.API.csproj,
    src/services/payments/ECommerce.Payments.API/ECommerce.Payments.API.csproj,
    src/services/fulfillment/ECommerce.Fulfillment.API/ECommerce.Fulfillment.API.csproj,
    src/services/notifications/ECommerce.Notifications.API/ECommerce.Notifications.API.csproj
  </files>
  <read_first>
    - src/building-blocks/Contracts/Contracts.csproj — baseline PropertyGroup settings (net10.0, ImplicitUsings, Nullable); confirms the project path for relative reference calculation
    - .planning/phases/01-foundations/01-PATTERNS.md — Group B, service csproj section with exact package versions and ProjectReference path (lines ~199–232)
    - .planning/phases/01-foundations/01-CONTEXT.md — D-05 (stub contents), D-06 (naming ECommerce.{ServiceName}.API), D-07 (folder structure)
  </read_first>
  <action>
    Create each directory and csproj file. The preferred approach is to use `dotnet new web --name ECommerce.{ServiceName}.API --output src/services/{service}/ECommerce.{ServiceName}.API` and then replace the generated csproj content — or create the csproj directly with the Write tool.

    All 8 csproj files must be structurally identical, differing only by project name. The exact content for each:
    - Sdk: "Microsoft.NET.Sdk.Web"
    - PropertyGroup: TargetFramework net10.0, ImplicitUsings enable, Nullable enable
    - PackageReference group with exactly these 4 packages at these exact versions:
      - Serilog.AspNetCore Version="10.0.0"
      - Serilog.Sinks.OpenTelemetry Version="4.2.0"
      - OpenTelemetry.Extensions.Hosting Version="1.15.3"
      - OpenTelemetry.Instrumentation.AspNetCore Version="1.15.2"
    - ProjectReference to Contracts: path is `../../../building-blocks/Contracts/Contracts.csproj` (relative from src/services/{service}/ECommerce.{Service}.API/ — three levels up to src/, then into building-blocks/)

    Package legitimacy: All 4 packages are [ASSUMED-APPROVED] per 01-RESEARCH.md Package Legitimacy Audit (Microsoft and Serilog org packages with 40M–709M downloads). Approved to install without checkpoint.

    Service list (folder → project name):
    - catalog → ECommerce.Catalog.API
    - cart → ECommerce.Cart.API
    - checkout → ECommerce.Checkout.API
    - orders → ECommerce.Orders.API
    - identity → ECommerce.Identity.API
    - payments → ECommerce.Payments.API
    - fulfillment → ECommerce.Fulfillment.API
    - notifications → ECommerce.Notifications.API

    If using `dotnet new web` to scaffold, afterwards replace the generated csproj to match the exact pattern above (the template adds extra properties not needed). Do NOT add FrameworkReference elements — Microsoft.NET.Sdk.Web includes ASP.NET Core implicitly.
  </action>
  <verify>
    <automated>for f in catalog cart checkout orders identity payments fulfillment notifications; do dotnet build src/services/$f/ECommerce.${f^}.API/ECommerce.${f^}.API.csproj --configuration Release 2>&1 | grep -E "error|warning|Build succeeded" | tail -1; done</automated>
  </verify>
  <acceptance_criteria>
    - All 8 .csproj files exist at src/services/{service}/ECommerce.{ServiceName}.API/ECommerce.{ServiceName}.API.csproj
    - Each .csproj contains Sdk="Microsoft.NET.Sdk.Web"
    - Each .csproj contains all 4 PackageReference entries with exact versions: Serilog.AspNetCore 10.0.0, Serilog.Sinks.OpenTelemetry 4.2.0, OpenTelemetry.Extensions.Hosting 1.15.3, OpenTelemetry.Instrumentation.AspNetCore 1.15.2
    - Each .csproj contains ProjectReference to "../../../building-blocks/Contracts/Contracts.csproj"
    - Each .csproj does NOT contain FrameworkReference elements (they are provided implicitly by Sdk.Web)
    - dotnet restore succeeds for all 8 projects (packages resolve from NuGet)
  </acceptance_criteria>
  <done>All 8 service .csproj files exist with correct SDK, pinned package versions, and relative Contracts ProjectReference.</done>
</task>

<task type="auto">
  <name>Task 2: Create 8 service Program.cs files with OTel + Serilog + /health</name>
  <files>
    src/services/catalog/ECommerce.Catalog.API/Program.cs,
    src/services/cart/ECommerce.Cart.API/Program.cs,
    src/services/checkout/ECommerce.Checkout.API/Program.cs,
    src/services/orders/ECommerce.Orders.API/Program.cs,
    src/services/identity/ECommerce.Identity.API/Program.cs,
    src/services/payments/ECommerce.Payments.API/Program.cs,
    src/services/fulfillment/ECommerce.Fulfillment.API/Program.cs,
    src/services/notifications/ECommerce.Notifications.API/Program.cs
  </files>
  <read_first>
    - .planning/phases/01-foundations/01-PATTERNS.md — Group B, Program.cs section with the full try/catch/finally pattern and annotated key notes (lines ~249–309)
    - .planning/phases/01-foundations/01-PATTERNS.md — Shared Patterns, OTel + Serilog Wiring section and Health Check Registration section (lines ~626–668)
    - .planning/phases/01-foundations/01-CONTEXT.md — D-11 (OTel scope: AddAspNetCoreInstrumentation + OTLP exporter), D-12 (W3C TraceContext enrichment via Enrich.FromLogContext), D-13 (/health returns 200 with default response writer)
  </read_first>
  <action>
    All 8 Program.cs files are structurally identical — no substitution needed for service name. Create the same content in each file.

    The Program.cs must follow the exact pattern from PATTERNS.md (Pattern 3):
    1. Two using directives at the top: `using Serilog;` and `using Serilog.Events;`
    2. Bootstrap logger before WebApplication.CreateBuilder — configured with:
       - .MinimumLevel.Override("Microsoft", LogEventLevel.Information)
       - .Enrich.FromLogContext()   ← satisfies D-12 W3C TraceContext enrichment
       - .WriteTo.Console()
       - .WriteTo.OpenTelemetry()   ← exports to OTEL_EXPORTER_OTLP_ENDPOINT env var
    3. Wrapped in try/catch(Exception ex)/finally per Serilog startup pattern
    4. Inside try block:
       - WebApplication.CreateBuilder(args)
       - builder.Host.UseSerilog()
       - builder.Services.AddOpenApi()   ← Microsoft.AspNetCore.OpenApi built-in, NOT Swashbuckle
       - builder.Services.AddHealthChecks()
       - builder.Services.AddOpenTelemetry().WithTracing(t => t.AddAspNetCoreInstrumentation().AddOtlpExporter())
       - app.UseHttpsRedirection()
       - app.MapOpenApi()
       - app.MapHealthChecks("/health")   ← satisfies D-13 and INF-04
       - app.Run()
    5. catch(Exception ex): Log.Fatal(ex, "Application terminated unexpectedly"); return 1;
    6. finally: Log.CloseAndFlush();
    7. return 0; (after the try/catch/finally block)

    CRITICAL: Do NOT install Swashbuckle. AddOpenApi() is the .NET 10 built-in OpenAPI method (no extra NuGet package needed).
    CRITICAL: The OTLP endpoint is NOT hardcoded — it is read from OTEL_EXPORTER_OTLP_ENDPOINT environment variable automatically by the SDK. Aspire injects this variable at runtime.
    CRITICAL: MapHealthChecks("/health") uses the default ResponseWriter which returns {"status":"Healthy"} — do NOT configure a custom writer in Phase 1.

    If `dotnet new web` generated a Program.cs with the weather forecast sample, REPLACE it entirely with the pattern above.
  </action>
  <verify>
    <automated>dotnet build src/services/catalog/ECommerce.Catalog.API/ECommerce.Catalog.API.csproj --configuration Release 2>&1 | tail -3 && dotnet build src/services/notifications/ECommerce.Notifications.API/ECommerce.Notifications.API.csproj --configuration Release 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - All 8 Program.cs files exist
    - Each contains "MapHealthChecks(\"/health\")" (satisfies INF-04)
    - Each contains "AddOtlpExporter()" (satisfies INF-03 OTel traces)
    - Each contains "WriteTo.OpenTelemetry()" (satisfies INF-03 structured logs)
    - Each contains "Enrich.FromLogContext()" (satisfies D-12 W3C TraceContext)
    - Each contains "builder.Services.AddOpenApi()" — NOT "AddSwaggerGen" or any Swashbuckle reference
    - Each uses the try/catch/finally startup wrapper with Log.Fatal in catch and Log.CloseAndFlush in finally
    - dotnet build on catalog and notifications stubs (first and last) exits 0 with 0 errors
  </acceptance_criteria>
  <done>All 8 Program.cs files compile with OTel traces, Serilog structured logs with W3C correlation, and GET /health endpoint. No business logic, no Swashbuckle.</done>
</task>

<task type="auto">
  <name>Task 3: Create 8 service .sln files with Contracts reference</name>
  <files>
    src/services/catalog/Catalog.sln,
    src/services/cart/Cart.sln,
    src/services/checkout/Checkout.sln,
    src/services/orders/Orders.sln,
    src/services/identity/Identity.sln,
    src/services/payments/Payments.sln,
    src/services/fulfillment/Fulfillment.sln,
    src/services/notifications/Notifications.sln
  </files>
  <read_first>
    - .planning/phases/01-foundations/01-PATTERNS.md — Group B, service .sln section with exact dotnet CLI commands and substitution table (lines ~312–337)
    - .planning/phases/01-foundations/01-CONTEXT.md — D-07 (each service has own .sln in src/services/{service}/); REPO-01 (independently openable in Visual Studio, Contracts via relative path — not NuGet)
  </read_first>
  <action>
    For each of the 8 services, run three commands in order:

    1. `dotnet new sln --name {SolutionName} --output src/services/{service}`
    2. `dotnet sln src/services/{service}/{SolutionName}.sln add src/services/{service}/ECommerce.{ServiceName}.API/ECommerce.{ServiceName}.API.csproj`
    3. `dotnet sln src/services/{service}/{SolutionName}.sln add src/building-blocks/Contracts/Contracts.csproj`

    Solution name table (D-07):
    - service=catalog,       SolutionName=Catalog,       ServiceName=Catalog
    - service=cart,          SolutionName=Cart,           ServiceName=Cart
    - service=checkout,      SolutionName=Checkout,       ServiceName=Checkout
    - service=orders,        SolutionName=Orders,         ServiceName=Orders
    - service=identity,      SolutionName=Identity,       ServiceName=Identity
    - service=payments,      SolutionName=Payments,       ServiceName=Payments
    - service=fulfillment,   SolutionName=Fulfillment,    ServiceName=Fulfillment
    - service=notifications, SolutionName=Notifications,  ServiceName=Notifications

    Critical rules:
    - Each solution includes BOTH the service project AND Contracts.csproj (satisfies REPO-02: Contracts referenced via relative path, not NuGet).
    - Do NOT hand-author .sln file bodies. Always use `dotnet new sln` + `dotnet sln add`.
    - The Contracts.csproj is added to the solution so Visual Studio can navigate it directly when the service solution is open.
  </action>
  <verify>
    <automated>dotnet build src/services/catalog/Catalog.sln --configuration Release 2>&1 | tail -3 && dotnet build src/services/notifications/Notifications.sln --configuration Release 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - All 8 .sln files exist at src/services/{service}/{ServiceName}.sln
    - Each .sln file contains both the service .csproj path and "Contracts.csproj" (grep confirms both entries)
    - dotnet build src/services/catalog/Catalog.sln --configuration Release exits 0
    - dotnet build src/services/notifications/Notifications.sln --configuration Release exits 0
    - dotnet sln src/services/catalog/Catalog.sln list outputs exactly 2 projects: ECommerce.Catalog.API.csproj and Contracts.csproj (order may vary)
    - The service .csproj in each solution has a ProjectReference to Contracts.csproj — NOT a PackageReference (grep for "PackageReference.*Contracts" in each service .csproj returns 0 matches)
  </acceptance_criteria>
  <done>All 8 service solutions build independently with Contracts included via project reference. Each solution is independently openable in Visual Studio (REPO-01 satisfied).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| NuGet restore → package cache | 4 packages are restored per service (Serilog, OTel) — all [ASSUMED-APPROVED] from RESEARCH.md audit |
| Service /health endpoint | Unauthenticated by design (Phase 1 health checks are public) — acceptable for stubs, not for production |
| Aspire-injected env vars | OTEL_EXPORTER_OTLP_ENDPOINT injected at runtime — not hardcoded in source |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | Program.cs OTLP endpoint | mitigate | Endpoint sourced from OTEL_EXPORTER_OTLP_ENDPOINT env var only — never hardcoded in source; acceptance criteria verify no hardcoded URL |
| T-02-02 | Information Disclosure | appsettings.json (if generated by dotnet new) | mitigate | If dotnet new web generates appsettings.json with connection strings, verify it contains no secrets before committing. Phase 1 stubs should have no secrets in appsettings — connection strings are Aspire-injected via env vars |
| T-02-03 | Tampering | NuGet package versions | mitigate | All 4 packages pinned to exact versions in .csproj. Floating versions (e.g., "10.*") are forbidden — exact version string required |
| T-02-SC | Tampering | NuGet package installs | accept | All packages are [ASSUMED-APPROVED] per RESEARCH.md Package Legitimacy Audit (Microsoft and Serilog org publishers, 40M+ downloads each). No checkpoint needed. |
</threat_model>

<verification>
Full verification for service stubs deliverables:

1. All 8 .sln files build:
   `for sln in catalog/Catalog cart/Cart checkout/Checkout orders/Orders identity/Identity payments/Payments fulfillment/Fulfillment notifications/Notifications; do dotnet build src/services/$sln.sln --configuration Release --no-restore; done`
   (each exits 0)

2. No service has Swashbuckle:
   `grep -r "Swashbuckle" src/services/ --include="*.csproj"` — returns no matches

3. All Program.cs files wire health checks:
   `grep -rl "MapHealthChecks" src/services/` — returns 8 files

4. All Program.cs files wire OTel OTLP:
   `grep -rl "AddOtlpExporter" src/services/` — returns 8 files

5. All .csproj files reference Contracts via ProjectReference not NuGet:
   `grep -r "PackageReference.*Contracts" src/services/` — returns no matches
   `grep -rl "building-blocks/Contracts" src/services/` — returns 8 files (one per csproj)
</verification>

<success_criteria>
- 8 service stubs exist, each with .csproj + Program.cs + .sln
- All 8 .sln files build independently via `dotnet build`
- Each Program.cs: OTel traces (AddAspNetCoreInstrumentation + AddOtlpExporter), Serilog (UseSerilog + WriteTo.OpenTelemetry + Enrich.FromLogContext), /health endpoint (MapHealthChecks)
- Each .csproj: ProjectReference to Contracts (not NuGet), 4 pinned package versions, Microsoft.NET.Sdk.Web
- Zero business logic, zero domain code, zero EF Core dependencies
- REPO-01 satisfied: 8 independently-openable .sln files
- REPO-02 satisfied: all solutions reference Contracts via relative ProjectReference
- INF-03 satisfied: OTel + Serilog wired with W3C correlation IDs
- INF-04 satisfied: GET /health on each service
</success_criteria>

<output>
Create `.planning/phases/01-foundations/01-02-SUMMARY.md` when done.
</output>
