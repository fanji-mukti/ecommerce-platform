---
phase: 01-foundations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/building-blocks/Contracts/Contracts.csproj
  - src/building-blocks/Contracts/Class1.cs
  - src/building-blocks/Contracts/IMessageEnvelope.cs
  - src/building-blocks/Contracts/Catalog/Events/V1/Placeholder.cs
  - src/building-blocks/Contracts/Cart/Events/V1/Placeholder.cs
  - src/building-blocks/Contracts/Checkout/Events/V1/Placeholder.cs
  - src/building-blocks/Contracts/Orders/Events/V1/Placeholder.cs
  - src/building-blocks/Contracts/Identity/Events/V1/Placeholder.cs
  - src/building-blocks/Contracts/Payments/Events/V1/Placeholder.cs
  - src/building-blocks/Contracts/Fulfillment/Events/V1/Placeholder.cs
  - src/building-blocks/Contracts/Notifications/Events/V1/Placeholder.cs
  - src/building-blocks/Contracts/Catalog/Commands/V1/Placeholder.cs
  - src/building-blocks/Contracts/Cart/Commands/V1/Placeholder.cs
  - src/building-blocks/Contracts/Checkout/Commands/V1/Placeholder.cs
  - src/building-blocks/Contracts/Orders/Commands/V1/Placeholder.cs
  - src/building-blocks/Contracts/Identity/Commands/V1/Placeholder.cs
  - src/building-blocks/Contracts/Payments/Commands/V1/Placeholder.cs
  - src/building-blocks/Contracts/Fulfillment/Commands/V1/Placeholder.cs
  - src/building-blocks/Contracts/Notifications/Commands/V1/Placeholder.cs
  - src/building-blocks/Contracts/Contracts.sln
autonomous: true
requirements:
  - CON-01
  - CON-02
  - CON-03
  - REPO-02

must_haves:
  truths:
    - "Contracts.csproj compiles with zero NuGet package references — only the SDK and target framework"
    - "IMessageEnvelope interface defines exactly 4 properties: Guid MessageId, Guid CorrelationId, Guid CausationId, DateTimeOffset OccurredAt"
    - "All 8 service Events.V1 AND Commands.V1 namespace placeholders exist as positional records implementing IMessageEnvelope (16 total files per D-02)"
    - "Contracts.sln exists at src/building-blocks/Contracts/Contracts.sln and builds independently via dotnet build"
    - "Class1.cs is deleted — no stub class remains in the project"
  artifacts:
    - path: "src/building-blocks/Contracts/IMessageEnvelope.cs"
      provides: "Shared message envelope interface in ECommerce.Contracts namespace"
      contains: "public interface IMessageEnvelope"
    - path: "src/building-blocks/Contracts/Catalog/Events/V1/Placeholder.cs"
      provides: "ECommerce.Catalog.Events.V1 namespace stub"
      contains: "CatalogServiceReady"
    - path: "src/building-blocks/Contracts/Contracts.sln"
      provides: "Standalone solution for CI matrix"
      contains: "Contracts.csproj"
  key_links:
    - from: "src/building-blocks/Contracts/Catalog/Events/V1/Placeholder.cs"
      to: "src/building-blocks/Contracts/IMessageEnvelope.cs"
      via: "implements IMessageEnvelope"
      pattern: "IMessageEnvelope"
    - from: "src/building-blocks/Contracts/Contracts.sln"
      to: "src/building-blocks/Contracts/Contracts.csproj"
      via: "solution project reference"
      pattern: "Contracts.csproj"
---

## Phase Goal

**As a** platform developer, **I want to** lock the shared contracts shape and namespace structure, **so that** every later phase builds domain events against a stable, compiler-enforced contract.

<objective>
Populate the Contracts class library: define the IMessageEnvelope interface, stub all 8 service namespaces with placeholder records, delete Class1.cs, and create Contracts.sln for independent CI building.

Purpose: Establishes the compile-time contract that every ASB message in the system must implement. Locks the namespace convention (ECommerce.{ServiceName}.Events.V1) that all later phases extend.

Output: Compilable Contracts library with interface + 8 namespace stubs + standalone solution file. Zero NuGet references — pure C# class library.
</objective>

<execution_context>
@C:/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundations/01-CONTEXT.md
@.planning/phases/01-foundations/01-RESEARCH.md
@.planning/phases/01-foundations/01-PATTERNS.md

<interfaces>
<!-- Existing Contracts.csproj — read before touching -->
<!-- src/building-blocks/Contracts/Contracts.csproj (9 lines, no NuGet refs): -->
<!--   Sdk="Microsoft.NET.Sdk", TargetFramework=net10.0, ImplicitUsings=enable, Nullable=enable -->
<!-- CONSTRAINT (D-03): This file must NEVER gain PackageReference or FrameworkReference elements. -->

<!-- IMessageEnvelope contract to create (D-01): -->
<!--   namespace ECommerce.Contracts -->
<!--   public interface IMessageEnvelope -->
<!--     Guid MessageId { get; } -->
<!--     Guid CorrelationId { get; } -->
<!--     Guid CausationId { get; } -->
<!--     DateTimeOffset OccurredAt { get; } -->

<!-- Placeholder record pattern (from RESEARCH.md Pattern 2): -->
<!--   positional record parameters auto-generate { get; init; } which satisfies { get; } on the interface -->
<!--   e.g.: public record CatalogServiceReady(Guid MessageId, Guid CorrelationId, Guid CausationId, DateTimeOffset OccurredAt) : IMessageEnvelope; -->
<!--   requires: using ECommerce.Contracts; at top of each Placeholder.cs file -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define IMessageEnvelope and delete Class1.cs</name>
  <files>
    src/building-blocks/Contracts/IMessageEnvelope.cs,
    src/building-blocks/Contracts/Class1.cs
  </files>
  <read_first>
    - src/building-blocks/Contracts/Contracts.csproj — confirm no PackageReference exists; note TargetFramework, ImplicitUsings, Nullable settings
    - src/building-blocks/Contracts/Class1.cs — read before deleting to confirm it is the generated stub with no useful content
    - .planning/phases/01-foundations/01-CONTEXT.md — D-01 (interface properties), D-03 (no NuGet constraint)
    - .planning/phases/01-foundations/01-PATTERNS.md — Group A, IMessageEnvelope.cs section (lines ~118–141)
  </read_first>
  <action>
    1. Delete src/building-blocks/Contracts/Class1.cs (the generated stub — confirmed to contain only "namespace Contracts { public class Class1 {} }" or equivalent empty content).

    2. Create src/building-blocks/Contracts/IMessageEnvelope.cs with:
       - File-scoped namespace declaration: `namespace ECommerce.Contracts;`
       - Public interface named `IMessageEnvelope` with exactly these 4 properties using `{ get; }` accessors:
         - `Guid MessageId { get; }`
         - `Guid CorrelationId { get; }`
         - `Guid CausationId { get; }`
         - `DateTimeOffset OccurredAt { get; }`
       - No `using` directives needed (Guid and DateTimeOffset are in System, covered by ImplicitUsings).
       - No XML doc comments, no attributes, no sealed modifier.

    Do NOT modify Contracts.csproj — it is correct as-is (per D-03, zero NuGet refs).
    Do NOT add `{ get; init; }` to the interface — use `{ get; }` only. Records satisfy this via positional constructor synthesis.
  </action>
  <verify>
    <automated>dotnet build src/building-blocks/Contracts/Contracts.csproj --configuration Release 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/building-blocks/Contracts/Class1.cs does not exist
    - src/building-blocks/Contracts/IMessageEnvelope.cs exists
    - IMessageEnvelope.cs contains the string "public interface IMessageEnvelope"
    - IMessageEnvelope.cs contains exactly: "Guid MessageId", "Guid CorrelationId", "Guid CausationId", "DateTimeOffset OccurredAt"
    - IMessageEnvelope.cs namespace is "ECommerce.Contracts" (file-scoped namespace declaration)
    - src/building-blocks/Contracts/Contracts.csproj contains zero PackageReference elements (grep -c "PackageReference" Contracts.csproj == 0)
    - dotnet build src/building-blocks/Contracts/Contracts.csproj --configuration Release exits 0
  </acceptance_criteria>
  <done>IMessageEnvelope interface compiles, Class1.cs is gone, Contracts.csproj has zero NuGet references.</done>
</task>

<task type="auto">
  <name>Task 2: Create 8 service namespace placeholder records</name>
  <files>
    src/building-blocks/Contracts/Catalog/Events/V1/Placeholder.cs,
    src/building-blocks/Contracts/Cart/Events/V1/Placeholder.cs,
    src/building-blocks/Contracts/Checkout/Events/V1/Placeholder.cs,
    src/building-blocks/Contracts/Orders/Events/V1/Placeholder.cs,
    src/building-blocks/Contracts/Identity/Events/V1/Placeholder.cs,
    src/building-blocks/Contracts/Payments/Events/V1/Placeholder.cs,
    src/building-blocks/Contracts/Fulfillment/Events/V1/Placeholder.cs,
    src/building-blocks/Contracts/Notifications/Events/V1/Placeholder.cs,
    src/building-blocks/Contracts/Catalog/Commands/V1/Placeholder.cs,
    src/building-blocks/Contracts/Cart/Commands/V1/Placeholder.cs,
    src/building-blocks/Contracts/Checkout/Commands/V1/Placeholder.cs,
    src/building-blocks/Contracts/Orders/Commands/V1/Placeholder.cs,
    src/building-blocks/Contracts/Identity/Commands/V1/Placeholder.cs,
    src/building-blocks/Contracts/Payments/Commands/V1/Placeholder.cs,
    src/building-blocks/Contracts/Fulfillment/Commands/V1/Placeholder.cs,
    src/building-blocks/Contracts/Notifications/Commands/V1/Placeholder.cs
  </files>
  <read_first>
    - src/building-blocks/Contracts/IMessageEnvelope.cs — read the just-created interface to confirm namespace and property names before implementing
    - .planning/phases/01-foundations/01-PATTERNS.md — Group A, Placeholder.cs section with namespace substitution table (lines ~144–177)
    - .planning/phases/01-foundations/01-CONTEXT.md — D-02 (namespace pattern ECommerce.{ServiceName}.Events.V1)
  </read_first>
  <action>
    Create 8 files, one per service, each following this exact pattern (substitute {ServiceName} and {RecordName} per the table below):

    File structure:
    ```
    using ECommerce.Contracts;

    namespace ECommerce.{ServiceName}.Events.V1;

    public record {RecordName}ServiceReady(
        Guid MessageId,
        Guid CorrelationId,
        Guid CausationId,
        DateTimeOffset OccurredAt
    ) : IMessageEnvelope;
    ```

    Substitution table (D-02, PATTERNS.md namespace substitution table):
    - Catalog  → namespace ECommerce.Catalog.Events.V1,       record CatalogServiceReady
    - Cart     → namespace ECommerce.Cart.Events.V1,          record CartServiceReady
    - Checkout → namespace ECommerce.Checkout.Events.V1,      record CheckoutServiceReady
    - Orders   → namespace ECommerce.Orders.Events.V1,        record OrdersServiceReady
    - Identity → namespace ECommerce.Identity.Events.V1,      record IdentityServiceReady
    - Payments → namespace ECommerce.Payments.Events.V1,      record PaymentsServiceReady
    - Fulfillment → namespace ECommerce.Fulfillment.Events.V1, record FulfillmentServiceReady
    - Notifications → namespace ECommerce.Notifications.Events.V1, record NotificationsServiceReady

    Key rules:
    - Use positional record parameters (the 4 parameters in the primary constructor). Do NOT add explicit property bodies — the positional form generates { get; init; } automatically, which satisfies IMessageEnvelope's { get; } requirements.
    - Each file MUST include `using ECommerce.Contracts;` — ImplicitUsings does not include project-local namespaces.
    - File path follows the namespace: Catalog/Events/V1/Placeholder.cs under the Contracts project root.
    - Create intermediate directories as needed (Catalog/Events/V1/, Cart/Events/V1/, etc.).
  </action>
  <verify>
    <automated>dotnet build src/building-blocks/Contracts/Contracts.csproj --configuration Release 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - All 8 Events.V1 Placeholder.cs files exist at their exact paths under src/building-blocks/Contracts/{ServiceName}/Events/V1/
    - All 8 Commands.V1 Placeholder.cs files exist at their exact paths under src/building-blocks/Contracts/{ServiceName}/Commands/V1/
    - Each file contains "using ECommerce.Contracts;" as first line
    - Each file contains ": IMessageEnvelope" (implements the interface)
    - Each file uses positional record syntax (parameters in constructor, not { get; init; } explicit body)
    - dotnet build src/building-blocks/Contracts/Contracts.csproj --configuration Release exits 0 with 0 errors and 0 warnings (excluding nullable analysis warnings if any)
    - The 8 Events.V1 record names are exactly: CatalogServiceReady, CartServiceReady, CheckoutServiceReady, OrdersServiceReady, IdentityServiceReady, PaymentsServiceReady, FulfillmentServiceReady, NotificationsServiceReady
    - The 8 Commands.V1 record names are exactly: CatalogCommandsPlaceholder, CartCommandsPlaceholder, CheckoutCommandsPlaceholder, OrdersCommandsPlaceholder, IdentityCommandsPlaceholder, PaymentsCommandsPlaceholder, FulfillmentCommandsPlaceholder, NotificationsCommandsPlaceholder
  </acceptance_criteria>
  <done>All 16 placeholder records (8 Events.V1 + 8 Commands.V1) implement IMessageEnvelope and compile cleanly. Both namespace conventions ECommerce.{ServiceName}.Events.V1 and ECommerce.{ServiceName}.Commands.V1 are locked per D-02.</done>
</task>

<task type="auto">
  <name>Task 3: Create Contracts.sln for standalone CI builds</name>
  <files>
    src/building-blocks/Contracts/Contracts.sln
  </files>
  <read_first>
    - .planning/phases/01-foundations/01-PATTERNS.md — Group A, Contracts.sln section with exact dotnet CLI commands (lines ~181–189)
    - .planning/phases/01-foundations/01-CONTEXT.md — D-16 (CI matrix includes Contracts entry)
    - src/building-blocks/Contracts/Contracts.csproj — confirm the project file exists at the expected path before running dotnet sln add
  </read_first>
  <action>
    Use the dotnet CLI to generate the solution file and add the project. Run these two commands in order from the repo root:

    Command 1: dotnet new sln --name Contracts --output src/building-blocks/Contracts
    Command 2: dotnet sln src/building-blocks/Contracts/Contracts.sln add src/building-blocks/Contracts/Contracts.csproj

    Do NOT hand-author the .sln file body — .sln format has GUIDs and magic strings that must be generated by the CLI.
    Do NOT add any service projects to this solution — it references Contracts.csproj only.
    The solution is named "Contracts" matching the CI matrix path entry from D-16.
  </action>
  <verify>
    <automated>dotnet build src/building-blocks/Contracts/Contracts.sln --configuration Release 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/building-blocks/Contracts/Contracts.sln exists
    - dotnet build src/building-blocks/Contracts/Contracts.sln --configuration Release exits 0
    - The .sln file references Contracts.csproj (grep for "Contracts.csproj" in the .sln file returns a match)
    - The .sln file does NOT reference any service project csproj files
    - dotnet sln src/building-blocks/Contracts/Contracts.sln list outputs exactly one project: Contracts.csproj (or its path)
  </acceptance_criteria>
  <done>Contracts.sln builds independently. The CI matrix can run dotnet build src/building-blocks/Contracts/Contracts.sln without needing any service project.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer workstation → source control | Contracts.csproj must not carry unauthorized NuGet dependencies that could introduce supply chain risk |
| NuGet restore → package cache | Phase 1 Contracts library has zero NuGet packages — no supply chain surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | Contracts.csproj | mitigate | Enforce zero PackageReference via PR checklist; acceptance criteria verify `grep -c PackageReference Contracts.csproj == 0` |
| T-01-02 | Information Disclosure | Class1.cs deletion | accept | Class1.cs contains no sensitive data; deletion is safe and verified by acceptance criteria |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | Phase 1 Contracts task has zero package manager installs — no supply chain gate needed |
</threat_model>

<verification>
Full phase verification for Contracts deliverables:

1. `dotnet build src/building-blocks/Contracts/Contracts.sln --configuration Release` — exits 0
2. `grep -c "PackageReference" src/building-blocks/Contracts/Contracts.csproj` — outputs 0
3. `ls src/building-blocks/Contracts/IMessageEnvelope.cs` — file exists
4. `grep -l "IMessageEnvelope" src/building-blocks/Contracts/*/Events/V1/Placeholder.cs` — returns 8 files
5. `ls src/building-blocks/Contracts/Class1.cs 2>&1` — "No such file" (file deleted)
6. `dotnet sln src/building-blocks/Contracts/Contracts.sln list` — outputs exactly 1 project path
</verification>

<success_criteria>
- Contracts.csproj has zero NuGet references (SDK + target framework only)
- IMessageEnvelope interface exists with exactly 4 properties in ECommerce.Contracts namespace
- 8 placeholder records exist implementing IMessageEnvelope across all 8 service namespaces (ECommerce.{ServiceName}.Events.V1)
- Class1.cs is deleted
- Contracts.sln builds independently via dotnet build
- All records use positional constructor syntax (compile-time proof of { get; init; } synthesis)
</success_criteria>

<output>
Create `.planning/phases/01-foundations/01-01-SUMMARY.md` when done.
</output>
