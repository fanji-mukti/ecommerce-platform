---
phase: 02-identity-catalog-gateway
plan: "10"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/catalog/ECommerce.Catalog.API/Features/Products/ProductsEndpoints.cs
  - src/services/catalog/ECommerce.Catalog.API/Features/Products/PaginationHelper.cs
  - src/services/catalog/ECommerce.Catalog.Tests/Unit/ProductValidationSteps.cs
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Pagination clamping logic lives in one place — PaginationHelper.Clamp(int page, int pageSize) — not duplicated between production code and test code"
    - "ProductsEndpoints.cs calls PaginationHelper.Clamp instead of inline if-statements"
    - "ProductValidationSteps.When_Validated() calls PaginationHelper.Clamp instead of duplicating the logic inline"
    - "Unit tests exercise the real production clamping function — changing ProductsEndpoints clamping will cause unit tests to fail"
    - "All existing catalog tests still compile and the build exits 0"
  artifacts:
    - path: "src/services/catalog/ECommerce.Catalog.API/Features/Products/PaginationHelper.cs"
      provides: "Extracted static pagination clamping logic callable by both endpoint and tests"
      contains: "static class PaginationHelper"
    - path: "src/services/catalog/ECommerce.Catalog.API/Features/Products/ProductsEndpoints.cs"
      provides: "Updated endpoint that delegates clamping to PaginationHelper.Clamp"
      contains: "PaginationHelper.Clamp"
    - path: "src/services/catalog/ECommerce.Catalog.Tests/Unit/ProductValidationSteps.cs"
      provides: "Updated unit test steps that call PaginationHelper.Clamp"
      contains: "PaginationHelper.Clamp"
  key_links:
    - from: "ProductValidationSteps.When_Validated"
      to: "PaginationHelper.Clamp"
      via: "direct method call"
      pattern: "PaginationHelper\\.Clamp"
    - from: "ProductsEndpoints MapGet /products"
      to: "PaginationHelper.Clamp"
      via: "direct method call"
      pattern: "PaginationHelper\\.Clamp"
---

<objective>
Fix Gap 7: catalog unit tests duplicate the pagination clamping logic from ProductsEndpoints.cs inline rather than calling the production code. This means a regression in the endpoint (wrong boundary condition, different default) will never be caught by the unit tests — they always pass because they test their own copy of the logic.

Fix Option A: Extract the clamping logic from ProductsEndpoints.cs into a new static PaginationHelper class in the same Features/Products folder. Update ProductsEndpoints.cs to call PaginationHelper.Clamp. Update ProductValidationSteps.When_Validated() to call PaginationHelper.Clamp instead of duplicating the logic. The unit tests now exercise the real production code path.

Purpose: Restore the regression-detection value of the unit tests — changing ProductsEndpoints clamping incorrectly will now cause unit test failures.
Output: PaginationHelper.cs (new), updated ProductsEndpoints.cs, updated ProductValidationSteps.cs.
</objective>

<execution_context>
@C:\Users\User\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\User\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract PaginationHelper and update endpoint + unit test steps to use it</name>
  <files>
    src/services/catalog/ECommerce.Catalog.API/Features/Products/PaginationHelper.cs
    src/services/catalog/ECommerce.Catalog.API/Features/Products/ProductsEndpoints.cs
    src/services/catalog/ECommerce.Catalog.Tests/Unit/ProductValidationSteps.cs
  </files>
  <read_first>
    - src/services/catalog/ECommerce.Catalog.API/Features/Products/ProductsEndpoints.cs (current 45-line file — contains the inline clamping at lines 18-19)
    - src/services/catalog/ECommerce.Catalog.Tests/Unit/ProductValidationSteps.cs (current 30-line file — contains the duplicated logic at lines 23-24)
  </read_first>
  <action>
    Step 1: Create src/services/catalog/ECommerce.Catalog.API/Features/Products/PaginationHelper.cs with:

      namespace ECommerce.Catalog.API.Features.Products;

      public static class PaginationHelper
      {
          /// <summary>
          /// Clamps pagination parameters to valid bounds.
          /// Page below 1 is clamped to 1.
          /// PageSize below 1 or above 100 is clamped to the default (12).
          /// </summary>
          public static (int page, int pageSize) Clamp(int page, int pageSize)
          {
              if (page < 1) page = 1;
              if (pageSize < 1 || pageSize > 100) pageSize = 12;
              return (page, pageSize);
          }
      }

    The exact boundary conditions MUST match what is currently in ProductsEndpoints.cs lines 18-19:
      page < 1 → clamp to 1
      pageSize < 1 || pageSize > 100 → clamp to 12

    Step 2: Update ProductsEndpoints.cs MapGet("/products") lambda. Replace lines 18-19 (the inline clamping):
      // Input validation — clamp bounds (ASVS V5, T-02-03-02)
      if (page < 1) page = 1;
      if (pageSize < 1 || pageSize > 100) pageSize = 12;
    with:
      // Input validation — clamp bounds (ASVS V5, T-02-03-02)
      (page, pageSize) = PaginationHelper.Clamp(page, pageSize);

    Do NOT change any other code in ProductsEndpoints.cs (imports, the MapGet("/products/{id:guid}") handler, the static Map method signature, or any other lines).

    Step 3: Update ProductValidationSteps.cs. Replace the When_Validated method body:
      _clampedPage = _page < 1 ? 1 : _page;
      _clampedPageSize = (_pageSize < 1 || _pageSize > 100) ? 12 : _pageSize;
    with:
      (_clampedPage, _clampedPageSize) = PaginationHelper.Clamp(_page, _pageSize);

    Add the using statement at the top of the file:
      using ECommerce.Catalog.API.Features.Products;

    The ProductValidationSteps test project already references ECommerce.Catalog.API (or ECommerce.Catalog.Tests.csproj already has a ProjectReference to it). Confirm this ProjectReference exists in ECommerce.Catalog.Tests.csproj before editing. If missing, add:
      <ProjectReference Include="../../ECommerce.Catalog.API/ECommerce.Catalog.API.csproj" />

    The field declarations, Given_QueryParams, Then_PageIs, and Then_PageSizeIs methods in ProductValidationSteps must remain unchanged.
  </action>
  <verify>
    <automated>dotnet build src/services/catalog/ECommerce.Catalog.Tests/ECommerce.Catalog.Tests.csproj --no-incremental 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - PaginationHelper.cs exists at src/services/catalog/ECommerce.Catalog.API/Features/Products/PaginationHelper.cs
    - PaginationHelper.cs contains `public static (int page, int pageSize) Clamp(int page, int pageSize)`
    - PaginationHelper.Clamp body: `if (page < 1) page = 1;` and `if (pageSize < 1 || pageSize > 100) pageSize = 12;` with `return (page, pageSize);`
    - ProductsEndpoints.cs contains `PaginationHelper.Clamp` and does NOT contain `if (page < 1) page = 1;` as an inline statement
    - ProductValidationSteps.cs When_Validated method body contains `PaginationHelper.Clamp` and does NOT contain the ternary `_page < 1 ? 1 : _page` pattern
    - ProductValidationSteps.cs has `using ECommerce.Catalog.API.Features.Products;`
    - `dotnet build src/services/catalog/ECommerce.Catalog.Tests/ECommerce.Catalog.Tests.csproj --no-incremental` exits 0
    - `dotnet build src/services/catalog/ECommerce.Catalog.API/ECommerce.Catalog.API.csproj --no-incremental` exits 0
  </acceptance_criteria>
  <done>
    PaginationHelper.Clamp is the single source of truth for pagination bounds. ProductsEndpoints delegates to it. ProductValidationSteps calls it directly. A change to the clamping logic in PaginationHelper will now cause both the endpoint behavior and the unit test assertion to change in lockstep — the unit tests have regression value again.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PaginationHelper | Internal static utility; no external trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10-01 | Tampering | PaginationHelper.Clamp boundary conditions | accept | Extracted from production code with identical bounds — no behavior change; unit tests now verify real bounds rather than a copy |
</threat_model>

<verification>
After task completes:
1. `dotnet build src/services/catalog/ECommerce.Catalog.API/ECommerce.Catalog.API.csproj --no-incremental` exits 0
2. `dotnet build src/services/catalog/ECommerce.Catalog.Tests/ECommerce.Catalog.Tests.csproj --no-incremental` exits 0
3. PaginationHelper.cs exists and contains the Clamp static method
4. ProductsEndpoints.cs does not contain inline `if (page < 1)` clamping
5. ProductValidationSteps.cs does not contain ternary clamping inline in When_Validated
6. Both ProductsEndpoints.cs and ProductValidationSteps.cs call PaginationHelper.Clamp
</verification>

<success_criteria>
- Pagination clamping logic lives in exactly one place (PaginationHelper.Clamp)
- Unit tests exercise real production clamping code — changing the bounds in PaginationHelper will cause unit tests to fail
- All catalog builds and test compilations pass
</success_criteria>

<output>
Create .planning/phases/02-identity-catalog-gateway/02-10-SUMMARY.md when done
</output>
