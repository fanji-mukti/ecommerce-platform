---
phase: 02-identity-catalog-gateway
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/building-blocks/Contracts/Catalog/Events/V1/CatalogSeeded.cs
  - src/building-blocks/Tests.Common/ECommerce.Tests.Common.csproj
  - src/building-blocks/Tests.Common/PostgresFixture.cs
  - src/building-blocks/Tests.Common/ServiceWebApplicationFactory.cs
  - src/building-blocks/Tests.Common/Builders/ProductBuilder.cs
  - src/building-blocks/Tests.Common/Builders/UserBuilder.cs
autonomous: true
requirements:
  - INF-01
  - INF-02

must_haves:
  truths:
    - "CatalogSeeded event record exists in ECommerce.Catalog.Events.V1 namespace with all IMessageEnvelope fields and domain-specific fields (SeedId, ItemCount, SeededAt)"
    - "Contracts library compiles cleanly with zero new external dependencies introduced"
    - "Tests.Common project compiles and PostgresFixture can start/stop a Postgres Testcontainer"
    - "ProductBuilder and UserBuilder produce valid entity instances"
  artifacts:
    - path: "src/building-blocks/Contracts/Catalog/Events/V1/CatalogSeeded.cs"
      provides: "Event contract for MassTransit outbox/inbox wiring"
      contains: "record CatalogSeeded"
    - path: "src/building-blocks/Tests.Common/ECommerce.Tests.Common.csproj"
      provides: "Shared test infrastructure project"
      exports: ["PostgresFixture", "ServiceWebApplicationFactory", "ProductBuilder", "UserBuilder"]
    - path: "src/building-blocks/Tests.Common/PostgresFixture.cs"
      provides: "Per-class Testcontainers Postgres isolation"
      contains: "IAsyncLifetime"
    - path: "src/building-blocks/Tests.Common/Builders/ProductBuilder.cs"
      provides: "Test data builder for Product entity"
      contains: "class ProductBuilder"
  key_links:
    - from: "CatalogSeeded.cs"
      to: "IMessageEnvelope"
      via: ": IMessageEnvelope"
      pattern: "IMessageEnvelope"
    - from: "CatalogDbContext.DbInitializer"
      to: "CatalogSeeded record"
      via: "new CatalogSeeded(...)"
      pattern: "CatalogSeeded"
---

<objective>
Replace the Catalog placeholder contract with the real CatalogSeeded event record, and create the shared Tests.Common infrastructure that all Phase 2 service test projects will reference.

Purpose: CatalogSeeded is the proof event for MassTransit outbox/inbox wiring (INF-01, INF-02). Tests.Common provides the Testcontainers Postgres fixture, WebApplicationFactory base, and test data builders that prevent boilerplate duplication across the Identity, Catalog, Notifications, and Gateway test projects.

Output: CatalogSeeded.cs replacing Placeholder.cs, and a new Tests.Common class library with PostgresFixture, ServiceWebApplicationFactory, ProductBuilder, and UserBuilder.
</objective>

<execution_context>
@C:\Users\User\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\User\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-identity-catalog-gateway/02-CONTEXT.md
@.planning/phases/02-identity-catalog-gateway/02-RESEARCH.md
@.planning/phases/02-identity-catalog-gateway/02-PATTERNS.md

<interfaces>
<!-- Contracts baseline — must not change the namespace convention or add dependencies -->

From src/building-blocks/Contracts/Catalog/Events/V1/Placeholder.cs:
```csharp
using ECommerce.Contracts;

namespace ECommerce.Catalog.Events.V1;

public record CatalogServiceReady(
    Guid MessageId,
    Guid CorrelationId,
    Guid CausationId,
    DateTimeOffset OccurredAt
) : IMessageEnvelope;
```

From src/building-blocks/Contracts/Contracts.csproj — zero external dependencies (net10.0 + ImplicitUsings + Nullable only).

New CatalogSeeded shape (per D-11, D-12):
```csharp
public record CatalogSeeded(
    Guid MessageId, Guid CorrelationId, Guid CausationId, DateTimeOffset OccurredAt,
    Guid SeedId, int ItemCount, DateTimeOffset SeededAt
) : IMessageEnvelope;
```

Product entity fields (Claude's Discretion + RESEARCH.md):
Id (Guid), Name (string), Sku (string), Description (string), Price (decimal),
StockQuantity (int), Category (string), ImageUrl (string?), CreatedAt (DateTimeOffset)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace CatalogServiceReady placeholder with CatalogSeeded contract</name>
  <files>
    src/building-blocks/Contracts/Catalog/Events/V1/CatalogSeeded.cs
  </files>
  <read_first>
    - src/building-blocks/Contracts/Catalog/Events/V1/Placeholder.cs (current file to replace)
    - src/building-blocks/Contracts/Contracts.csproj (zero-dependency constraint)
    - src/building-blocks/Contracts/IMessageEnvelope.cs (interface contract to implement)
    - .planning/phases/02-identity-catalog-gateway/02-CONTEXT.md (D-11, D-12 decision details)
  </read_first>
  <behavior>
    - CatalogSeeded.cs in namespace ECommerce.Catalog.Events.V1 implements IMessageEnvelope
    - Primary constructor envelope fields first (MessageId, CorrelationId, CausationId, OccurredAt), then domain fields (SeedId, ItemCount, SeededAt)
    - Only using statement is "using ECommerce.Contracts;" — no additional dependencies
    - Placeholder.cs is deleted; the new file takes its place in the same directory
    - Contracts.csproj compiles without error after the swap
  </behavior>
  <action>
    Delete src/building-blocks/Contracts/Catalog/Events/V1/Placeholder.cs.

    Create src/building-blocks/Contracts/Catalog/Events/V1/CatalogSeeded.cs with namespace ECommerce.Catalog.Events.V1, single using ECommerce.Contracts, record primary constructor with parameters in order: Guid MessageId, Guid CorrelationId, Guid CausationId, DateTimeOffset OccurredAt, Guid SeedId, int ItemCount, DateTimeOffset SeededAt — implementing IMessageEnvelope.

    Verify the Contracts library builds: dotnet build src/building-blocks/Contracts/Contracts.csproj --configuration Release
  </action>
  <verify>
    <automated>dotnet build src/building-blocks/Contracts/Contracts.csproj --configuration Release --no-incremental</automated>
  </verify>
  <done>
    - Placeholder.cs is gone; CatalogSeeded.cs exists at Contracts/Catalog/Events/V1/
    - CatalogSeeded implements IMessageEnvelope with all 7 constructor parameters in correct order
    - dotnet build Contracts.csproj exits 0 with no warnings
    - Contracts.csproj has no new package references (zero-dependency constraint maintained)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create Tests.Common shared infrastructure project</name>
  <files>
    src/building-blocks/Tests.Common/ECommerce.Tests.Common.csproj
    src/building-blocks/Tests.Common/PostgresFixture.cs
    src/building-blocks/Tests.Common/ServiceWebApplicationFactory.cs
    src/building-blocks/Tests.Common/Builders/ProductBuilder.cs
    src/building-blocks/Tests.Common/Builders/UserBuilder.cs
  </files>
  <read_first>
    - src/building-blocks/Contracts/Contracts.csproj (baseline PropertyGroup pattern: net10.0, ImplicitUsings, Nullable)
    - src/services/catalog/ECommerce.Catalog.API/ECommerce.Catalog.API.csproj (existing csproj shape)
    - .planning/phases/02-identity-catalog-gateway/02-CONTEXT.md (D-22 through D-29 decisions)
    - .planning/phases/02-identity-catalog-gateway/02-PATTERNS.md (PostgresFixture and builder patterns)
    - .planning/phases/02-identity-catalog-gateway/02-RESEARCH.md (Pattern 10 — Testcontainers, package versions)
  </read_first>
  <behavior>
    - ECommerce.Tests.Common.csproj targets net10.0, ImplicitUsings enabled, Nullable enabled, IsPackable=false, IsTestProject=true
    - Dependencies: Testcontainers.PostgreSql 4.12.0, FluentAssertions 8.10.0, NSubstitute 5.3.0, MassTransit.Testing 8.3.6, Microsoft.AspNetCore.Mvc.Testing, xunit.v3 (or xunit 2.9.x if v3 package name fails)
    - PostgresFixture implements IAsyncLifetime, uses PostgreSqlBuilder with database "test-db", username "test", password "test". Exposes ConnectionString property.
    - ServiceWebApplicationFactory is a generic WebApplicationFactory base class that accepts a connection string constructor arg and overrides ConfigureWebHost to swap the "postgres" connection string.
    - ProductBuilder fluent builder produces Product entities with defaults: Name="Test Product", Sku="TST-001", Description="A test product.", Price=9.99m, StockQuantity=100, Category="Electronics". Fluent WithName/WithPrice/WithCategory/WithStock/WithSku methods. Build() returns new Product.
    - UserBuilder fluent builder: Email="test@example.com", Password="Password123!". Fluent WithEmail/WithPassword. Build() returns (Email, Password) tuple or a UserBuilder record.
    - All classes in namespace ECommerce.Tests.Common; builders in ECommerce.Tests.Common.Builders
  </behavior>
  <action>
    Create src/building-blocks/Tests.Common/ directory structure.

    ECommerce.Tests.Common.csproj: Sdk="Microsoft.NET.Sdk", PropertyGroup with TargetFramework=net10.0, ImplicitUsings=enable, Nullable=enable, IsPackable=false, IsTestProject=true. ItemGroup with package references: Testcontainers.PostgreSql Version="4.12.0", FluentAssertions Version="8.10.0", NSubstitute Version="5.3.0", MassTransit.Testing Version="8.3.6", Microsoft.AspNetCore.Mvc.Testing (no version pin — picks up from SDK). Second ItemGroup: ProjectReference to ../Contracts/Contracts.csproj.

    PostgresFixture.cs: IAsyncLifetime with private readonly field PostgreSqlContainer using PostgreSqlBuilder().WithDatabase("test-db").WithUsername("test").WithPassword("test").Build(). Public string ConnectionString property returning _container.GetConnectionString(). InitializeAsync calls _container.StartAsync(); DisposeAsync calls _container.DisposeAsync().

    ServiceWebApplicationFactory.cs: Generic class ServiceWebApplicationFactory<TProgram> extends WebApplicationFactory<TProgram> where TProgram : class. Constructor takes string connectionString. Override ConfigureWebHost: call builder.ConfigureAppConfiguration with delegate that replaces ConnectionStrings:postgres in IConfiguration.

    ProductBuilder.cs: Fluent builder for the Product entity type (from ECommerce.Catalog.API.Features.Products). Because Tests.Common is a shared project across services (not referencing any specific service), ProductBuilder.Build() should return a plain object or anonymous type. Use a concrete ProductData record/class inside Tests.Common: record ProductData(Guid Id, string Name, string Sku, string Description, decimal Price, int StockQuantity, string Category, string? ImageUrl, DateTimeOffset CreatedAt). ProductBuilder builds a ProductData. Service integration tests that need to seed a real Product entity will map ProductData to the service's Product entity using matching property names.

    UserBuilder.cs: Fluent builder producing UserData record: record UserData(string Email, string Password). Defaults: Email="test@example.com", Password="Password123!". WithEmail/WithPassword fluent methods.

    Verify: dotnet build src/building-blocks/Tests.Common/ECommerce.Tests.Common.csproj --configuration Release
  </action>
  <verify>
    <automated>dotnet build src/building-blocks/Tests.Common/ECommerce.Tests.Common.csproj --configuration Release --no-incremental</automated>
  </verify>
  <done>
    - ECommerce.Tests.Common.csproj builds cleanly (exit 0)
    - PostgresFixture.cs, ServiceWebApplicationFactory.cs, ProductBuilder.cs, UserBuilder.cs all exist with correct namespaces
    - All MassTransit references pin Version="8.3.6"
    - No floating package versions in the csproj
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Contracts library (shared) | Read by all services — no runtime trust boundary; compile-time only |
| Test infrastructure | Test-only project; never deployed to production |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-SC | Tampering | npm/pip/cargo installs | accept | No npm/pip/cargo in this plan — .NET NuGet only |
| T-02-01-01 | Tampering | Testcontainers.PostgreSql package | accept | Verified established package from testcontainers org (see RESEARCH.md Package Legitimacy Audit); test-only dependency, not deployed |
| T-02-01-02 | Information Disclosure | Hardcoded test DB credentials in PostgresFixture | accept | Credentials ("test"/"test") are for ephemeral Testcontainers instances only; never reach production; test isolation is per-class |
</threat_model>

<verification>
After both tasks complete:

1. Contracts.csproj builds: `dotnet build src/building-blocks/Contracts/Contracts.csproj --configuration Release` exits 0
2. Tests.Common.csproj builds: `dotnet build src/building-blocks/Tests.Common/ECommerce.Tests.Common.csproj --configuration Release` exits 0
3. CatalogSeeded.cs exists and contains "record CatalogSeeded" and "IMessageEnvelope"
4. Placeholder.cs no longer exists at Contracts/Catalog/Events/V1/Placeholder.cs
5. PostgresFixture.cs contains "IAsyncLifetime" and "PostgreSqlBuilder"
6. ProductBuilder.cs contains "class ProductBuilder" and "Build()"
7. All MassTransit references in Tests.Common.csproj use Version="8.3.6"
</verification>

<success_criteria>
- CatalogSeeded event record in ECommerce.Catalog.Events.V1 namespace with correct 7-param primary constructor
- Contracts library compiles with zero new external dependencies
- Tests.Common project compiles with all 5 expected types (PostgresFixture, ServiceWebApplicationFactory, ProductBuilder, UserBuilder, and supporting data records)
- All subsequent Phase 2 plans can reference Tests.Common without modification
</success_criteria>

<output>
Create .planning/phases/02-identity-catalog-gateway/02-01-SUMMARY.md when done
</output>
