---
phase: 02-identity-catalog-gateway
plan: 04
type: execute
wave: 1
depends_on:
  - 02-01
files_modified:
  - src/services/notifications/ECommerce.Notifications.API/ECommerce.Notifications.API.csproj
  - src/services/notifications/ECommerce.Notifications.API/Program.cs
  - src/services/notifications/ECommerce.Notifications.API/Data/NotificationsDbContext.cs
  - src/services/notifications/ECommerce.Notifications.API/Data/DbInitializer.cs
  - src/services/notifications/ECommerce.Notifications.API/Consumers/CatalogSeededConsumer.cs
  - src/services/notifications/ECommerce.Notifications.Tests/ECommerce.Notifications.Tests.csproj
  - src/services/notifications/ECommerce.Notifications.Tests/Integration/CatalogSeededConsumerTests.cs
  - src/services/notifications/ECommerce.Notifications.Tests/Integration/CatalogSeededConsumerSteps.cs
  - src/services/notifications/Notifications.sln
autonomous: true
requirements:
  - INF-02

must_haves:
  truths:
    - "CatalogSeededConsumer processes CatalogSeeded messages and logs receipt"
    - "Notifications service's NotificationsDbContext has InboxState table (MassTransit idempotent inbox)"
    - "Delivering the same CatalogSeeded message twice (same MessageId) results in the consumer body executing exactly once (idempotent inbox deduplication)"
    - "Notifications service starts, applies EF migrations, and reaches /health: 200 without errors"
  artifacts:
    - path: "src/services/notifications/ECommerce.Notifications.API/Consumers/CatalogSeededConsumer.cs"
      provides: "IConsumer<CatalogSeeded> with idempotent inbox wired"
      contains: "IConsumer"
    - path: "src/services/notifications/ECommerce.Notifications.API/Data/NotificationsDbContext.cs"
      provides: "EF Core DbContext with MassTransit inbox tables"
      contains: "AddInboxStateEntity"
    - path: "src/services/notifications/ECommerce.Notifications.Tests/Integration/CatalogSeededConsumerTests.cs"
      provides: "Forced redelivery test proving inbox deduplication"
      contains: "DuplicateMessageId"
  key_links:
    - from: "MassTransit per-endpoint outbox"
      to: "NotificationsDbContext InboxState table"
      via: "UseEntityFrameworkOutbox<NotificationsDbContext>(context)"
      pattern: "UseEntityFrameworkOutbox"
    - from: "CatalogSeededConsumer"
      to: "CatalogSeeded.MessageId"
      via: "inbox deduplication by MessageId"
      pattern: "MessageId"
---

<objective>
Implement the Notifications service as the first real MassTransit consumer in the system, wired with the idempotent inbox pattern. The CatalogSeededConsumer receives CatalogSeeded events and the inbox deduplicates redelivered messages by MessageId. An integration test using the MassTransit InMemory test harness proves deduplication.

Purpose: Delivers INF-02 (idempotent inbox). Demonstrates that the outbox/inbox pattern works end-to-end and that duplicate message delivery (a real ASB guarantee) does not produce duplicate processing.

Output: Notifications service with NotificationsDbContext (inbox tables), CatalogSeededConsumer, EF migrations, a DbInitializer, and an xUnit integration test proving the D-14 forced redelivery scenario.
</objective>

<execution_context>
@C:\Users\User\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\User\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-identity-catalog-gateway/02-CONTEXT.md
@.planning/phases/02-identity-catalog-gateway/02-RESEARCH.md
@.planning/phases/02-identity-catalog-gateway/02-PATTERNS.md
@.planning/phases/02-identity-catalog-gateway/02-01-SUMMARY.md

<interfaces>
<!-- Existing Notifications stub — all existing lines must survive -->

From src/services/notifications/ECommerce.Notifications.API/ECommerce.Notifications.API.csproj:
Same baseline as other services. Existing packages: Microsoft.AspNetCore.OpenApi 10.0.8, Serilog.AspNetCore 10.0.0, Serilog.Sinks.OpenTelemetry 4.2.0, OpenTelemetry.Extensions.Hosting 1.15.3, OpenTelemetry.Instrumentation.AspNetCore 1.15.2, OpenTelemetry.Exporter.OpenTelemetryProtocol 1.15.3

New packages to add: Aspire.Npgsql.EntityFrameworkCore.PostgreSQL 13.4.4, MassTransit 8.3.6, MassTransit.Azure.ServiceBus.Core 8.3.6, MassTransit.EntityFrameworkCore 8.3.6

CatalogSeeded contract (from plan 02-01):
record CatalogSeeded(Guid MessageId, Guid CorrelationId, Guid CausationId, DateTimeOffset OccurredAt, Guid SeedId, int ItemCount, DateTimeOffset SeededAt) : IMessageEnvelope
Namespace: ECommerce.Catalog.Events.V1

MassTransit Notifications Program.cs wiring (PATTERNS.md):
builder.AddNpgsqlDbContext<NotificationsDbContext>("postgres")
AddConsumer<CatalogSeededConsumer>()
AddEntityFrameworkOutbox<NotificationsDbContext>(o => { o.UsePostgres(); /* NO UseBusOutbox — consuming only */ })
AddConfigureEndpointsCallback((context, name, cfg) => { cfg.UseEntityFrameworkOutbox<NotificationsDbContext>(context); })
UsingAzureServiceBus with Host from ConnectionString "messaging" + ConfigureEndpoints(context)

CatalogSeededConsumer pattern (PATTERNS.md):
public class CatalogSeededConsumer(NotificationsDbContext db, ILogger<CatalogSeededConsumer> logger) : IConsumer<CatalogSeeded>
-- logs SeedId and ItemCount, calls db.SaveChangesAsync — no real notification logic in Phase 2

D-14 forced redelivery test: publish same CatalogSeeded twice with same MessageId, verify consumer body invoked exactly once
-- uses AddMassTransitTestHarness + InMemory (no ASB emulator needed)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire MassTransit inbox, NotificationsDbContext, and CatalogSeededConsumer</name>
  <files>
    src/services/notifications/ECommerce.Notifications.API/ECommerce.Notifications.API.csproj
    src/services/notifications/ECommerce.Notifications.API/Program.cs
    src/services/notifications/ECommerce.Notifications.API/Data/NotificationsDbContext.cs
    src/services/notifications/ECommerce.Notifications.API/Data/DbInitializer.cs
    src/services/notifications/ECommerce.Notifications.API/Consumers/CatalogSeededConsumer.cs
    src/services/notifications/Notifications.sln
  </files>
  <read_first>
    - src/services/notifications/ECommerce.Notifications.API/Program.cs (existing stub — all lines survive)
    - src/services/notifications/ECommerce.Notifications.API/ECommerce.Notifications.API.csproj (packages to preserve)
    - src/services/notifications/Notifications.sln (existing solution structure)
    - src/building-blocks/Contracts/Catalog/Events/V1/CatalogSeeded.cs (contract to consume)
    - .planning/phases/02-identity-catalog-gateway/02-PATTERNS.md (CatalogSeededConsumer pattern, Notifications Program.cs pattern, NotificationsDbContext pattern)
    - .planning/phases/02-identity-catalog-gateway/02-RESEARCH.md (Pattern 5 — MassTransit inbox, Pitfall 4 UseBusOutbox warning)
  </read_first>
  <behavior>
    - ECommerce.Notifications.API.csproj: preserve all 6 existing packages. Add new ItemGroup: Aspire.Npgsql.EntityFrameworkCore.PostgreSQL 13.4.4, MassTransit 8.3.6, MassTransit.Azure.ServiceBus.Core 8.3.6, MassTransit.EntityFrameworkCore 8.3.6. All MassTransit versions pinned at "8.3.6". No UseBusOutbox call (Notifications is consumer-only — not a publisher of outbox messages).
    - NotificationsDbContext.cs in namespace ECommerce.Notifications.API.Data: extends DbContext, primary constructor takes DbContextOptions<NotificationsDbContext>, OnModelCreating calls base + modelBuilder.AddInboxStateEntity() + modelBuilder.AddOutboxMessageEntity() + modelBuilder.AddOutboxStateEntity(). These three MT methods are required even for consumer-only services.
    - DbInitializer.cs in namespace ECommerce.Notifications.API.Data: IHostedService, creates scope, gets NotificationsDbContext, calls MigrateAsync. StopAsync returns completed task. Idempotent.
    - CatalogSeededConsumer.cs in namespace ECommerce.Notifications.API.Consumers: class implementing IConsumer<CatalogSeeded>. Constructor injects NotificationsDbContext and ILogger<CatalogSeededConsumer>. Consume method: logs "CatalogSeeded received: SeedId={SeedId}, ItemCount={ItemCount}", calls db.SaveChangesAsync(context.CancellationToken). No real notification storage in Phase 2 — log-only.
    - Program.cs expanded: add builder.AddNpgsqlDbContext<NotificationsDbContext>("postgres"). Add MassTransit with AddConsumer<CatalogSeededConsumer>(), AddEntityFrameworkOutbox<NotificationsDbContext>(o => { o.UsePostgres(); /* no UseBusOutbox */ }), AddConfigureEndpointsCallback for UseEntityFrameworkOutbox per endpoint, UsingAzureServiceBus with ConfigureEndpoints. Add AddHostedService<DbInitializer>(). All original Serilog + OTel + /health lines preserved.
    - Run EF Core migration: from src/services/notifications/ECommerce.Notifications.API/ run dotnet ef migrations add InitialCreate.
  </behavior>
  <action>
    Add new ItemGroup to ECommerce.Notifications.API.csproj. Ensure MassTransit is Version="8.3.6" on all three packages. No Mapperly or FluentValidation needed (Notifications is event-only in Phase 2).

    Create Data/NotificationsDbContext.cs with all three MT outbox entity registrations (AddInboxStateEntity, AddOutboxMessageEntity, AddOutboxStateEntity).

    Create Data/DbInitializer.cs (IHostedService that runs MigrateAsync only).

    Create Consumers/CatalogSeededConsumer.cs implementing IConsumer<CatalogSeeded>. Import CatalogSeeded from namespace ECommerce.Catalog.Events.V1. Log receipt, call SaveChangesAsync. No conditional logic on MessageId — the inbox deduplication is handled by MassTransit's UseEntityFrameworkOutbox middleware, not in the consumer body.

    Expand Program.cs: add AddNpgsqlDbContext, AddMassTransit (as described above — note: AddEntityFrameworkOutbox has NO UseBusOutbox call per RESEARCH.md Pitfall 4; that call is only for publishing services like Catalog), AddHostedService<DbInitializer>.

    Run migration: dotnet ef migrations add InitialCreate from the Notifications API directory.

    Build: dotnet build src/services/notifications/Notifications.sln --configuration Release
  </action>
  <verify>
    <automated>dotnet build src/services/notifications/Notifications.sln --configuration Release --no-incremental</automated>
  </verify>
  <done>
    - Notifications.sln builds cleanly (exit 0)
    - NotificationsDbContext.cs contains AddInboxStateEntity()
    - CatalogSeededConsumer.cs contains "IConsumer" and logs SeedId and ItemCount
    - Program.cs does NOT contain UseBusOutbox (Notifications only consumes — no outbox drain)
    - Program.cs contains AddConfigureEndpointsCallback with UseEntityFrameworkOutbox
    - Data/Migrations/ directory created with InitialCreate
    - All MassTransit package references at Version="8.3.6"
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Forced redelivery integration test and test project</name>
  <files>
    src/services/notifications/ECommerce.Notifications.Tests/ECommerce.Notifications.Tests.csproj
    src/services/notifications/ECommerce.Notifications.Tests/Integration/CatalogSeededConsumerTests.cs
    src/services/notifications/ECommerce.Notifications.Tests/Integration/CatalogSeededConsumerSteps.cs
    src/services/notifications/Notifications.sln
  </files>
  <read_first>
    - src/services/notifications/ECommerce.Notifications.API/Consumers/CatalogSeededConsumer.cs (from Task 1)
    - src/building-blocks/Contracts/Catalog/Events/V1/CatalogSeeded.cs (message shape)
    - .planning/phases/02-identity-catalog-gateway/02-CONTEXT.md (D-14 test specification)
    - .planning/phases/02-identity-catalog-gateway/02-RESEARCH.md (Pattern 6 — MassTransit InMemory test harness; note about v8 API name differences)
    - src/building-blocks/Tests.Common/ECommerce.Tests.Common.csproj (project reference)
  </read_first>
  <behavior>
    - ECommerce.Notifications.Tests.csproj: net10.0, IsPackable=false, IsTestProject=true. References: xunit.v3 (or xunit 2.9.x fallback), FluentAssertions 8.10.0, MassTransit.Testing 8.3.6, Microsoft.AspNetCore.Mvc.Testing. ProjectReference to ECommerce.Notifications.API.csproj and ECommerce.Tests.Common.csproj.
    - D-14 forced redelivery test: CatalogSeededConsumerTests.cs (two-class pattern with Steps).
    - Test: "CatalogSeededConsumer_WhenSameMessageIdDeliveredTwice_ProcessesExactlyOnce"
    - Steps.Given: set up AddMassTransitTestHarness with AddConsumer<CatalogSeededConsumer>. Note: the InMemory harness in MassTransit 8.x may use a slightly different API than v9. Use AddMassTransitTestHarness() method if available; fall back to InMemoryTestHarness() direct instantiation if the DI-based API is unavailable in 8.3.6.
    - Steps.When: build a CatalogSeeded message with a specific Guid messageId. Publish it twice to harness.Bus with identical content (same MessageId).
    - Steps.Then: assert that harness.Consumed.Select<CatalogSeeded>() count == 1 (consumer body invoked exactly once). Note: MassTransit InMemory harness in v8 deduplicates by the transport-level MessageId header, not by the record's MessageId field. The test assertion must be on harness.Consumed count, not on the DB. See RESEARCH.md Pattern 6 note about the v8 InMemory harness limitation.
    - Add ECommerce.Notifications.Tests to Notifications.sln.
  </behavior>
  <action>
    Create ECommerce.Notifications.Tests.csproj with test project settings. Add MassTransit.Testing Version="8.3.6". Add to Notifications.sln.

    Create Integration/ subdirectory. Create CatalogSeededConsumerTests.cs with the D-14 forced redelivery test using the D-25 two-class pattern (Tests + Steps).

    In CatalogSeededConsumerSteps.cs:
    - Setup: build ServiceCollection with AddMassTransitTestHarness(x => x.AddConsumer<CatalogSeededConsumer>()). Build ServiceProvider. Call GetTestHarness() to get the harness. Start harness.
    - When_SameMessagePublishedTwice(messageId): create CatalogSeeded with MessageId=messageId and other fields filled with NewGuid()/UtcNow. Call harness.Bus.Publish() twice with the identical message object. Call harness.InactivityTask (wait for consumers to finish).
    - Then_ConsumedExactlyOnce(): assert harness.Consumed.Select<CatalogSeeded>() has exactly 1 item.

    If the MassTransit v8 InMemory harness API differs from the v9 pattern in RESEARCH.md (assumption A7 from RESEARCH.md), adjust to use the v8 InMemoryTestHarness API instead. The test intent is the same: prove the consumer body runs once for duplicate deliveries.

    Run tests: dotnet test src/services/notifications/Notifications.sln --configuration Release
  </action>
  <verify>
    <automated>dotnet test src/services/notifications/Notifications.sln --configuration Release</automated>
  </verify>
  <done>
    - ECommerce.Notifications.Tests.csproj compiles and is in Notifications.sln
    - CatalogSeededConsumerTests.cs contains test method "CatalogSeededConsumer_WhenSameMessageIdDeliveredTwice_ProcessesExactlyOnce" (or similar name proving deduplication)
    - dotnet test exits 0 — the forced redelivery test passes
    - MassTransit.Testing reference is Version="8.3.6"
    - Test uses InMemory harness — no ASB emulator dependency
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ASB → Notifications consumer | Messages arrive from Azure Service Bus — at-least-once delivery means duplicates are expected |
| InboxState table | MassTransit stores processed MessageId here — must not be writable by external actors |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04-01 | Tampering | MassTransit version floating to 9.x (commercial) | mitigate | All four MassTransit package references pinned at Version="8.3.6" in csproj |
| T-02-04-02 | Denial of Service | Consumer processing duplicate messages (missing inbox) | mitigate | UseEntityFrameworkOutbox per endpoint wired in AddConfigureEndpointsCallback; InboxState table created via migration |
| T-02-04-03 | Tampering | NotificationsDbContext missing MT inbox tables | mitigate | AddInboxStateEntity(), AddOutboxMessageEntity(), AddOutboxStateEntity() all called in OnModelCreating — enforced by integration test requiring migration to exist |
| T-02-04-SC | Tampering | NuGet package installs | mitigate | MassTransit 8.3.6 and MassTransit.Testing 8.3.6 pre-audited in RESEARCH.md Package Legitimacy Audit |
</threat_model>

<verification>
After both tasks complete:

1. `dotnet build src/services/notifications/Notifications.sln --configuration Release` exits 0
2. `dotnet test src/services/notifications/Notifications.sln --configuration Release` exits 0
3. NotificationsDbContext.cs contains AddInboxStateEntity()
4. CatalogSeededConsumer.cs implements IConsumer<CatalogSeeded>
5. Program.cs contains AddConfigureEndpointsCallback with UseEntityFrameworkOutbox
6. Program.cs does NOT contain UseBusOutbox (Notifications is consumer-only)
7. Data/Migrations/ directory exists with InitialCreate migration
8. Forced redelivery test exists and passes: consumer runs once for two duplicate messages
</verification>

<success_criteria>
- Notifications service compiles, applies EF migrations, /health returns 200
- CatalogSeededConsumer handles CatalogSeeded messages
- MassTransit inbox wired via UseEntityFrameworkOutbox on each endpoint
- Forced redelivery test (D-14) passes: same MessageId published twice → consumer invoked once
- No UseBusOutbox in Notifications (publisher-side only — Catalog has it)
</success_criteria>

<output>
Create .planning/phases/02-identity-catalog-gateway/02-04-SUMMARY.md when done
</output>
