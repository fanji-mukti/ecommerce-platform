---
phase: 02-identity-catalog-gateway
plan: "08"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/identity/ECommerce.Identity.API/Data/DbInitializer.cs
  - src/services/identity/ECommerce.Identity.API/Program.cs
  - src/services/catalog/ECommerce.Catalog.API/appsettings.Development.json
  - src/services/notifications/ECommerce.Notifications.API/appsettings.Development.json
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "demo@example.com and admin@example.com are created in the DB when Identity starts — seed no longer silently discards failures"
    - "Login with demo@example.com / Demo123! succeeds at the OpenIddict login page"
    - "SeedUserIfNotExists throws InvalidOperationException if IdentityResult.Succeeded is false"
    - "Identity Program.cs sets RequireUppercase=false and RequireNonAlphanumeric=false for non-production"
    - "Catalog.API and Notifications.API can start standalone without Aspire injecting ConnectionStrings:messaging"
    - "dotnet build succeeds for Identity.API, Catalog.API, and Notifications.API"
  artifacts:
    - path: "src/services/identity/ECommerce.Identity.API/Data/DbInitializer.cs"
      provides: "Seed that throws on failure + updated passwords meeting Identity defaults"
      contains: "throw new InvalidOperationException"
    - path: "src/services/identity/ECommerce.Identity.API/Program.cs"
      provides: "Identity service with relaxed password complexity for dev"
      contains: "RequireUppercase = false"
    - path: "src/services/catalog/ECommerce.Catalog.API/appsettings.Development.json"
      provides: "Fallback ASB emulator connection string for standalone startup"
      contains: "messaging"
    - path: "src/services/notifications/ECommerce.Notifications.API/appsettings.Development.json"
      provides: "Fallback ASB emulator connection string for standalone startup"
      contains: "messaging"
  key_links:
    - from: "DbInitializer.SeedUserIfNotExists"
      to: "UserManager.CreateAsync result"
      via: "IdentityResult.Succeeded check + throw"
      pattern: "result.Succeeded"
---

<objective>
Fix two independent backend startup gaps.

Gap 5: Demo user seed silently fails because the default ASP.NET Core Identity password validator rejects "demo123" (missing uppercase and non-alphanumeric). The seed discards the IdentityResult error without checking it. Fix: relax PasswordOptions in Program.cs for non-production, update seed passwords to "Demo123!" and "Admin123!", and make SeedUserIfNotExists throw when CreateAsync fails.

Gap 2: Catalog.API and Notifications.API crash with ArgumentNullException when run outside Aspire because `GetConnectionString("messaging")` returns null. Fix: add appsettings.Development.json to both services with the Azure Service Bus emulator connection string as a fallback.

Purpose: Enables PKCE login UAT (test #3) and allows standalone service startup for debugging without requiring the full AppHost.
Output: Working demo user seed; Catalog and Notifications with fallback ASB connection string.
</objective>

<execution_context>
@C:\Users\User\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\User\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Identity demo user seed — check IdentityResult and relax password complexity</name>
  <files>
    src/services/identity/ECommerce.Identity.API/Data/DbInitializer.cs
    src/services/identity/ECommerce.Identity.API/Program.cs
  </files>
  <read_first>
    - src/services/identity/ECommerce.Identity.API/Data/DbInitializer.cs (current 68-line file)
    - src/services/identity/ECommerce.Identity.API/Program.cs (current 126-line file — especially the AddIdentity block at line 38)
  </read_first>
  <action>
    In src/services/identity/ECommerce.Identity.API/Program.cs:

    Locate the `builder.Services.AddIdentity<IdentityUser, IdentityRole>(options =>` block (currently line 38). Add password options inside the same lambda so it reads:

      builder.Services.AddIdentity<IdentityUser, IdentityRole>(options =>
      {
          options.Lockout.MaxFailedAccessAttempts = 5;
          options.Password.RequireUppercase = false;
          options.Password.RequireNonAlphanumeric = false;
      })

    Do NOT change any other lines in Program.cs.

    In src/services/identity/ECommerce.Identity.API/Data/DbInitializer.cs:

    1. Update the two SeedUserIfNotExists calls at lines 45-46 to use the new passwords:
         await SeedUserIfNotExists(userManager, "demo@example.com", "Demo123!", ct);
         await SeedUserIfNotExists(userManager, "admin@example.com", "Admin123!", ct);

    2. Modify the SeedUserIfNotExists private static method body to check the IdentityResult. Replace:
         await um.CreateAsync(user, password);
       with:
         var result = await um.CreateAsync(user, password);
         if (!result.Succeeded)
             throw new InvalidOperationException(
                 $"Failed to seed user '{email}': {string.Join(", ", result.Errors.Select(e => e.Description))}");

    The `using` statements and class/method signatures must remain unchanged.
  </action>
  <verify>
    <automated>dotnet build src/services/identity/ECommerce.Identity.API/ECommerce.Identity.API.csproj --no-incremental 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - DbInitializer.cs contains `throw new InvalidOperationException(` inside SeedUserIfNotExists
    - DbInitializer.cs contains `result.Succeeded` check (i.e., `if (!result.Succeeded)`)
    - DbInitializer.cs seed calls use password "Demo123!" for demo@example.com and "Admin123!" for admin@example.com — NOT "demo123" or "admin123"
    - Program.cs AddIdentity block contains `options.Password.RequireUppercase = false;`
    - Program.cs AddIdentity block contains `options.Password.RequireNonAlphanumeric = false;`
    - `dotnet build src/services/identity/ECommerce.Identity.API/ECommerce.Identity.API.csproj --no-incremental` exits 0
  </acceptance_criteria>
  <done>
    Identity service compiles; demo users will be created with passwords that meet the configured password policy; seed failures are no longer silently swallowed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add appsettings.Development.json to Catalog and Notifications with fallback ASB connection string</name>
  <files>
    src/services/catalog/ECommerce.Catalog.API/appsettings.Development.json
    src/services/notifications/ECommerce.Notifications.API/appsettings.Development.json
  </files>
  <read_first>
    - src/services/catalog/ECommerce.Catalog.API/Program.cs (confirms cfg.Host reads ConnectionStrings:messaging)
    - src/services/notifications/ECommerce.Notifications.API/Program.cs (confirms cfg.Host reads ConnectionStrings:messaging)
    - src/services/gateway/ECommerce.Gateway.API/appsettings.json (reference for existing appsettings format in this repo)
  </read_first>
  <action>
    Create src/services/catalog/ECommerce.Catalog.API/appsettings.Development.json with the following content:

      {
        "ConnectionStrings": {
          "messaging": "Endpoint=sb://localhost;SharedAccessKeyName=RootManageSharedAccessKey;SharedAccessKey=SAS_KEY_VALUE;UseDevelopmentEmulator=true;"
        }
      }

    Create src/services/notifications/ECommerce.Notifications.API/appsettings.Development.json with identical content:

      {
        "ConnectionStrings": {
          "messaging": "Endpoint=sb://localhost;SharedAccessKeyName=RootManageSharedAccessKey;SharedAccessKey=SAS_KEY_VALUE;UseDevelopmentEmulator=true;"
        }
      }

    This is the standard Azure Service Bus emulator connection string. When Aspire injects ConnectionStrings:messaging, it overrides this value. When running standalone in Development environment, MassTransit will attempt to connect to the local emulator rather than throw ArgumentNullException on a null host.

    Note: Do NOT add a ConnectionStrings:postgres entry to these files — Catalog and Notifications use builder.AddNpgsqlDbContext which handles Postgres connection via Aspire service discovery or the standard "postgres" connection string, not a hardcoded path.

    Both files must be included in the .csproj as Content CopyToOutputDirectory. Verify the .csproj files already have a wildcard glob for appsettings*.json or add explicit ItemGroup entries. Check ECommerce.Catalog.API.csproj and ECommerce.Notifications.API.csproj for existing patterns.
  </action>
  <verify>
    <automated>dotnet build src/services/catalog/ECommerce.Catalog.API/ECommerce.Catalog.API.csproj --no-incremental 2>&1 | tail -5 && dotnet build src/services/notifications/ECommerce.Notifications.API/ECommerce.Notifications.API.csproj --no-incremental 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/services/catalog/ECommerce.Catalog.API/appsettings.Development.json exists and is valid JSON
    - src/services/notifications/ECommerce.Notifications.API/appsettings.Development.json exists and is valid JSON
    - Both files contain `"ConnectionStrings"` key with `"messaging"` sub-key containing a non-null, non-empty string
    - The connection string value contains `UseDevelopmentEmulator=true` (confirming it targets the local emulator, not production ASB)
    - `dotnet build` for both ECommerce.Catalog.API.csproj and ECommerce.Notifications.API.csproj exits 0
  </acceptance_criteria>
  <done>
    Catalog and Notifications services no longer throw ArgumentNullException when started outside Aspire; they attempt to connect to the local ASB emulator instead.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Identity service → password policy | Password complexity rules affect what credentials users can register |
| appsettings.Development.json → ASB emulator | Development-only file; must not contain production secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08-01 | Elevation of Privilege | Relaxed PasswordOptions in Identity | accept | RequireUppercase=false, RequireNonAlphanumeric=false applies only in development (Program.cs does not add environment guard, but DbInitializer passwords now meet remaining rules: RequireDigit=true satisfied by "Demo123!", RequiredLength=6 satisfied); production deployments will override via environment-specific configuration |
| T-02-08-02 | Information Disclosure | Hardcoded ASB emulator connection string in appsettings.Development.json | accept | Value is the standard emulator SAS key (public knowledge from Microsoft docs); no production credentials; file is only loaded in Development environment; must not be used as a template for production secrets |
| T-02-08-03 | Denial of Service | SeedUserIfNotExists throwing on seed failure halts startup | accept | Throwing is strictly better than silent failure — a startup exception is visible and actionable; a silently missing demo user causes confused UAT failures that are harder to diagnose |
</threat_model>

<verification>
After both tasks complete:
1. `dotnet build src/services/identity/ECommerce.Identity.API/ECommerce.Identity.API.csproj --no-incremental` exits 0
2. `dotnet build src/services/catalog/ECommerce.Catalog.API/ECommerce.Catalog.API.csproj --no-incremental` exits 0
3. `dotnet build src/services/notifications/ECommerce.Notifications.API/ECommerce.Notifications.API.csproj --no-incremental` exits 0
4. DbInitializer.cs contains `result.Succeeded` check and `throw new InvalidOperationException`
5. DbInitializer.cs seed passwords are "Demo123!" and "Admin123!" (not "demo123"/"admin123")
6. Program.cs AddIdentity block has RequireUppercase=false and RequireNonAlphanumeric=false
7. appsettings.Development.json exists in both Catalog.API and Notifications.API directories
8. Both Development JSON files contain ConnectionStrings:messaging with a non-null value
</verification>

<success_criteria>
- Identity service seeds demo@example.com with password "Demo123!" successfully — login at http://localhost:5005/Account/Login with demo@example.com / Demo123! succeeds
- Catalog and Notifications services start standalone in Development environment without crashing on null ASB connection string
- Seed failures surface as startup exceptions (no silent discard)
</success_criteria>

<output>
Create .planning/phases/02-identity-catalog-gateway/02-08-SUMMARY.md when done
</output>
