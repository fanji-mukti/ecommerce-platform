---
phase: 05-fulfillment-notifications
plan: 06
type: execute
wave: 3
depends_on: ["05-04"]
files_modified:
  - src/services/fulfillment/Fulfillment.sln
  - src/services/fulfillment/ECommerce.Fulfillment.Tests/ECommerce.Fulfillment.Tests.csproj
  - src/services/fulfillment/ECommerce.Fulfillment.Tests/Integration/OrderPaidConsumerSteps.cs
  - src/services/fulfillment/ECommerce.Fulfillment.Tests/Integration/OrderPaidConsumerTests.cs
  - src/services/fulfillment/ECommerce.Fulfillment.Tests/Integration/OrderPaidInboxDeduplicationSteps.cs
  - src/services/fulfillment/ECommerce.Fulfillment.Tests/Integration/OrderPaidInboxDeduplicationTests.cs
autonomous: true
requirements: [FUL-01, FUL-02]

must_haves:
  truths:
    - "OrderPaidConsumer schedules OrderShipped only when NewStatus is Paid, carrying the correct CheckoutId/UserId — proven against a real MassTransit test harness, not just by code review (FUL-01/FUL-02)."
    - "A redelivered OrderStatusChanged (same transport MessageId) is processed at most once by OrderPaidConsumer — proven against a real Postgres-backed EF Core inbox, mirroring the existing CatalogSeededInboxDeduplicationSteps pattern (INF-02)."
  artifacts:
    - path: "src/services/fulfillment/ECommerce.Fulfillment.Tests/ECommerce.Fulfillment.Tests.csproj"
      provides: "New Test project, added to Fulfillment.sln, picked up automatically by the existing CI matrix (no workflow file change needed — Fulfillment.sln is already listed)"
  key_links:
    - from: "OrderPaidConsumerSteps.cs"
      to: "OrderPaidConsumer"
      via: "MassTransit.Testing in-memory harness, harness.Scheduled.Select<OrderShipped>()"
      pattern: "Scheduled\\.Select<OrderShipped>"
---

<objective>
Prove Fulfillment's `OrderPaidConsumer` (05-04) independently and correctly: filter behavior against an in-memory MassTransit test harness, and redelivery idempotency against a real Postgres-backed EF Core inbox — mirroring the exact two-class `*Steps`/`*Tests` pattern this repo already established for `CatalogSeededConsumer`. This is Fulfillment's first Test project; it does not exist yet.

Purpose: Deliver automated, repeatable proof of FUL-01/FUL-02 and INF-02 compliance for Fulfillment, independent of Orders' saga or Notifications running.
Output: New `ECommerce.Fulfillment.Tests` project wired into `Fulfillment.sln` (already listed in the CI matrix — no workflow change needed), two test suites.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-fulfillment-notifications/05-CONTEXT.md
@.planning/phases/05-fulfillment-notifications/05-RESEARCH.md
@.planning/phases/05-fulfillment-notifications/05-PATTERNS.md

<interfaces>
Two-class test pattern to replicate (from `src/services/notifications/ECommerce.Notifications.Tests/Integration/CatalogSeededConsumerSteps.cs` and `CatalogSeededInboxDeduplicationSteps.cs`, both full files already read during planning — see `<read_first>` below). Key detail: pin the transport-level `MessageId` explicitly on `Bus.Publish` calls (`ctx.MessageId = messageId`) for dedup assertions to be meaningful — MassTransit generates a fresh transport `MessageId` per `Publish` call by default.

`ECommerce.Notifications.Tests.csproj` package set to copy (already-vetted, already-pinned): `xunit.v3` 3.2.2, `FluentAssertions` 8.10.0, `MassTransit.TestFramework` 8.3.6, `Microsoft.EntityFrameworkCore`/`.InMemory`/`.Relational` 10.0.9, `Testcontainers.PostgreSql` 4.12.0.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: New Fulfillment.Tests project + OrderPaidConsumer filter/scheduling test</name>
  <files>
    src/services/fulfillment/Fulfillment.sln,
    src/services/fulfillment/ECommerce.Fulfillment.Tests/ECommerce.Fulfillment.Tests.csproj,
    src/services/fulfillment/ECommerce.Fulfillment.Tests/Integration/OrderPaidConsumerSteps.cs,
    src/services/fulfillment/ECommerce.Fulfillment.Tests/Integration/OrderPaidConsumerTests.cs
  </files>
  <read_first>
    src/services/notifications/ECommerce.Notifications.Tests/ECommerce.Notifications.Tests.csproj,
    src/services/notifications/ECommerce.Notifications.Tests/Integration/CatalogSeededConsumerSteps.cs,
    src/services/notifications/ECommerce.Notifications.Tests/Integration/CatalogSeededConsumerTests.cs,
    src/services/fulfillment/Fulfillment.sln,
    src/services/notifications/Notifications.sln
  </read_first>
  <behavior>
    - `OrderStatusChanged{NewStatus: "Paid", OrderId, UserId}` published -> `OrderPaidConsumer` consumes it and the harness's `Scheduled` collection contains exactly one `OrderShipped` whose `CheckoutId == OrderId` and `UserId == UserId`.
    - `OrderStatusChanged{NewStatus: "Cancelled"}` published -> `OrderPaidConsumer` consumes it (still acked) but the harness's `Scheduled` collection contains zero `OrderShipped` messages.
  </behavior>
  <action>
    Create `ECommerce.Fulfillment.Tests.csproj` by copying `ECommerce.Notifications.Tests.csproj` verbatim (same package versions: `xunit.v3` 3.2.2, `FluentAssertions` 8.10.0, `MassTransit.TestFramework` 8.3.6, `Microsoft.EntityFrameworkCore`/`.InMemory`/`.Relational` 10.0.9, `Testcontainers.PostgreSql` 4.12.0), swapping the `ProjectReference` from `ECommerce.Notifications.API` to `../ECommerce.Fulfillment.API/ECommerce.Fulfillment.API.csproj` (keep the `Tests.Common` reference).

    Add the new project to `Fulfillment.sln`: `dotnet sln src/services/fulfillment/Fulfillment.sln add src/services/fulfillment/ECommerce.Fulfillment.Tests/ECommerce.Fulfillment.Tests.csproj` (the CI matrix in `.github/workflows/ci.yml` already lists `src/services/fulfillment/Fulfillment.sln` — no workflow file change needed).

    Create `OrderPaidConsumerSteps.cs` mirroring `CatalogSeededConsumerSteps.cs`'s in-memory harness shape (`ServiceCollection`, `AddDbContext&lt;FulfillmentDbContext&gt;(o => o.UseInMemoryDatabase(...))`, `AddMassTransitTestHarness(x => { x.AddConsumer&lt;OrderPaidConsumer&gt;(); x.UsingInMemory((context, cfg) => { cfg.UseInMemoryScheduler(); cfg.ConfigureEndpoints(context); }); })` — note the added `UseInMemoryScheduler()` call, required because `OrderPaidConsumer` calls `SchedulePublish`, unlike `CatalogSeededConsumer`, which has no scheduler dependency). Add step methods: `Given_HarnessWithInMemoryScheduler()`; `When_OrderStatusChangedPublished(Guid orderId, string userId, string newStatus)` constructing `new OrderStatusChanged(MessageId: Guid.NewGuid(), CorrelationId: orderId, CausationId: Guid.Empty, OccurredAt: DateTimeOffset.UtcNow, OrderId: orderId, UserId: userId, PreviousStatus: "Pending", NewStatus: newStatus, ChangedAt: DateTimeOffset.UtcNow)` and publishing via `_harness!.Bus.Publish(...)`, then `await _harness!.InactivityTask;`; `Then_OrderShippedScheduledWithCheckoutIdAndUserId(Guid expectedCheckoutId, string expectedUserId)` asserting `_harness!.Scheduled.Select&lt;OrderShipped&gt;().Any(s => s.Context.Message.CheckoutId == expectedCheckoutId && s.Context.Message.UserId == expectedUserId)` is true; `Then_NoOrderShippedScheduled()` asserting `_harness!.Scheduled.Select&lt;OrderShipped&gt;().Any()` is false. Configure `FulfillmentOptions` with a short `ProcessingSeconds` (e.g. 0.1) via `services.Configure&lt;FulfillmentOptions&gt;(o => o.ProcessingSeconds = 0.1);` so the scheduled-message assertion does not depend on a long real-time wait — `Scheduled` collection membership does not require the delay to actually elapse.

    Create `OrderPaidConsumerTests.cs` (`IAsyncDisposable`-delegating `[Fact]`s, mirrors `CatalogSeededConsumerTests.cs`'s shape) with two tests: (1) `OrderPaidConsumer_WhenNewStatusIsPaid_SchedulesOrderShippedWithMatchingCheckoutIdAndUserId`; (2) `OrderPaidConsumer_WhenNewStatusIsNotPaid_DoesNotScheduleOrderShipped`.
  </action>
  <acceptance_criteria>
    - `dotnet test src/services/fulfillment/Fulfillment.sln --filter "FullyQualifiedName~OrderPaidConsumerTests"` passes with at least 2 tests and 0 failures.
    - `grep -c "ECommerce.Fulfillment.Tests" src/services/fulfillment/Fulfillment.sln` returns at least 1.
    - `grep -c "Scheduled.Select<OrderShipped>" src/services/fulfillment/ECommerce.Fulfillment.Tests/Integration/OrderPaidConsumerSteps.cs` returns at least 2.
  </acceptance_criteria>
  <verify>
    <automated>dotnet test src/services/fulfillment/Fulfillment.sln --filter "FullyQualifiedName~OrderPaidConsumerTests" --logger "console;verbosity=normal"</automated>
  </verify>
  <done>ECommerce.Fulfillment.Tests exists, is part of Fulfillment.sln, and proves OrderPaidConsumer's filter + scheduling behavior against a real MassTransit in-memory test harness.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Postgres-backed forced-redelivery inbox dedup test</name>
  <files>
    src/services/fulfillment/ECommerce.Fulfillment.Tests/Integration/OrderPaidInboxDeduplicationSteps.cs,
    src/services/fulfillment/ECommerce.Fulfillment.Tests/Integration/OrderPaidInboxDeduplicationTests.cs
  </files>
  <read_first>
    src/services/notifications/ECommerce.Notifications.Tests/Integration/CatalogSeededInboxDeduplicationSteps.cs,
    src/services/notifications/ECommerce.Notifications.Tests/Integration/CatalogSeededInboxDeduplicationTests.cs,
    src/building-blocks/Tests.Common/PostgresFixture.cs
  </read_first>
  <behavior>
    - The SAME `OrderStatusChanged{NewStatus: "Paid"}` message (same transport `MessageId`, pinned explicitly) published TWICE against a real Postgres-backed EF Core outbox/inbox -> exactly ONE `InboxState` row exists afterward — `OrderPaidConsumer`'s body does not re-execute (and therefore does not schedule a duplicate `OrderShipped`) on the second delivery.
  </behavior>
  <action>
    Create `OrderPaidInboxDeduplicationSteps.cs` mirroring `CatalogSeededInboxDeduplicationSteps.cs`'s EXACT shape: `PostgresFixture`, `AddDbContext&lt;FulfillmentDbContext&gt;(o => o.UseNpgsql(connectionString))`, `AddMassTransitTestHarness(x => { x.AddConsumer&lt;OrderPaidConsumer&gt;(); x.AddEntityFrameworkOutbox&lt;FulfillmentDbContext&gt;(o => { o.UsePostgres(); o.UseBusOutbox(); }); x.AddConfigureEndpointsCallback((context, name, cfg) => cfg.UseEntityFrameworkOutbox&lt;FulfillmentDbContext&gt;(context)); x.UsingInMemory((context, cfg) => { cfg.UseInMemoryScheduler(); cfg.ConfigureEndpoints(context); }); })` (note: `UseBusOutbox()` IS needed here, unlike the Notifications analog — `OrderPaidConsumer` publishes/schedules `OrderShipped`, so the bus outbox drainer applies; `CatalogSeededConsumer` is consumer-only and omits it). Migrate the DbContext before starting the harness (`await db.Database.MigrateAsync();`), same as the analog. Step methods: `Given_HarnessWithPostgresInboxAndOutbox()`; `When_SameOrderStatusChangedPublishedTwice(Guid messageId, Guid orderId, string userId)` constructing the message once and publishing it TWICE via `_harness!.Bus.Publish(message, ctx => ctx.MessageId = messageId);` (pinned transport MessageId, mirrors the analog's Gap-4-fix comment), each followed by `await _harness!.InactivityTask;`; `Then_InboxStateHasExactlyOneRow()` asserting `db.Set&lt;InboxState&gt;().CountAsync() == 1`.

    Create `OrderPaidInboxDeduplicationTests.cs` (`IAsyncLifetime`-delegating, mirrors `CatalogSeededInboxDeduplicationTests.cs`'s shape exactly) with one `[Fact]`: `OrderPaidConsumer_WhenSameMessageIdDeliveredTwice_InboxStateContainsExactlyOneRow`.

    This test, by using a REAL Postgres-backed transactional outbox (not the InMemory harness from Task 1), also exercises whether `ConsumeContext.SchedulePublish&lt;T&gt;` correctly participates in the EF Core outbox transaction end-to-end against a real database — the scenario RESEARCH.md's Assumption A1 flags as MEDIUM-confidence. No additional test code is needed for this — a passing run of this test is itself the strongest available evidence in this repo that the assumption holds.
  </action>
  <acceptance_criteria>
    - `dotnet test src/services/fulfillment/Fulfillment.sln --filter "FullyQualifiedName~OrderPaidInboxDeduplicationTests"` passes with 1 test and 0 failures.
    - `grep -c "ctx.MessageId = messageId" src/services/fulfillment/ECommerce.Fulfillment.Tests/Integration/OrderPaidInboxDeduplicationSteps.cs` returns 2 (both publishes pinned to the same transport MessageId).
    - `grep -c "InboxState" src/services/fulfillment/ECommerce.Fulfillment.Tests/Integration/OrderPaidInboxDeduplicationSteps.cs` returns at least 1.
  </acceptance_criteria>
  <verify>
    <automated>dotnet test src/services/fulfillment/Fulfillment.sln --filter "FullyQualifiedName~OrderPaidInboxDeduplicationTests" --logger "console;verbosity=normal"</automated>
  </verify>
  <done>A real Postgres-backed forced-redelivery test proves Fulfillment's EF Core inbox deduplicates OrderStatusChanged by transport MessageId, satisfying INF-02 for Fulfillment.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Test-only — no new production trust boundary | This plan adds test infrastructure only; no production code changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-05-11 | N/A (test-only plan) | — | accept | This plan modifies only test projects; no new production attack surface is introduced |
</threat_model>

<verification>
`dotnet test src/services/fulfillment/Fulfillment.sln --filter "FullyQualifiedName~OrderPaidConsumerTests"` and `dotnet test src/services/fulfillment/Fulfillment.sln --filter "FullyQualifiedName~OrderPaidInboxDeduplicationTests"` both pass with 0 failures.
</verification>

<success_criteria>
Fulfillment's OrderPaidConsumer is independently and automatically proven correct — filter behavior, scheduling correctness, and inbox-based idempotency — without requiring Orders' saga or Notifications to be running.
</success_criteria>

<output>
Create `.planning/phases/05-fulfillment-notifications/05-06-SUMMARY.md` when done
</output>
