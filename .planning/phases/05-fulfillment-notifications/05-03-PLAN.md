---
phase: 05-fulfillment-notifications
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/services/orders/ECommerce.Orders.API/Features/Orders/OrderStateMachine.cs
  - src/services/payments/ECommerce.Payments.API/Features/Payments/AuthorisePaymentConsumer.cs
  - src/services/orders/ECommerce.Orders.Tests/Unit/OrderStateMachineSteps.cs
  - src/services/orders/ECommerce.Orders.Tests/Unit/OrderStateMachineTests.cs
  - src/services/payments/ECommerce.Payments.Tests/Integration/AuthorisePaymentConsumerSteps.cs
  - src/services/payments/ECommerce.Payments.Tests/Integration/AuthorisePaymentConsumerTests.cs
autonomous: true
requirements: [FUL-02, NOT-02]

must_haves:
  truths:
    - "When Fulfillment (05-04) publishes OrderShipped, the Orders saga transitions Paid -> Fulfilled and the order detail view reflects Fulfilled without manual intervention (SC2)."
    - "A late/redelivered OrderShipped arriving after the saga already left Paid is absorbed, not faulted — consistent with every other event this saga defends against (CR-01/CR-02/WR-02 discipline)."
    - "Every OrderStatusChanged and AuthorisePayment message the Orders saga publishes, and every PaymentFailed message Payments publishes, carries the correct UserId (D-03) — the prerequisite Notifications (05-05) needs to scope inbox entries without a synchronous lookup."
  artifacts:
    - path: "src/services/orders/ECommerce.Orders.API/Features/Orders/OrderStateMachine.cs"
      provides: "Event<OrderShipped> binding: Paid -> Fulfilled; UserId populated on every OrderStatusChanged/AuthorisePayment publish site"
    - path: "src/services/payments/ECommerce.Payments.API/Features/Payments/AuthorisePaymentConsumer.cs"
      provides: "UserId echoed from the (now-enriched) AuthorisePayment command into every PaymentFailed publish site"
  key_links:
    - from: "OrderStateMachine.cs During(Paid, ...)"
      to: "OrderShippedEvent"
      via: "When(OrderShippedEvent).TransitionTo(Fulfilled)"
      pattern: "When\\(OrderShippedEvent\\)"
    - from: "AuthorisePaymentConsumer.cs"
      to: "PaymentFailed"
      via: "msg.UserId echoed directly, no lookup"
      pattern: "msg\\.UserId"
---

<objective>
Wire the Orders saga to react to Fulfillment's new `OrderShipped` event (closing the `Paid -> Fulfilled` loop, SC1/SC2), and propagate `UserId` (D-03) through every existing publish site in the Orders saga and the Payments consumer that 05-01 touched. This is mechanical UserId propagation plus one new saga transition — no new persisted saga state (ADR-0009 unchanged: `Pending/Paid/Fulfilled/Cancelled/Failed`).

Purpose: Complete the saga-side half of FUL-02 (the saga must actually consume `OrderShipped` for Fulfillment's publish to have any effect) and unblock Notifications (05-05), which cannot scope inbox entries without `UserId` flowing correctly through `OrderStatusChanged` and `PaymentFailed`.
Output: `OrderStateMachine.cs` with a new `Event<OrderShipped>` binding and `UserId` on every existing publish; `AuthorisePaymentConsumer.cs` echoing `UserId` into `PaymentFailed`; updated Orders/Payments unit and integration tests proving both.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-fulfillment-notifications/05-CONTEXT.md
@.planning/phases/05-fulfillment-notifications/05-RESEARCH.md
@.planning/phases/05-fulfillment-notifications/05-PATTERNS.md
@docs/adr/0005-saga-orchestration.md
@docs/adr/0009-checkout-saga-state-reconciliation.md

<interfaces>
Contracts this plan consumes (from 05-01, already landed):

```csharp
// ECommerce.Fulfillment.Events.V1
public record OrderShipped(Guid MessageId, Guid CorrelationId, Guid CausationId, DateTimeOffset OccurredAt, Guid CheckoutId, string UserId, DateTimeOffset ShippedAt) : IMessageEnvelope;

// ECommerce.Orders.Events.V1 (UserId inserted after OrderId)
public record OrderStatusChanged(Guid MessageId, Guid CorrelationId, Guid CausationId, DateTimeOffset OccurredAt, Guid OrderId, string UserId, string PreviousStatus, string NewStatus, DateTimeOffset ChangedAt, string? FailureReason = null) : IMessageEnvelope;

// ECommerce.Payments.Commands.V1 (UserId inserted after CheckoutId)
public record AuthorisePayment(Guid MessageId, Guid CorrelationId, Guid CausationId, DateTimeOffset OccurredAt, Guid CheckoutId, string UserId, decimal Amount, bool SimulatePaymentFailure = false) : IMessageEnvelope;

// ECommerce.Payments.Events.V1 (UserId inserted after CheckoutId)
public record PaymentFailed(Guid MessageId, Guid CorrelationId, Guid CausationId, DateTimeOffset OccurredAt, Guid CheckoutId, string UserId, decimal Amount, string Reason, DateTimeOffset FailedAt) : IMessageEnvelope;
```

`Order` saga instance (unchanged) already exposes `ctx.Saga.UserId` (set from `OrderCreated.UserId` in `Initially()`'s existing `.Then()` — see read_first).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire OrderShipped into the saga; propagate UserId on every existing publish</name>
  <files>src/services/orders/ECommerce.Orders.API/Features/Orders/OrderStateMachine.cs</files>
  <read_first>
    src/services/orders/ECommerce.Orders.API/Features/Orders/OrderStateMachine.cs,
    src/building-blocks/Contracts/Fulfillment/Events/V1/FulfillmentFailed.cs,
    src/building-blocks/Contracts/Fulfillment/Events/V1/OrderShipped.cs
  </read_first>
  <behavior>
    - Saga in `Paid`, receives `OrderShipped` correlated by `CheckoutId` -> transitions to `Fulfilled`, publishes `OrderStatusChanged{PreviousStatus="Paid", NewStatus="Fulfilled", UserId=ctx.Saga.UserId}`.
    - Saga already in `Pending`, `Cancelled`, `Fulfilled`, or `Failed` receives a late/redelivered `OrderShipped` -> absorbed via `Ignore(OrderShippedEvent)`, no fault, no transition (mirrors the CR-01/CR-02/WR-02 discipline already applied to every other event type in this file).
    - Every existing `new OrderStatusChanged(...)` publish site now includes `UserId: ctx.Saga.UserId`.
    - The existing `Initially()` `new AuthorisePayment(...)` publish now includes `UserId: ctx.Saga.UserId` (already set by the preceding `.Then()` in the same activity chain, before the `.Publish()`).
  </behavior>
  <action>
    Add a new `public Event&lt;OrderShipped&gt; OrderShippedEvent { get; private set; } = null!;` property alongside the existing `Event&lt;FulfillmentFailed&gt; FulfillmentFailedEvent` declaration (~line 32), and register it with the same correlation key convention as `FulfillmentFailedEvent`: `Event(() => OrderShippedEvent, x => x.CorrelateById(m => m.Message.CheckoutId));` (add immediately after the existing `Event(() => FulfillmentFailedEvent, ...)` line, ~line 44).

    `using ECommerce.Fulfillment.Events.V1;` is already present at the top of the file (needed today for `FulfillmentFailed`) — no new using statement required.

    Inside `During(Paid, ...)` (currently spans lines 177-231), add a new `When(OrderShippedEvent)` binding alongside the existing `FulfillmentFailedEvent` binding (before the trailing `Ignore(...)` catch-alls): `.Then(ctx => ctx.Saga.UpdatedAt = ctx.Message.ShippedAt).Publish(ctx => new OrderStatusChanged(MessageId: Guid.NewGuid(), CorrelationId: ctx.Saga.CorrelationId, CausationId: ctx.Message.MessageId, OccurredAt: DateTimeOffset.UtcNow, OrderId: ctx.Saga.CorrelationId, UserId: ctx.Saga.UserId, PreviousStatus: "Paid", NewStatus: "Fulfilled", ChangedAt: ctx.Message.ShippedAt, FailureReason: null)).TransitionTo(Fulfilled)`.

    Add `Ignore(OrderShippedEvent),` to the trailing catch-all list of the FOUR other `During()` blocks: `During(Pending, ...)` (~line 174, alongside the existing `Ignore(FulfillmentFailedEvent)`), `During(Cancelled, ...)` (~line 253), `During(Fulfilled, ...)` (~line 270), `During(Failed, ...)` (~line 280). Do NOT add it to `During(Paid, ...)` — that block has the real binding, not a catch-all.

    Add `UserId: ctx.Saga.UserId` as a named argument to every EXISTING `new OrderStatusChanged(...)` construction in this file (4 sites — `Initially()` itself does not publish `OrderStatusChanged`, only `AuthorisePayment`; the 4 `OrderStatusChanged` sites are: `During(Pending, ...)`'s `When(PaymentAuthorisedEvent)` binding [~line 116], `When(PaymentFailedEvent)` binding [~line 130], `When(CheckoutTimeout.Received)` binding [~line 145], and `During(Paid, ...)`'s `When(FulfillmentFailedEvent)` binding [~line 202]). Insert `UserId: ctx.Saga.UserId,` immediately after the `OrderId:` named argument in each.

    Add `UserId: ctx.Saga.UserId` to the existing `new AuthorisePayment(...)` construction inside `Initially()` (~line 77), immediately after the `CheckoutId:` named argument — `ctx.Saga.UserId` is already populated by the preceding `.Then()` in the same `When(OrderCreatedEvent)` activity chain (line ~63), so this is safe to reference.
  </action>
  <acceptance_criteria>
    - `dotnet build src/services/orders/Orders.sln` succeeds with zero errors.
    - `grep -c "Event<OrderShipped> OrderShippedEvent" src/services/orders/ECommerce.Orders.API/Features/Orders/OrderStateMachine.cs` returns 1.
    - `grep -c "When(OrderShippedEvent)" src/services/orders/ECommerce.Orders.API/Features/Orders/OrderStateMachine.cs` returns 1.
    - `grep -c "Ignore(OrderShippedEvent)" src/services/orders/ECommerce.Orders.API/Features/Orders/OrderStateMachine.cs` returns 4 (Pending, Cancelled, Fulfilled, Failed — not Paid).
    - `grep -c "UserId: ctx.Saga.UserId" src/services/orders/ECommerce.Orders.API/Features/Orders/OrderStateMachine.cs` returns at least 6 (5 existing OrderStatusChanged/AuthorisePayment sites + 1 new OrderShipped-triggered site).
  </acceptance_criteria>
  <verify>
    <automated>dotnet build src/services/orders/Orders.sln</automated>
  </verify>
  <done>OrderStateMachine.cs compiles, has a real Paid->Fulfilled binding on OrderShipped, absorbs late OrderShipped in every other state, and every OrderStatusChanged/AuthorisePayment publish now carries UserId.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Echo UserId into PaymentFailed in AuthorisePaymentConsumer</name>
  <files>src/services/payments/ECommerce.Payments.API/Features/Payments/AuthorisePaymentConsumer.cs</files>
  <read_first>src/services/payments/ECommerce.Payments.API/Features/Payments/AuthorisePaymentConsumer.cs</read_first>
  <behavior>
    - Redelivered `AuthorisePayment` for a `CheckoutId` whose stored `Outcome` is `"Failed"` (replay branch) -> republished `PaymentFailed` carries `msg.UserId` (from the redelivered command itself, not a stored column — `ProcessedPayment` gets no new column).
    - New `AuthorisePayment` that fails the PAY-02 deterministic rule (new-row branch) -> the freshly-published `PaymentFailed` carries `msg.UserId`.
    - The two `PaymentAuthorised` publish sites (replay + new-row) are UNCHANGED — `PaymentAuthorised` did not gain a `UserId` field in 05-01 (Notifications never consumes it).
  </behavior>
  <action>
    In the replay branch's `case "Failed":` (currently `await publish.Publish(new PaymentFailed(Guid.NewGuid(), msg.CheckoutId, msg.MessageId, now, msg.CheckoutId, existing.Amount, existing.FailureReason!, existing.ProcessedAt), context.CancellationToken);`), insert `msg.UserId` as the 6th positional argument, immediately after the second `msg.CheckoutId` (the `CheckoutId:` field) and before `existing.Amount` (the `Amount:` field) — matching `PaymentFailed`'s new field order from 05-01 (`MessageId, CorrelationId, CausationId, OccurredAt, CheckoutId, UserId, Amount, Reason, FailedAt`).

    In the new-row branch (currently `await publish.Publish(new PaymentFailed(Guid.NewGuid(), msg.CheckoutId, msg.MessageId, now, msg.CheckoutId, msg.Amount, DeclinedReason, now), context.CancellationToken);`), insert `msg.UserId` as the 6th positional argument in the same position (after the second `msg.CheckoutId`, before `msg.Amount`).

    Do not modify either `PaymentAuthorised` publish site (replay branch's `case "Authorised":`, or the new-row `else` branch) — `PaymentAuthorised`'s shape is unchanged.
  </action>
  <acceptance_criteria>
    - `dotnet build src/services/payments/Payments.sln` succeeds with zero errors.
    - `grep -c "msg.UserId" src/services/payments/ECommerce.Payments.API/Features/Payments/AuthorisePaymentConsumer.cs` returns 2 (both PaymentFailed sites, replay + new-row).
    - `grep -c "new PaymentAuthorised(" src/services/payments/ECommerce.Payments.API/Features/Payments/AuthorisePaymentConsumer.cs` returns 2, and neither of those two lines/blocks contains `msg.UserId` (confirm manually — PaymentAuthorised stays unchanged).
  </acceptance_criteria>
  <verify>
    <automated>dotnet build src/services/payments/Payments.sln</automated>
  </verify>
  <done>Both PaymentFailed publish sites in AuthorisePaymentConsumer.cs echo msg.UserId; both PaymentAuthorised sites are untouched; Payments.sln builds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Update Orders/Payments tests for the UserId contract change + new OrderShipped saga tests</name>
  <files>
    src/services/orders/ECommerce.Orders.Tests/Unit/OrderStateMachineSteps.cs,
    src/services/orders/ECommerce.Orders.Tests/Unit/OrderStateMachineTests.cs,
    src/services/payments/ECommerce.Payments.Tests/Integration/AuthorisePaymentConsumerSteps.cs,
    src/services/payments/ECommerce.Payments.Tests/Integration/AuthorisePaymentConsumerTests.cs
  </files>
  <read_first>
    src/services/orders/ECommerce.Orders.Tests/Unit/OrderStateMachineSteps.cs,
    src/services/orders/ECommerce.Orders.Tests/Unit/OrderStateMachineTests.cs,
    src/services/payments/ECommerce.Payments.Tests/Integration/AuthorisePaymentConsumerSteps.cs,
    src/services/payments/ECommerce.Payments.Tests/Integration/AuthorisePaymentConsumerTests.cs
  </read_first>
  <behavior>
    - `OrderShipped` published while the saga is `Paid` -> saga transitions to `Fulfilled`.
    - `OrderShipped` published while the saga is already `Cancelled` -> saga stays `Cancelled`, no fault published (mirrors the existing `PaymentAuthorised_WhenAlreadyCancelled_IsAbsorbedWithoutFault` test shape).
    - `AuthorisePayment` that deterministically fails (amount ending in `.99`) -> the published `PaymentFailed`'s `UserId` matches the `UserId` on the originating `AuthorisePayment` command.
  </behavior>
  <action>
    In `OrderStateMachineSteps.cs`: add an optional `string userId = "user-1"` parameter to `When_OrderStatusChangedPublished` and `Given_PaymentFailedPublished`, passing `UserId: userId` into their respective `OrderStatusChanged`/`PaymentFailed` constructions (keeps every existing call site in `OrderStateMachineTests.cs` compiling unchanged, matching `Given_OrderCreatedPublished`'s existing `UserId: "user-1"` convention). Add a new step method `When_OrderShippedPublished(Guid orderId, string userId = "user-1")` that publishes `new OrderShipped(MessageId: Guid.NewGuid(), CorrelationId: orderId, CausationId: Guid.Empty, OccurredAt: DateTimeOffset.UtcNow, CheckoutId: orderId, UserId: userId, ShippedAt: DateTimeOffset.UtcNow)` via `_harness!.Bus.Publish(...)` and awaits `_harness!.Consumed.Any&lt;OrderShipped&gt;(x => x.Context.Message.CheckoutId == orderId)`, mirroring `When_FulfillmentFailedPublished`'s exact shape.

    In `OrderStateMachineTests.cs`: add two new `[Fact]` tests mirroring the existing `FulfillmentFailed_WhenPaid_TransitionsToCancelledAndPublishesRefundPayment` / `PaymentAuthorised_WhenAlreadyCancelled_IsAbsorbedWithoutFault` shapes exactly: (1) `OrderShipped_WhenPaid_TransitionsToFulfilled` — `Given_OrderCreatedPublished` -> `Given_PaymentAuthorisedPublished` -> `Then_SagaExistsInState(Machine.Paid)` -> `When_OrderShippedPublished` -> `Then_SagaExistsInState(Machine.Fulfilled)`; (2) `OrderShipped_WhenAlreadyCancelled_IsAbsorbedWithoutFault` — `Given_OrderCreatedPublished` -> `Given_PaymentFailedPublished` -> `Then_SagaExistsInState(Machine.Cancelled)` -> `When_OrderShippedPublished` -> `Then_SagaExistsInState(Machine.Cancelled)` -> `Then_NoFaultPublished&lt;ECommerce.Fulfillment.Events.V1.OrderShipped&gt;()`.

    In `AuthorisePaymentConsumerSteps.cs`: add an optional `string userId = "user-1"` parameter to `When_AuthorisePaymentPublished` and `When_AuthorisePaymentPublishedTwice`, passing `UserId: userId` into the `AuthorisePayment` construction (keeps every existing call site in `AuthorisePaymentConsumerTests.cs` compiling unchanged). Add a new step method `Then_PublishedPaymentFailedHasUserId(string expectedUserId)` asserting `(await _harness!.Published.Any&lt;PaymentFailed&gt;(ctx => ctx.Context.Message.UserId == expectedUserId)).Should().BeTrue(...)`.

    In `AuthorisePaymentConsumerTests.cs`: add one new `[Fact]` — `AuthorisePayment_WhenAmountEndsIn99_PublishedPaymentFailedCarriesUserId` — publishes with `When_AuthorisePaymentPublished(checkoutId, amount: 19.99m, userId: "user-42")`, then calls `Then_PublishedPaymentFailedHasUserId("user-42")`.
  </action>
  <acceptance_criteria>
    - `dotnet test src/services/orders/Orders.sln --filter "FullyQualifiedName~OrderStateMachineTests"` passes with at least 12 tests (10 existing + 2 new) and 0 failures.
    - `dotnet test src/services/payments/Payments.sln --filter "FullyQualifiedName~AuthorisePaymentConsumerTests"` passes with at least 7 tests (6 existing + 1 new) and 0 failures.
    - `grep -c "OrderShipped_WhenPaid_TransitionsToFulfilled" src/services/orders/ECommerce.Orders.Tests/Unit/OrderStateMachineTests.cs` returns 1.
    - `grep -c "PublishedPaymentFailedHasUserId" src/services/payments/ECommerce.Payments.Tests/Integration/AuthorisePaymentConsumerSteps.cs` returns at least 1.
  </acceptance_criteria>
  <verify>
    <automated>dotnet test src/services/orders/Orders.sln --filter "FullyQualifiedName~OrderStateMachineTests" --logger "console;verbosity=normal" && dotnet test src/services/payments/Payments.sln --filter "FullyQualifiedName~AuthorisePaymentConsumerTests" --logger "console;verbosity=normal"</automated>
  </verify>
  <done>OrderStateMachineTests.cs proves OrderShipped drives Paid->Fulfilled and is absorbed when late; AuthorisePaymentConsumerTests.cs proves UserId flows from AuthorisePayment into PaymentFailed; both test suites pass against real MassTransit test harnesses (InMemory saga harness for Orders, Postgres-backed outbox harness for Payments).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Orders saga <-> Azure Service Bus | No new inbound trust boundary — `OrderShipped` arrives from Fulfillment, an internal first-party service, over the existing bus transport already trusted for `FulfillmentFailed`/`PaymentAuthorised`/`PaymentFailed` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-05-04 | Denial of Service | Late/redelivered `OrderShipped` after the saga left `Paid` | mitigate | `Ignore(OrderShippedEvent)` catch-all added to all four other `During()` blocks — absorbed, not faulted, consistent with the existing CR-01/CR-02/WR-02 discipline for every other event type in this saga |
| T-05-05 | Repudiation | `PaymentFailed.UserId` now sourced from the redelivered `AuthorisePayment` command rather than a stored column | accept | `AuthorisePayment.UserId` is itself derived server-side from the JWT claim at saga-creation time (`OrderStateMachine.Initially()`), never client-supplied at the Payments layer — echoing it introduces no new spoofing surface |
</threat_model>

<verification>
`dotnet build src/services/orders/Orders.sln` and `dotnet build src/services/payments/Payments.sln` both succeed. `dotnet test src/services/orders/Orders.sln --filter "FullyQualifiedName~OrderStateMachineTests"` and `dotnet test src/services/payments/Payments.sln --filter "FullyQualifiedName~AuthorisePaymentConsumerTests"` both pass with 0 failures, including the two new OrderShipped saga tests and the new UserId-propagation test.
</verification>

<success_criteria>
The Orders saga correctly transitions Paid -> Fulfilled on OrderShipped and absorbs late redeliveries; every OrderStatusChanged/AuthorisePayment/PaymentFailed publish site in Orders and Payments carries the correct UserId — unblocking Notifications (05-05) to scope inbox entries without any synchronous lookup.
</success_criteria>

<output>
Create `.planning/phases/05-fulfillment-notifications/05-03-SUMMARY.md` when done
</output>
