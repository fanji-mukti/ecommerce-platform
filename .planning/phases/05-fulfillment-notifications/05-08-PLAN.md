---
phase: 05-fulfillment-notifications
plan: 08
type: execute
wave: 3
depends_on: ["05-05"]
files_modified:
  - src/frontend/ecommerce-app/src/app/shared/models/notification.model.ts
  - src/frontend/ecommerce-app/src/app/core/services/notifications.service.ts
  - src/frontend/ecommerce-app/src/app/features/notifications/notifications-page/notifications-page.component.ts
  - src/frontend/ecommerce-app/src/app/features/notifications/notifications-page/notifications-page.component.html
  - src/frontend/ecommerce-app/src/app/features/notifications/notifications-page/notifications-page.component.scss
  - src/frontend/ecommerce-app/src/app/app.routes.ts
  - src/frontend/ecommerce-app/src/app/app.html
autonomous: true
requirements: [NOT-01]

must_haves:
  truths:
    - "A logged-in user can navigate to /notifications via a nav bar link and see a list of their inbox entries, each linking to /orders/:id (D-05, D-06)."
    - "Empty, loading, and error states are all handled per UI-SPEC — never a blank or broken page."
  artifacts:
    - path: "src/frontend/ecommerce-app/src/app/features/notifications/notifications-page/notifications-page.component.ts"
      provides: "NOT-01's only UI surface — the sole way a user sees their notification inbox without calling the API directly"
  key_links:
    - from: "notifications-page.component.ts"
      to: "GET /api/notifications"
      via: "NotificationsService.getNotifications()"
      pattern: "getNotifications"
    - from: "app.html toolbar"
      to: "/notifications"
      via: "routerLink"
      pattern: "routerLink=\"/notifications\""
---

<objective>
Build the Angular `/notifications` page — a minimal `mat-list` inbox (D-05) with a nav bar link (D-06) — the ONLY UI surface for NOT-01. Depends on 05-05's `GET /notifications` endpoint existing and its response shape (raw `NotificationEntry` JSON, camelCase per ASP.NET Core's default serialization) being stable; does not require 05-07's tests to have landed first.

Purpose: Make NOT-01 demoable end-to-end through the UI, not API/Swagger-only — matching the precedent FE-03 set for saga visibility in Phase 4 (D-05).
Output: `notification.model.ts`, `notifications.service.ts`, `NotificationsPageComponent`, a new `/notifications` route, and a toolbar nav link.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-fulfillment-notifications/05-CONTEXT.md
@.planning/phases/05-fulfillment-notifications/05-UI-SPEC.md
@.planning/phases/05-fulfillment-notifications/05-PATTERNS.md

<interfaces>
Backend response shape this plan's service consumes (from plan 05-05's `GET /notifications` — raw entity serialization, camelCase per ASP.NET Core Minimal API defaults):

```typescript
interface NotificationEntry {
  id: string;
  orderId: string;
  message: string;
  eventType: 'OrderPaid' | 'OrderShipped' | 'PaymentFailed'; // LOCKED values, set by plan 05-05's consumers — do not invent others
  occurredAt: string;
}
```

Thin HTTP-wrapper pattern to copy exactly (from `src/frontend/ecommerce-app/src/app/core/services/orders.service.ts`, full file):

```typescript
@Injectable({ providedIn: 'root' })
export class OrdersService {
  private http = inject(HttpClient);
  getOrder(id: string): Observable&lt;OrderDetail&gt; {
    return this.http.get&lt;OrderDetail&gt;(`/api/orders/${id}`);
  }
}
```

List-fetch-render component structure to copy for signals/lifecycle (from `catalog-list.component.ts` — `isLoading`/`hasError` signal pair, `ngOnInit` fetch, `retry()` method). NOTE: no `mat-list` precedent exists anywhere in this repo (`grep -rl "mat-list"` returns zero matches) — the TEMPLATE markup for this page is new Material-doc-sourced markup (`MatListModule`, `mat-list-item`), not copied from an existing template; only the component TS structure (signals, lifecycle, error handling) is copied from `catalog-list.component.ts`.

Empty/error state SCSS shape to reuse verbatim (from `cart-page.component.scss`/`checkout-page.component.scss`, and `order-detail.component.scss`'s `var(--space-*)` convention — this NEW component uses the CSS-custom-property convention, matching the more recent `order-detail`/`checkout-page` components, not `cart-page`'s older literal-px values):

```scss
.empty-state, .error-state {
  text-align: center;
  padding: var(--space-2xl) 0;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Model and service</name>
  <files>
    src/frontend/ecommerce-app/src/app/shared/models/notification.model.ts,
    src/frontend/ecommerce-app/src/app/core/services/notifications.service.ts
  </files>
  <read_first>
    src/frontend/ecommerce-app/src/app/shared/models/order.model.ts,
    src/frontend/ecommerce-app/src/app/core/services/orders.service.ts
  </read_first>
  <action>
    Create `shared/models/notification.model.ts`: `export type NotificationEventType = 'OrderPaid' | 'OrderShipped' | 'PaymentFailed'; export interface NotificationEntry { id: string; orderId: string; message: string; eventType: NotificationEventType; occurredAt: string; }` — these three `eventType` values are LOCKED to exactly what plan 05-05's three consumers write (`"OrderPaid"`, `"OrderShipped"`, `"PaymentFailed"`); do not invent additional values.

    Create `core/services/notifications.service.ts` mirroring `orders.service.ts`'s exact `@Injectable({ providedIn: 'root' })` + `inject(HttpClient)` shape: `getNotifications(): Observable&lt;NotificationEntry[]&gt; { return this.http.get&lt;NotificationEntry[]&gt;('/api/notifications'); }`. No `userId` parameter anywhere on this service — the backend scopes by JWT server-side (per 05-05); passing a client-supplied user id would be a IDOR anti-pattern this project explicitly avoids.
  </action>
  <acceptance_criteria>
    - `grep -c "NotificationEntry" src/frontend/ecommerce-app/src/app/shared/models/notification.model.ts` returns at least 1.
    - `grep -c "get<NotificationEntry\[\]>('/api/notifications')" src/frontend/ecommerce-app/src/app/core/services/notifications.service.ts` returns 1.
    - `cd src/frontend/ecommerce-app && npx tsc --noEmit -p tsconfig.app.json` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>cd src/frontend/ecommerce-app && npx tsc --noEmit -p tsconfig.app.json</automated>
  </verify>
  <done>notification.model.ts and notifications.service.ts exist as a thin HttpClient wrapper matching orders.service.ts's convention, with the eventType union locked to the three values 05-05's consumers produce.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: NotificationsPageComponent, route, nav link</name>
  <files>
    src/frontend/ecommerce-app/src/app/features/notifications/notifications-page/notifications-page.component.ts,
    src/frontend/ecommerce-app/src/app/features/notifications/notifications-page/notifications-page.component.html,
    src/frontend/ecommerce-app/src/app/features/notifications/notifications-page/notifications-page.component.scss,
    src/frontend/ecommerce-app/src/app/app.routes.ts,
    src/frontend/ecommerce-app/src/app/app.html
  </files>
  <read_first>
    .planning/phases/05-fulfillment-notifications/05-UI-SPEC.md,
    src/frontend/ecommerce-app/src/app/features/catalog/catalog-list/catalog-list.component.ts,
    src/frontend/ecommerce-app/src/app/features/cart/cart-page/cart-page.component.html,
    src/frontend/ecommerce-app/src/app/app.routes.ts,
    src/frontend/ecommerce-app/src/app/app.html
  </read_first>
  <behavior>
    - On init: fetches `notificationsService.getNotifications()`; `isLoading` true until it resolves; `hasError` true on failure (mirrors `catalog-list.component.ts`'s exact loading/error signal pattern).
    - Zero entries after a successful load -> empty state, heading `"No notifications yet"`, body `"Updates about your orders will show up here."`, no action button (UI-SPEC: no single "next step" applies here).
    - One or more entries -> a `mat-list` of `mat-list-item` rows, each a full-row `routerLink="/orders/{{ n.orderId }}"` target (not a button — the whole row is the click target), showing an icon per `eventType` (`payment` for `OrderPaid`, `local_shipping` for `OrderShipped`, `error` for `PaymentFailed`, red-colored ONLY for the `error` icon — `payment`/`local_shipping` stay unstyled per UI-SPEC's color-discipline note), the `message` text, and a timestamp (Label role, 14px/400).
    - Error state (fetch failure) -> heading `"Failed to load notifications"`, body `"Something went wrong while loading your notifications. Please refresh the page or try again."`, `mat-raised-button color="primary"` labeled `"Retry"` calling a `retry()` method that re-fetches.
  </behavior>
  <action>
    Build `NotificationsPageComponent` as a standalone component: `private notificationsService = inject(NotificationsService);` signals `notifications = signal&lt;NotificationEntry[]&gt;([]); isLoading = signal(false); hasError = signal(false);`. `ngOnInit()` calls a private `loadNotifications()` method (mirrors `catalog-list.component.ts`'s `loadProducts()` shape): sets `isLoading(true)`/`hasError(false)`, subscribes to `getNotifications()`, on success sets `notifications` and `isLoading(false)`, on error sets `isLoading(false)`/`hasError(true)`. `retry()` calls `loadNotifications()` again (mirrors `catalog-list.component.ts`'s `retry()`). Add a helper `iconFor(eventType: NotificationEventType): string` returning `'payment' | 'local_shipping' | 'error'` per the locked mapping, and `isDestructive(eventType: NotificationEventType): boolean` returning `eventType === 'PaymentFailed'` (drives the red icon color, per UI-SPEC's "only PaymentFailed is colored" discipline). Import `MatListModule` (`@angular/material/list`), `MatIconModule`, `MatProgressBarModule`, `MatButtonModule`, `RouterLink` into the component's `imports` array.

    Template (`notifications-page.component.html`) per UI-SPEC: `<h1>Notifications</h1>`; `@if (isLoading())` -> `mat-progress-bar mode="indeterminate"`; `@if (hasError())` -> `.error-state` div reusing the exact heading/body/Retry-button shape from `cart-page.component.html`'s `.error-state` block, with this page's own copy; `@if (!hasError() && !isLoading())` -> `@if (notifications().length === 0)` -> `.empty-state` div (heading + body, no button); `@else` -> `<mat-list>` with `@for (n of notifications(); track n.id)` rendering `<a mat-list-item [routerLink]="['/orders', n.orderId]">` containing `<mat-icon matListItemIcon [class.destructive-icon]="isDestructive(n.eventType)">{{ iconFor(n.eventType) }}</mat-icon>`, `<span matListItemTitle>{{ n.message }}</span>`, `<span matListItemLine>{{ n.occurredAt | date }}</span>` (import `DatePipe` from `@angular/common`).

    In `notifications-page.component.scss`: `.notifications-page { padding: var(--space-xl); h1 { margin-bottom: var(--space-lg); } } .empty-state, .error-state { text-align: center; padding: var(--space-2xl) 0; h2 { margin-bottom: var(--space-md); } p { margin-bottom: var(--space-lg); } } .destructive-icon { color: #d32f2f; } @media (max-width: 600px) { .notifications-page { padding: var(--space-md); } }` (matches `order-detail.component.scss`'s established `var(--space-*)` + mobile-breakpoint convention).

    In `app.routes.ts`: import `NotificationsPageComponent` from `./features/notifications/notifications-page/notifications-page.component`; add `{ path: 'notifications', component: NotificationsPageComponent, title: 'Notifications — eCommerce' }` after the existing `orders/:id` route entry and before the `login` route, keeping it above the `{ path: '**', ... }` wildcard.

    In `app.html`: inside the existing `@if (isAuthenticated().isAuthenticated) { ... }` block, add `<a mat-button routerLink="/notifications" routerLinkActive="active-link">Notifications</a>` immediately after the existing `<a mat-button routerLink="/cart" ...>Cart</a>` link (D-06 — plain nav link, no unread-count badge).
  </action>
  <acceptance_criteria>
    - `grep -c "mat-list" src/frontend/ecommerce-app/src/app/features/notifications/notifications-page/notifications-page.component.html` returns at least 1.
    - `grep -c "No notifications yet" src/frontend/ecommerce-app/src/app/features/notifications/notifications-page/notifications-page.component.html` returns 1.
    - `grep -c "Failed to load notifications" src/frontend/ecommerce-app/src/app/features/notifications/notifications-page/notifications-page.component.html` returns 1.
    - `grep -c "routerLink=\"/notifications\"" src/frontend/ecommerce-app/src/app/app.html` returns 1.
    - `grep -c "path: 'notifications'" src/frontend/ecommerce-app/src/app/app.routes.ts` returns 1.
    - `grep -c "destructive-icon" src/frontend/ecommerce-app/src/app/features/notifications/notifications-page/notifications-page.component.html` returns at least 1.
    - `cd src/frontend/ecommerce-app && npx tsc --noEmit -p tsconfig.app.json` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>cd src/frontend/ecommerce-app && npx tsc --noEmit -p tsconfig.app.json</automated>
  </verify>
  <done>NotificationsPageComponent renders loading/empty/error/populated states per UI-SPEC, each row links to /orders/:id, only the PaymentFailed icon is colored red, and the page is reachable via a new toolbar nav link and route.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Angular runtime (browser) -> YARP Gateway -> Notifications.API | Existing authenticated GET pattern (same as `/orders`); this plan adds no new auth surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-05-13 | Information Disclosure | `message`/`eventType` rendered verbatim in the DOM | accept | Backend only ever produces 3 curated, locked strings (plan 05-05) — Angular does no server-error passthrough, so no stack trace or internal detail can reach the DOM via this field |
| T-05-14 | Tampering | Client attempting to pass a `userId`/filter parameter to scope results | mitigate | `NotificationsService.getNotifications()` takes no parameters by design — the backend derives scope entirely from the JWT server-side; there is no client-side code path that could even attempt to pass one |
</threat_model>

<verification>
`cd src/frontend/ecommerce-app && npx tsc --noEmit -p tsconfig.app.json` succeeds with zero type errors. Manual smoke check (documented for the developer, not automatable in this environment): after 05-04/05-05 land, log in, place an order, wait for it to reach Paid then Fulfilled, click "Notifications" in the nav bar, confirm three entries appear ("Your order has been paid.", "Your order has shipped.", plus a "Payment failed for your order." entry if a `.99` demo order was also placed), each linking to the correct `/orders/:id`.
</verification>

<success_criteria>
NOT-01 is demoable end-to-end through the UI: a logged-in user reaches `/notifications` via the nav bar and sees their inbox, matching the FE-03 precedent for making saga visibility real rather than API-only.
</success_criteria>

<output>
Create `.planning/phases/05-fulfillment-notifications/05-08-SUMMARY.md` when done
</output>
