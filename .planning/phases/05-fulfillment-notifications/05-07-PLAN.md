---
phase: 05-fulfillment-notifications
plan: 07
type: execute
wave: 3
depends_on: ["05-05"]
files_modified:
  - src/services/notifications/ECommerce.Notifications.Tests/ECommerce.Notifications.Tests.csproj
  - src/services/notifications/ECommerce.Notifications.Tests/Integration/NotificationsEndpointSteps.cs
  - src/services/notifications/ECommerce.Notifications.Tests/Integration/NotificationsEndpointTests.cs
  - src/services/notifications/ECommerce.Notifications.Tests/Integration/OrderPaidNotificationConsumerSteps.cs
  - src/services/notifications/ECommerce.Notifications.Tests/Integration/OrderPaidNotificationConsumerTests.cs
  - src/services/notifications/ECommerce.Notifications.Tests/Integration/OrderShippedNotificationConsumerSteps.cs
  - src/services/notifications/ECommerce.Notifications.Tests/Integration/OrderShippedNotificationConsumerTests.cs
  - src/services/notifications/ECommerce.Notifications.Tests/Integration/PaymentFailedNotificationConsumerSteps.cs
  - src/services/notifications/ECommerce.Notifications.Tests/Integration/PaymentFailedNotificationConsumerTests.cs
  - src/services/notifications/ECommerce.Notifications.Tests/Integration/OrderPaidNotificationInboxDeduplicationSteps.cs
  - src/services/notifications/ECommerce.Notifications.Tests/Integration/OrderPaidNotificationInboxDeduplicationTests.cs
autonomous: true
requirements: [NOT-01, NOT-02]

must_haves:
  truths:
    - "GET /notifications returns only the authenticated caller's own entries, newest first — proven against a real WebApplicationFactory-hosted endpoint, not just code review (NOT-01, IDOR-safe)."
    - "Each of the three notification consumers inserts a correctly-populated NotificationEntry row for its triggering event (NOT-02)."
    - "A redelivered OrderStatusChanged (same transport MessageId) produces exactly one NotificationEntry row, not two — proven against a real Postgres-backed EF Core inbox (NOT-02)."
  artifacts:
    - path: "src/services/notifications/ECommerce.Notifications.Tests/Integration/NotificationsEndpointSteps.cs"
      provides: "WebApplicationFactory-based proof that GET /notifications is JWT-scoped and IDOR-safe"
  key_links:
    - from: "NotificationsEndpointSteps.cs"
      to: "GET /notifications"
      via: "TestAuthHandler.TestUserIdHeader"
      pattern: "TestUserIdHeader"
    - from: "OrderPaidNotificationInboxDeduplicationSteps.cs"
      to: "NotificationEntry"
      via: "db.NotificationEntries.CountAsync() == 1"
      pattern: "NotificationEntries.CountAsync"
---

<objective>
Prove Notifications' new HTTP endpoint and three new consumers (05-05) independently and correctly: `GET /notifications` via a real `WebApplicationFactory`-hosted test (mirroring `OrdersEndpointSteps.cs`'s exact pattern), each consumer's row-insertion behavior via in-memory MassTransit harnesses (mirroring `CatalogSeededConsumerSteps.cs`), and idempotency via a real Postgres-backed forced-redelivery test (mirroring `CatalogSeededInboxDeduplicationSteps.cs`).

Purpose: Deliver automated, repeatable proof of NOT-01 (IDOR-safe scoped retrieval) and NOT-02 (correct content + idempotent persistence) — the two requirements this phase's REQUIREMENTS.md and ROADMAP.md success criteria hold Notifications to.
Output: A new `NotificationsWebApplicationFactory` + endpoint test suite, three consumer test suites, and one inbox-dedup test suite, all added to the existing `ECommerce.Notifications.Tests` project.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-fulfillment-notifications/05-CONTEXT.md
@.planning/phases/05-fulfillment-notifications/05-RESEARCH.md
@.planning/phases/05-fulfillment-notifications/05-PATTERNS.md

<interfaces>
`WebApplicationFactory` + `TestAuthHandler` pattern to replicate (from `src/services/orders/ECommerce.Orders.Tests/Integration/OrdersEndpointSteps.cs`, the ONLY existing example of this pattern in the repo — full excerpt already read during planning, see `<read_first>` below). Key pieces: `ServiceWebApplicationFactory`-style `ConfigureWebHost` override setting `ConnectionStrings:postgres`/`ConnectionStrings:messaging=placeholder` via BOTH `Environment.SetEnvironmentVariable` (Aspire's `AddNpgsqlDbContext` reads eagerly, before `ConfigureAppConfiguration` runs) AND `config.AddInMemoryCollection(...)`; `services.RemoveAll&lt;DbInitializer&gt;()`; swap the real JWT scheme for `TestAuthHandler.SchemeName`; requests set `TestAuthHandler.TestUserIdHeader` to authenticate as an arbitrary user.

```csharp
// TestAuthHandler (src/building-blocks/Tests.Common/TestAuthHandler.cs, unchanged, already exists)
public const string SchemeName = "TestAuth";
public const string TestUserIdHeader = "X-Test-User-Id";
```

Two-class consumer test pattern to replicate (from `CatalogSeededConsumerSteps.cs`/`CatalogSeededInboxDeduplicationSteps.cs`) — same as used in plan 05-06 for Fulfillment.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GET /notifications — WebApplicationFactory endpoint test (IDOR-safe, JWT-scoped)</name>
  <files>
    src/services/notifications/ECommerce.Notifications.Tests/ECommerce.Notifications.Tests.csproj,
    src/services/notifications/ECommerce.Notifications.Tests/Integration/NotificationsEndpointSteps.cs,
    src/services/notifications/ECommerce.Notifications.Tests/Integration/NotificationsEndpointTests.cs
  </files>
  <read_first>
    src/services/orders/ECommerce.Orders.Tests/Integration/OrdersEndpointSteps.cs,
    src/services/orders/ECommerce.Orders.Tests/Integration/OrdersEndpointTests.cs,
    src/building-blocks/Tests.Common/TestAuthHandler.cs,
    src/services/notifications/ECommerce.Notifications.API/Program.cs
  </read_first>
  <behavior>
    - Authenticated as `userA`, two `NotificationEntry` rows exist for `userA` and one for `userB` -> `GET /notifications` returns exactly the two `userA` rows, newest `OccurredAt` first.
    - Authenticated as `userB` (different `X-Test-User-Id` header) -> the SAME endpoint returns exactly `userB`'s one row — `userA`'s rows never leak (IDOR-safe, mirrors `GET /orders`'s existing, tested mitigation).
    - No `NotificationEntry` rows exist for the caller -> `GET /notifications` returns 200 with an empty array (not 404 — an empty inbox is a valid, expected state).
  </behavior>
  <action>
    In `ECommerce.Notifications.Tests.csproj`, add `Microsoft.AspNetCore.Mvc.Testing` version `10.0.9` (already-vetted, already-pinned identically in `ECommerce.Orders.Tests.csproj`) — required for `WebApplicationFactory&lt;Program&gt;`, not currently referenced by this test project.

    Create `NotificationsEndpointSteps.cs` containing a private `NotificationsWebApplicationFactory : WebApplicationFactory&lt;Program&gt;` class (mirrors `OrdersWebApplicationFactory`'s exact shape from `OrdersEndpointSteps.cs`, simplified — Notifications has no typed HTTP client to redirect, only postgres + messaging): constructor takes `string postgresConnectionString`, sets `Environment.SetEnvironmentVariable("ConnectionStrings__postgres", postgresConnectionString)` and `Environment.SetEnvironmentVariable("ConnectionStrings__messaging", "placeholder")` BEFORE the host builds (same eagerness rationale as `OrdersWebApplicationFactory`'s comment explains); `ConfigureWebHost` adds the same in-memory config collection, calls `services.RemoveAll&lt;DbInitializer&gt;();`, and swaps in `TestAuthHandler` via `services.AddAuthentication(TestAuthHandler.SchemeName).AddScheme&lt;AuthenticationSchemeOptions, TestAuthHandler&gt;(TestAuthHandler.SchemeName, _ => { });`. The outer `NotificationsEndpointSteps` class (mirrors `OrdersEndpointSteps`'s shape, takes a `PostgresFixture` in its constructor) exposes: `Given_NotificationExistsForUser(string userId, string eventType, string message, Guid orderId, DateTimeOffset occurredAt)` (inserts a row directly via a short-lived `NotificationsDbContext` against the fixture's connection string, migrating first); `When_GetNotificationsIsCalled(string userId)` (builds an `HttpRequestMessage(HttpMethod.Get, "/notifications")`, adds `TestAuthHandler.TestUserIdHeader` = `userId`, sends via `_factory.CreateClient()`); `Then_ResponseIs200(HttpResponseMessage)`; `Then_ResponseContainsExactlyEntriesFor(HttpResponseMessage, string expectedUserId, int expectedCount)` (deserializes the JSON array, asserts count and that every entry's `userId` field equals `expectedUserId` — confirm the exact camelCase JSON property name by inspecting a sample response or the entity's serialized shape, since `NotificationEntry` is returned as a raw entity, not a DTO).

    Create `NotificationsEndpointTests.cs` (`IClassFixture`-or-manual-`PostgresFixture`-per-test, mirrors `OrdersEndpointTests.cs`'s shape) with three `[Fact]`s covering the three behaviors above.
  </action>
  <acceptance_criteria>
    - `dotnet test src/services/notifications/Notifications.sln --filter "FullyQualifiedName~NotificationsEndpointTests"` passes with at least 3 tests and 0 failures.
    - `grep -c "TestAuthHandler.TestUserIdHeader" src/services/notifications/ECommerce.Notifications.Tests/Integration/NotificationsEndpointSteps.cs` returns at least 1.
    - `grep -c 'MvcTesting\|Mvc.Testing" Version="10.0.9"' src/services/notifications/ECommerce.Notifications.Tests/ECommerce.Notifications.Tests.csproj` returns at least 1.
  </acceptance_criteria>
  <verify>
    <automated>dotnet test src/services/notifications/Notifications.sln --filter "FullyQualifiedName~NotificationsEndpointTests" --logger "console;verbosity=normal"</automated>
  </verify>
  <done>GET /notifications is proven JWT-scoped and IDOR-safe against a real hosted instance — a different X-Test-User-Id never sees another user's entries.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Three consumer row-insertion tests</name>
  <files>
    src/services/notifications/ECommerce.Notifications.Tests/Integration/OrderPaidNotificationConsumerSteps.cs,
    src/services/notifications/ECommerce.Notifications.Tests/Integration/OrderPaidNotificationConsumerTests.cs,
    src/services/notifications/ECommerce.Notifications.Tests/Integration/OrderShippedNotificationConsumerSteps.cs,
    src/services/notifications/ECommerce.Notifications.Tests/Integration/OrderShippedNotificationConsumerTests.cs,
    src/services/notifications/ECommerce.Notifications.Tests/Integration/PaymentFailedNotificationConsumerSteps.cs,
    src/services/notifications/ECommerce.Notifications.Tests/Integration/PaymentFailedNotificationConsumerTests.cs
  </files>
  <read_first>
    src/services/notifications/ECommerce.Notifications.Tests/Integration/CatalogSeededConsumerSteps.cs,
    src/services/notifications/ECommerce.Notifications.Tests/Integration/CatalogSeededConsumerTests.cs
  </read_first>
  <behavior>
    - `OrderStatusChanged{NewStatus: "Paid"}` published -> exactly one `NotificationEntry` row exists with `Message == "Your order has been paid."` and `EventType == "OrderPaid"`.
    - `OrderStatusChanged{NewStatus: "Cancelled"}` published -> zero `NotificationEntry` rows are inserted by `OrderPaidNotificationConsumer`.
    - `OrderShipped` published -> exactly one row with `Message == "Your order has shipped."` and `EventType == "OrderShipped"`.
    - `PaymentFailed` published -> exactly one row with `Message == "Payment failed for your order."` and `EventType == "PaymentFailed"`.
  </behavior>
  <action>
    For EACH of the three consumers, create a `*Steps.cs`/`*Tests.cs` pair mirroring `CatalogSeededConsumerSteps.cs`'s in-memory-harness shape, extended to assert DATABASE CONTENT (not just consumed-count, since these consumers write a row `CatalogSeededConsumer` never did): `services.AddDbContext&lt;NotificationsDbContext&gt;(o => o.UseInMemoryDatabase($"notifications-{Guid.NewGuid()}"))` (unique DB name per test instance to avoid cross-test row bleed), `services.AddMassTransitTestHarness(x => x.AddConsumer&lt;{Consumer}&gt;())`. Step methods per suite: a `Given_Harness...` setup method; a `When_{Event}Published(...)` method taking the fields the test needs to vary (at minimum `userId`, and for `OrderPaidNotificationConsumer` also `newStatus` to test the filter); a `Then_NotificationEntryExists(string expectedMessage, string expectedEventType)` querying `db.NotificationEntries` directly (via a fresh scope against the SAME in-memory database name) and asserting exactly one matching row; for `OrderPaidNotificationConsumerSteps` specifically, also add `Then_NoNotificationEntryExists()` for the filtered-out (`NewStatus != "Paid"`) case.

    `OrderPaidNotificationConsumerTests.cs`: two `[Fact]`s — `OrderPaidNotificationConsumer_WhenNewStatusIsPaid_InsertsNotificationEntry`, `OrderPaidNotificationConsumer_WhenNewStatusIsNotPaid_InsertsNoRow`.
    `OrderShippedNotificationConsumerTests.cs`: one `[Fact]` — `OrderShippedNotificationConsumer_WhenOrderShippedPublished_InsertsNotificationEntry`.
    `PaymentFailedNotificationConsumerTests.cs`: one `[Fact]` — `PaymentFailedNotificationConsumer_WhenPaymentFailedPublished_InsertsNotificationEntry`.
  </action>
  <acceptance_criteria>
    - `dotnet test src/services/notifications/Notifications.sln --filter "FullyQualifiedName~OrderPaidNotificationConsumerTests|FullyQualifiedName~OrderShippedNotificationConsumerTests|FullyQualifiedName~PaymentFailedNotificationConsumerTests"` passes with at least 4 tests and 0 failures.
    - `grep -c "Your order has been paid." src/services/notifications/ECommerce.Notifications.Tests/Integration/OrderPaidNotificationConsumerSteps.cs` returns at least 1.
    - `grep -c "Your order has shipped." src/services/notifications/ECommerce.Notifications.Tests/Integration/OrderShippedNotificationConsumerSteps.cs` returns at least 1.
    - `grep -c "Payment failed for your order." src/services/notifications/ECommerce.Notifications.Tests/Integration/PaymentFailedNotificationConsumerSteps.cs` returns at least 1.
  </acceptance_criteria>
  <verify>
    <automated>dotnet test src/services/notifications/Notifications.sln --filter "FullyQualifiedName~OrderPaidNotificationConsumerTests|FullyQualifiedName~OrderShippedNotificationConsumerTests|FullyQualifiedName~PaymentFailedNotificationConsumerTests" --logger "console;verbosity=normal"</automated>
  </verify>
  <done>All three notification consumers are proven to insert correctly-populated rows for their triggering event, and OrderPaidNotificationConsumer is proven to correctly filter out non-Paid transitions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Postgres-backed forced-redelivery inbox dedup test</name>
  <files>
    src/services/notifications/ECommerce.Notifications.Tests/Integration/OrderPaidNotificationInboxDeduplicationSteps.cs,
    src/services/notifications/ECommerce.Notifications.Tests/Integration/OrderPaidNotificationInboxDeduplicationTests.cs
  </files>
  <read_first>
    src/services/notifications/ECommerce.Notifications.Tests/Integration/CatalogSeededInboxDeduplicationSteps.cs,
    src/services/notifications/ECommerce.Notifications.Tests/Integration/CatalogSeededInboxDeduplicationTests.cs
  </read_first>
  <behavior>
    - The SAME `OrderStatusChanged{NewStatus: "Paid"}` message (same transport `MessageId`, pinned explicitly) published TWICE against a real Postgres-backed EF Core outbox/inbox -> exactly ONE `InboxState` row AND exactly ONE `NotificationEntry` row exist afterward (NOT-02's exact stated requirement: "verified by a forced-redelivery test producing no duplicate inbox rows").
  </behavior>
  <action>
    Create `OrderPaidNotificationInboxDeduplicationSteps.cs` mirroring `CatalogSeededInboxDeduplicationSteps.cs`'s EXACT shape (`PostgresFixture`, `AddDbContext&lt;NotificationsDbContext&gt;(o => o.UseNpgsql(connectionString))`, `AddMassTransitTestHarness(x => { x.AddConsumer&lt;OrderPaidNotificationConsumer&gt;(); x.AddEntityFrameworkOutbox&lt;NotificationsDbContext&gt;(o => o.UsePostgres()); x.AddConfigureEndpointsCallback((context, name, cfg) => cfg.UseEntityFrameworkOutbox&lt;NotificationsDbContext&gt;(context)); })` — no `UseBusOutbox()`, Notifications is consumer-only, matching the analog exactly). Migrate before starting. Step methods: `Given_HarnessWithPostgresInbox()`; `When_SameOrderStatusChangedPublishedTwice(Guid messageId, Guid orderId, string userId)` publishing the pinned-`MessageId` message twice via `_harness!.Bus.Publish(message, ctx => ctx.MessageId = messageId);` (twice, each followed by `await _harness!.InactivityTask;`); `Then_InboxStateHasExactlyOneRow()` (as the analog); `Then_NotificationEntriesHasExactlyOneRow()` — NEW assertion beyond the analog, querying `db.NotificationEntries.CountAsync() == 1`, since NOT-02's literal requirement is about inbox ROW duplication, not just `InboxState` bookkeeping.

    Create `OrderPaidNotificationInboxDeduplicationTests.cs` (`IAsyncLifetime`-delegating, mirrors `CatalogSeededInboxDeduplicationTests.cs`'s shape) with one `[Fact]`: `OrderPaidNotificationConsumer_WhenSameMessageIdDeliveredTwice_ProducesExactlyOneNotificationEntry`.
  </action>
  <acceptance_criteria>
    - `dotnet test src/services/notifications/Notifications.sln --filter "FullyQualifiedName~OrderPaidNotificationInboxDeduplicationTests"` passes with 1 test and 0 failures.
    - `grep -c "ctx.MessageId = messageId" src/services/notifications/ECommerce.Notifications.Tests/Integration/OrderPaidNotificationInboxDeduplicationSteps.cs` returns 2.
    - `grep -c "NotificationEntries.CountAsync" src/services/notifications/ECommerce.Notifications.Tests/Integration/OrderPaidNotificationInboxDeduplicationSteps.cs` returns at least 1.
  </acceptance_criteria>
  <verify>
    <automated>dotnet test src/services/notifications/Notifications.sln --filter "FullyQualifiedName~OrderPaidNotificationInboxDeduplicationTests" --logger "console;verbosity=normal"</automated>
  </verify>
  <done>A real Postgres-backed forced-redelivery test proves NOT-02's literal requirement: duplicate delivery produces no duplicate inbox rows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Test-only — no new production trust boundary | This plan adds test infrastructure only; no production code changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-05-12 | N/A (test-only plan) | — | accept | This plan modifies only test projects; no new production attack surface is introduced. The IDOR-safety proof in Task 1 IS the mitigation evidence for T-05-08 (registered in plan 05-05) — that threat is closed here, not opened |
</threat_model>

<verification>
`dotnet test src/services/notifications/Notifications.sln --filter "FullyQualifiedName~NotificationsEndpointTests"`, the three consumer test filters, and `--filter "FullyQualifiedName~OrderPaidNotificationInboxDeduplicationTests"` all pass with 0 failures.
</verification>

<success_criteria>
NOT-01 and NOT-02 are independently and automatically proven: the endpoint is IDOR-safe, every consumer writes correct content, and redelivery produces no duplicate rows.
</success_criteria>

<output>
Create `.planning/phases/05-fulfillment-notifications/05-07-SUMMARY.md` when done
</output>
