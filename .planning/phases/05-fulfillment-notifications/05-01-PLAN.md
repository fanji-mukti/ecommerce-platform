---
phase: 05-fulfillment-notifications
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/building-blocks/Contracts/Fulfillment/Events/V1/OrderShipped.cs
  - src/building-blocks/Contracts/Orders/Events/V1/OrderStatusChanged.cs
  - src/building-blocks/Contracts/Payments/Events/V1/PaymentFailed.cs
  - src/building-blocks/Contracts/Payments/Commands/V1/AuthorisePayment.cs
autonomous: true
requirements: [FUL-01, FUL-02, NOT-02]

must_haves:
  truths:
    - "Every message Fulfillment publishes and every message Notifications will consume carries enough information (UserId) to scope an in-app inbox entry without a synchronous cross-service lookup (D-03)."
    - "No downstream Phase 5 plan (Orders saga wiring, Payments consumer, Fulfillment service, Notifications consumers) has to invent or renegotiate a contract shape mid-implementation."
  artifacts:
    - path: "src/building-blocks/Contracts/Fulfillment/Events/V1/OrderShipped.cs"
      provides: "New Fulfillment-context event: the FUL-02 signal that shipment processing completed"
    - path: "src/building-blocks/Contracts/Orders/Events/V1/OrderStatusChanged.cs"
      provides: "UserId field added (D-03) — the signal Fulfillment/Notifications filter on for the Paid transition"
    - path: "src/building-blocks/Contracts/Payments/Events/V1/PaymentFailed.cs"
      provides: "UserId field added (D-03) — consumed directly by Notifications' PaymentFailedNotificationConsumer"
    - path: "src/building-blocks/Contracts/Payments/Commands/V1/AuthorisePayment.cs"
      provides: "UserId field added (D-03) — lets Payments echo UserId into PaymentFailed without a lookup"
  key_links:
    - from: "src/building-blocks/Contracts/Fulfillment/Events/V1/OrderShipped.cs"
      to: "IMessageEnvelope"
      via: "record ... : IMessageEnvelope"
      pattern: "IMessageEnvelope"
---

<objective>
Define the one new message contract (`OrderShipped`) and extend three existing contracts with a `UserId` field (D-03) — the interface-first foundation every other Phase 5 plan (Orders saga wiring, Payments consumer, Fulfillment service, Notifications consumers) builds against. No other plan should need to invent a contract shape.

Purpose: Establish a single source of truth for Phase 5's message shapes before any consumer/producer code is written, per Interface-First Task Ordering. This is a pure `Contracts.csproj` change — no service logic.
Output: Compiling `Contracts.csproj` with `OrderShipped` (new) and `UserId` added to `OrderStatusChanged`, `PaymentFailed`, `AuthorisePayment`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-fulfillment-notifications/05-CONTEXT.md
@.planning/phases/05-fulfillment-notifications/05-RESEARCH.md
@.planning/phases/05-fulfillment-notifications/05-PATTERNS.md
@docs/adr/0005-saga-orchestration.md
@docs/adr/0006-masstransit-outbox-inbox.md

<interfaces>
Envelope every contract MUST implement (from `src/building-blocks/Contracts/IMessageEnvelope.cs`):

```
public interface IMessageEnvelope
{
    Guid MessageId { get; }
    Guid CorrelationId { get; }
    Guid CausationId { get; }
    DateTimeOffset OccurredAt { get; }
}
```

Analog to copy exactly for `OrderShipped` (from `src/building-blocks/Contracts/Fulfillment/Events/V1/FulfillmentFailed.cs`, full file):

```csharp
using ECommerce.Contracts;

namespace ECommerce.Fulfillment.Events.V1;

public record FulfillmentFailed(
    Guid MessageId,
    Guid CorrelationId,
    Guid CausationId,
    DateTimeOffset OccurredAt,
    Guid CheckoutId,
    string Reason,
    DateTimeOffset FailedAt
) : IMessageEnvelope;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the OrderShipped contract</name>
  <files>src/building-blocks/Contracts/Fulfillment/Events/V1/OrderShipped.cs</files>
  <read_first>
    src/building-blocks/Contracts/IMessageEnvelope.cs,
    src/building-blocks/Contracts/Fulfillment/Events/V1/FulfillmentFailed.cs
  </read_first>
  <action>
    Create `src/building-blocks/Contracts/Fulfillment/Events/V1/OrderShipped.cs` in namespace `ECommerce.Fulfillment.Events.V1`, following `FulfillmentFailed.cs`'s exact shape (same envelope fields, same `CheckoutId` naming convention — not `OrderId`, this is the Fulfillment context's own vocabulary per ADR-0007): `public record OrderShipped(Guid MessageId, Guid CorrelationId, Guid CausationId, DateTimeOffset OccurredAt, Guid CheckoutId, string UserId, DateTimeOffset ShippedAt) : IMessageEnvelope;`. This is the FUL-02 signal Fulfillment publishes after its timer-based processing simulation completes, correlated back to the Orders saga by `CheckoutId` and consumed directly by Notifications (`UserId` populated per D-03, no synchronous lookup).
  </action>
  <acceptance_criteria>
    - `dotnet build src/building-blocks/Contracts/Contracts.csproj` succeeds with zero errors.
    - `grep -c "IMessageEnvelope" src/building-blocks/Contracts/Fulfillment/Events/V1/OrderShipped.cs` returns 1.
    - `grep -c "string UserId" src/building-blocks/Contracts/Fulfillment/Events/V1/OrderShipped.cs` returns 1.
    - `grep -c "Guid CheckoutId" src/building-blocks/Contracts/Fulfillment/Events/V1/OrderShipped.cs` returns 1 (not `OrderId` — Fulfillment context vocabulary).
  </acceptance_criteria>
  <verify>
    <automated>dotnet build src/building-blocks/Contracts/Contracts.csproj</automated>
  </verify>
  <done>OrderShipped.cs exists, implements IMessageEnvelope, and compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Add UserId (D-03) to OrderStatusChanged, PaymentFailed, AuthorisePayment</name>
  <files>
    src/building-blocks/Contracts/Orders/Events/V1/OrderStatusChanged.cs,
    src/building-blocks/Contracts/Payments/Events/V1/PaymentFailed.cs,
    src/building-blocks/Contracts/Payments/Commands/V1/AuthorisePayment.cs
  </files>
  <read_first>
    src/building-blocks/Contracts/Orders/Events/V1/OrderStatusChanged.cs,
    src/building-blocks/Contracts/Payments/Events/V1/PaymentFailed.cs,
    src/building-blocks/Contracts/Payments/Commands/V1/AuthorisePayment.cs,
    src/building-blocks/Contracts/Payments/Events/V1/PaymentAuthorised.cs
  </read_first>
  <action>
    These are positional C# records — insertion POSITION matters, and every existing call site in the codebase uses named arguments (so a missed call site is a compile error, not a silent bug; that is deliberate and desired here, per RESEARCH.md Pitfall 4). Insert `string UserId,` immediately after the correlating id field in each record, keeping every other field and any trailing default parameter exactly where it is:

    - `OrderStatusChanged.cs`: insert `string UserId,` immediately after `Guid OrderId,` — new field order: `MessageId, CorrelationId, CausationId, OccurredAt, OrderId, UserId, PreviousStatus, NewStatus, ChangedAt, FailureReason = null`.
    - `PaymentFailed.cs`: insert `string UserId,` immediately after `Guid CheckoutId,` — new field order: `MessageId, CorrelationId, CausationId, OccurredAt, CheckoutId, UserId, Amount, Reason, FailedAt`.
    - `AuthorisePayment.cs`: insert `string UserId,` immediately after `Guid CheckoutId,` — new field order: `MessageId, CorrelationId, CausationId, OccurredAt, CheckoutId, UserId, Amount, SimulatePaymentFailure = false`. This lets Payments echo `msg.UserId` into `PaymentFailed` (plan 05-03) with zero synchronous lookup back to Orders/Identity.

    Do NOT touch `PaymentAuthorised.cs` — Notifications never consumes it (only `OrderStatusChanged`, `OrderShipped`, `PaymentFailed` per D-03), so adding an unused field there only widens the blast radius for no benefit (RESEARCH.md Anti-Patterns).
  </action>
  <acceptance_criteria>
    - `dotnet build src/building-blocks/Contracts/Contracts.csproj` succeeds with zero errors.
    - `grep -c "string UserId" src/building-blocks/Contracts/Orders/Events/V1/OrderStatusChanged.cs` returns 1.
    - `grep -c "string UserId" src/building-blocks/Contracts/Payments/Events/V1/PaymentFailed.cs` returns 1.
    - `grep -c "string UserId" src/building-blocks/Contracts/Payments/Commands/V1/AuthorisePayment.cs` returns 1.
    - `grep -c "string UserId" src/building-blocks/Contracts/Payments/Events/V1/PaymentAuthorised.cs` returns 0 (unchanged).
    - `dotnet build src/services/orders/Orders.sln` and `dotnet build src/services/payments/Payments.sln` are EXPECTED to fail after this task alone (existing named-argument call sites in `OrderStateMachine.cs`/`AuthorisePaymentConsumer.cs`/their test files do not yet pass `UserId:` — this is intentional; plan 05-03 fixes every call site). Do not attempt to fix those call sites in this plan.
  </acceptance_criteria>
  <verify>
    <automated>dotnet build src/building-blocks/Contracts/Contracts.csproj</automated>
  </verify>
  <done>OrderStatusChanged, PaymentFailed, and AuthorisePayment each compile with a new required UserId field in the documented position; Contracts.csproj builds standalone. Orders.sln/Payments.sln are left temporarily non-compiling by design — plan 05-03 (Wave 2) fixes every call site.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Internal service-to-service (Azure Service Bus) | `UserId` on these contracts is populated server-side by the originating JWT claim at the point a saga/command is first created (Orders/Checkout), never from client-supplied message input — no new external trust boundary is introduced by this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-05-01 | Information Disclosure | `OrderShipped`/`OrderStatusChanged`/`PaymentFailed`/`AuthorisePayment` now carrying `UserId` | accept | These are internal, first-party, bus-only messages (never exposed to a public HTTP surface directly) between services already inside the same trust domain; `UserId` is the same identifier already exposed to the owning user via `GET /orders`, not new exposure |
| T-05-02 | Spoofing | A malicious/buggy internal publisher forging a `UserId` on one of these events | accept | Out of scope for this phase's trust boundary — all publishers (Orders, Payments) are internal services deriving `UserId` from the originating JWT server-side (`OrdersEndpoints.GetUserId`), never from client input at message-construction time (RESEARCH.md Security Domain) |
</threat_model>

<verification>
`dotnet build src/building-blocks/Contracts/Contracts.csproj` succeeds with zero errors. `OrderShipped.cs` implements `IMessageEnvelope`. `OrderStatusChanged`, `PaymentFailed`, `AuthorisePayment` each compile with `UserId` inserted at the documented position. `PaymentAuthorised` is unchanged.
</verification>

<success_criteria>
All Phase 5 message contracts exist, compile in isolation, and follow the established envelope/namespace convention. Every downstream Phase 5 plan (05-03 through 05-08) can be implemented against these shapes without renegotiating a contract.
</success_criteria>

<output>
Create `.planning/phases/05-fulfillment-notifications/05-01-SUMMARY.md` when done
</output>
