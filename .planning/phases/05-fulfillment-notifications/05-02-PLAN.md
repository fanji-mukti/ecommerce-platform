---
phase: 05-fulfillment-notifications
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/frontend/ecommerce-app/src/app/features/orders/order-detail/order-detail.component.ts
  - src/frontend/ecommerce-app/src/app/features/orders/order-detail/order-detail.component.html
  - src/frontend/ecommerce-app/src/app/features/orders/order-detail/order-detail.component.scss
autonomous: true
requirements: [FUL-02]

must_haves:
  truths:
    - "A user viewing /orders/:id while the order is Paid sees a 'Preparing your shipment…' indicator (D-07) instead of a static, possibly-stale snapshot."
    - "/orders/:id polls the backend every 1.5s until the order reaches Fulfilled, Cancelled, or Failed, then stops — a user who lands directly on this page (bookmark, refresh, order-history link) while the order is still Paid sees it update to Fulfilled live, with zero manual refresh (D-08, SC2)."
  artifacts:
    - path: "src/frontend/ecommerce-app/src/app/features/orders/order-detail/order-detail.component.ts"
      provides: "Net-new interval(1500)+switchMap+takeWhile+takeUntilDestroyed polling block; status==='Paid' derived shipping-indicator signal"
  key_links:
    - from: "order-detail.component.ts"
      to: "GET /api/orders/{id}"
      via: "interval(1500).pipe(switchMap(...))"
      pattern: "interval\\(1500\\)"
---

<objective>
Add a client-side-only "Preparing your shipment…" indicator (D-07) and live polling (D-08) to the existing `/orders/:id` page. This page currently performs exactly ONE `ordersService.getOrder(id)` call in `ngOnInit` with zero polling (confirmed by RESEARCH.md Pitfall 1) — this is net-new polling code, not an extension of something that already exists. No backend change is required: both features derive entirely from the existing, unchanged `GET /api/orders/{id}` response shape.

Purpose: Close the gap RESEARCH.md Pitfall 1 identifies — without this, a user who reaches `/orders/:id` directly (not via `/checkout`'s auto-redirect) while the order is `Paid` sees a static page forever, never learning the order shipped, even after Fulfillment (05-04) and the Orders saga (05-03) correctly drive the order to `Fulfilled` server-side.
Output: `order-detail.component.ts`/`.html`/`.scss` with a shipping indicator and a self-stopping polling loop.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-fulfillment-notifications/05-CONTEXT.md
@.planning/phases/05-fulfillment-notifications/05-RESEARCH.md
@.planning/phases/05-fulfillment-notifications/05-PATTERNS.md
@.planning/phases/05-fulfillment-notifications/05-UI-SPEC.md

<interfaces>
`OrderDetail` model this component already consumes (from `shared/models/order.model.ts`, unchanged by this plan):

```typescript
export interface OrderDetail {
  id: string;
  status: string;
  totalAmount: number;
  lineItems: OrderLineItem[];
  createdAt: string;
  failureReason: string | null;
}
```

Polling pattern to structurally copy (from `checkout-page.component.ts`'s `startPolling`, full excerpt — this is the ONLY polling implementation in the repo today, it lives on `/checkout`, NOT on `/orders/:id`):

```typescript
const TERMINAL_STATUSES: CheckoutStatusValue[] = ['Paid', 'Cancelled', 'Failed', 'Fulfilled'];
private startPolling(id: string): void {
  interval(1500)
    .pipe(
      switchMap(() => this.checkoutService.getStatus(id)),
      takeWhile((status) => {
        this.currentStatus.set(status.status);
        return !this.isTerminal(status.status);
      }, true),
      takeUntilDestroyed(this.destroyRef),
    )
    .subscribe({
      next: (status) => { if (this.isTerminal(status.status)) { this.router.navigate(['/orders', id]); } },
      error: () => { this.hasError.set(true); },
    });
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Shipping indicator + self-stopping polling on OrderDetailComponent</name>
  <files>
    src/frontend/ecommerce-app/src/app/features/orders/order-detail/order-detail.component.ts,
    src/frontend/ecommerce-app/src/app/features/orders/order-detail/order-detail.component.html,
    src/frontend/ecommerce-app/src/app/features/orders/order-detail/order-detail.component.scss
  </files>
  <read_first>
    src/frontend/ecommerce-app/src/app/features/orders/order-detail/order-detail.component.ts,
    src/frontend/ecommerce-app/src/app/features/orders/order-detail/order-detail.component.html,
    src/frontend/ecommerce-app/src/app/features/orders/order-detail/order-detail.component.scss,
    src/frontend/ecommerce-app/src/app/features/checkout/checkout-page/checkout-page.component.ts,
    .planning/phases/05-fulfillment-notifications/05-UI-SPEC.md
  </read_first>
  <behavior>
    - On init, the existing single `ordersService.getOrder(id)` fetch remains (do not remove it — it renders the FIRST snapshot immediately, before polling's first 1.5s tick).
    - After the initial fetch succeeds, a new polling loop starts: `interval(1500)` → `switchMap` to `ordersService.getOrder(id)` → updates the `order` signal on every tick.
    - Polling stops (`takeWhile` with `inclusive: true`) the moment the fetched `status` is one of `'Fulfilled' | 'Cancelled' | 'Failed'` (D-08's terminal set — note this differs from `/checkout`'s `TERMINAL_STATUSES`, which also includes `'Paid'`; here `'Paid'` is NOT terminal because Fulfillment is still expected to move it to `'Fulfilled'`).
    - On terminal, polling stops IN PLACE — no `router.navigate` (the user is already on `/orders/:id`, unlike `/checkout`'s auto-redirect).
    - Polling tears down on component destroy via `takeUntilDestroyed(this.destroyRef)` (mirrors `checkout-page.component.ts`'s WR-04 fix — prevents duplicate polling loops if the user navigates away and back).
    - A new computed signal `isShipping = computed(() => this.order()?.status === 'Paid')` drives the shipping indicator's visibility — pure client-side inference from the already-fetched status, no new backend call.
    - A polling-tick error does not clear the currently-displayed order or flip the page into `notFound` — it is swallowed (log-free per existing project convention) so a single transient failure doesn't blank a page the user is actively viewing; only the INITIAL fetch error sets `notFound`.
  </behavior>
  <action>
    In `order-detail.component.ts`: add `DestroyRef` to the constructor injects (`private destroyRef = inject(DestroyRef)`), import `interval` from `rxjs` and `switchMap`, `takeWhile` from `rxjs/operators`, and `takeUntilDestroyed` from `@angular/core/rxjs-interop` (same imports `checkout-page.component.ts` already uses). Declare `const ORDER_DETAIL_TERMINAL_STATUSES = ['Fulfilled', 'Cancelled', 'Failed'];` at module scope (distinct name from `checkout-page.component.ts`'s `TERMINAL_STATUSES` — different terminal set, do not reuse/import across files). Add `isShipping = computed((): boolean => this.order()?.status === 'Paid');` alongside the existing `statusColor`/`showFailureReason` computed signals. After the existing `ordersService.getOrder(id).subscribe({...})` success branch in `ngOnInit` sets `this.order.set(order)` and `this.isLoading.set(false)`, call a new private method `this.startPolling(id);`. Implement `startPolling(id: string): void` structurally identical to `checkout-page.component.ts`'s `startPolling`, but: polls `this.ordersService.getOrder(id)` (not `checkoutService.getStatus`), the `takeWhile` predicate checks `ORDER_DETAIL_TERMINAL_STATUSES.includes(status.status)` (not the imported `CheckoutStatusValue`-typed helper), sets `this.order.set(status)` each tick (not just a status signal — the whole `OrderDetail`, since line items/total are also re-rendered), and the `next`/`error` callbacks do nothing beyond updating signals (no `router.navigate`, no `hasError`/`notFound` mutation on a polling-tick failure — only `ngOnInit`'s initial-fetch `error` callback sets `notFound`).

    In `order-detail.component.html`: add, immediately below the existing `<mat-chip-set class="status-badge">...</mat-chip-set>` block and its `@if (showFailureReason())` sibling, a new `@if (isShipping())` block containing `<mat-spinner diameter="18"></mat-spinner>` plus `<span class="shipping-indicator-text">Preparing your shipment…</span>` (exact locked copy per UI-SPEC), wrapped in a `<div class="shipping-indicator">`. Import `MatProgressSpinnerModule` into the component's `imports` array (not currently imported — confirm via read_first) and add it alongside the existing Material module imports.

    In `order-detail.component.scss`: add a `.shipping-indicator { display: flex; align-items: center; gap: var(--space-xs); margin-bottom: var(--space-lg); }` rule (per UI-SPEC's spacing table — `--space-xs` for spinner-to-text gap), placed after the existing `.status-badge` rule.
  </action>
  <acceptance_criteria>
    - `grep -c "interval(1500)" src/frontend/ecommerce-app/src/app/features/orders/order-detail/order-detail.component.ts` returns 1.
    - `grep -c "'Fulfilled', 'Cancelled', 'Failed'" src/frontend/ecommerce-app/src/app/features/orders/order-detail/order-detail.component.ts` returns at least 1.
    - `grep -c "isShipping" src/frontend/ecommerce-app/src/app/features/orders/order-detail/order-detail.component.ts` returns at least 2 (declaration + template usage source is html, so just check .ts declaration >=1 and .html usage >=1 separately if needed).
    - `grep -c "Preparing your shipment…" src/frontend/ecommerce-app/src/app/features/orders/order-detail/order-detail.component.html` returns 1.
    - `grep -c "takeUntilDestroyed" src/frontend/ecommerce-app/src/app/features/orders/order-detail/order-detail.component.ts` returns 1.
    - `grep -c "router.navigate" src/frontend/ecommerce-app/src/app/features/orders/order-detail/order-detail.component.ts` returns 0 (this page polls in place, unlike /checkout — no navigation on terminal state).
    - `cd src/frontend/ecommerce-app && npx tsc --noEmit -p tsconfig.app.json` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>cd src/frontend/ecommerce-app && npx tsc --noEmit -p tsconfig.app.json</automated>
  </verify>
  <done>OrderDetailComponent shows "Preparing your shipment…" with an 18px spinner while status is Paid, and self-stops a 1.5s polling loop once the order reaches Fulfilled/Cancelled/Failed — all against the existing, unchanged GET /api/orders/{id} endpoint.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Angular runtime (browser) → YARP Gateway | Existing, unchanged — this plan adds a polling cadence to an already-authenticated GET, no new auth surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-05-03 | Denial of Service (client-generated load) | New 1.5s polling loop on `/orders/:id` | accept | Identical cadence and stop-condition discipline already shipped and accepted on `/checkout` (Phase 4); `takeWhile`+`takeUntilDestroyed` bound the loop to a single terminal-status reach or component destroy — no unbounded polling |
</threat_model>

<verification>
`cd src/frontend/ecommerce-app && npx tsc --noEmit -p tsconfig.app.json` succeeds with zero type errors. Manual smoke check (documented for the developer, not automatable in this environment): open `/orders/:id` for an order still in `Paid` status (e.g. mid-Fulfillment-processing-delay after 05-04 lands), confirm the "Preparing your shipment…" spinner shows and the page updates to `Fulfilled` automatically within ~1.5s of the backend transition, with no manual refresh.
</verification>

<success_criteria>
D-07 and D-08 are fully satisfied on the frontend: the shipping indicator and live polling work correctly against the existing `GET /orders/{id}` contract, independent of and in parallel with the Phase 5 backend work (05-01, 05-03 through 05-07).
</success_criteria>

<output>
Create `.planning/phases/05-fulfillment-notifications/05-02-SUMMARY.md` when done
</output>
