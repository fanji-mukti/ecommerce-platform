---
phase: 04-checkout-saga-payments
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - src/services/orders/ECommerce.Orders.API/Features/Orders/OrderDto.cs
  - src/services/orders/ECommerce.Orders.API/Features/Orders/OrdersEndpoints.cs
  - src/services/orders/ECommerce.Orders.Tests/Integration/OrdersEndpointSteps.cs
  - src/services/orders/ECommerce.Orders.Tests/Integration/OrdersEndpointTests.cs
autonomous: true
requirements: [CHK-01, CHK-02]

must_haves:
  truths:
    - "A caller can POST a checkoutId + cart-derived order to Orders.API and receive 202 immediately, with the saga instance guaranteed to exist by the time the response returns."
    - "GET /orders/{id} returns the FailureReason alongside status, so downstream Checkout.API/Angular can surface a human-readable reason (D-09) without any additional endpoint."
    - "The Phase 3 demo-only test-trigger endpoint is fully retired, per Phase 3's own D-01/D-02 commitment."
  artifacts:
    - path: "src/services/orders/ECommerce.Orders.API/Features/Orders/OrdersEndpoints.cs"
      provides: "POST /orders/checkout (replaces test-create-from-cart), GET /orders/{id} extended with FailureReason"
  key_links:
    - from: "POST /orders/checkout"
      to: "OrderCreated"
      via: "IPublishEndpoint.Publish + SaveChangesAsync (outbox flush before HTTP response)"
      pattern: "Publish\\(new OrderCreated"
    - from: "GET /orders/{id}"
      to: "OrderDto.FailureReason"
      via: "OrderMapper.ToDto"
      pattern: "FailureReason"
---

<objective>
Give the Orders saga a real, checkout-driven HTTP entry point: `POST /orders/checkout` replaces Phase 3's demo-only `POST /orders/test-create-from-cart`, accepting a Checkout.API-minted `checkoutId` and using it directly as the `OrderId`/saga `CorrelationId`. Extend `GET /orders/{id}` to surface `FailureReason` so no separate "checkout status" endpoint is ever needed.

Purpose: Provide Checkout.API (plan 04-05) with the single Orders-side HTTP surface it needs to satisfy CHK-01 (checkoutId minting + order creation) and CHK-02 (status polling, via existing GET /orders/{id}).
Output: `POST /orders/checkout`, extended `GET /orders/{id}` response, retired test-trigger endpoint, updated integration test suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-checkout-saga-payments/04-CONTEXT.md
@.planning/phases/04-checkout-saga-payments/04-RESEARCH.md
@.planning/phases/04-checkout-saga-payments/04-PATTERNS.md

<interfaces>
`StartCheckout` contract (created in 04-01, `ECommerce.Checkout.Commands.V1`) — the JSON request body this endpoint deserializes:

```
public record StartCheckout(Guid MessageId, Guid CorrelationId, Guid CausationId, DateTimeOffset OccurredAt, Guid CheckoutId, bool SimulatePaymentFailure = false) : IMessageEnvelope;
```

`OrderCreated` (extended in 04-01) and `ICartClient`/`CartSnapshot` (existing, unchanged) are read_first sources below.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add POST /orders/checkout, extend OrderDto, retire test-create-from-cart</name>
  <files>
    src/services/orders/ECommerce.Orders.API/Features/Orders/OrderDto.cs,
    src/services/orders/ECommerce.Orders.API/Features/Orders/OrdersEndpoints.cs
  </files>
  <read_first>
    src/services/orders/ECommerce.Orders.API/Features/Orders/OrdersEndpoints.cs,
    src/services/orders/ECommerce.Orders.API/Features/Orders/OrderDto.cs,
    src/services/orders/ECommerce.Orders.API/Features/Orders/OrderMapper.cs,
    src/services/orders/ECommerce.Orders.API/Features/Orders/ICartClient.cs,
    src/building-blocks/Contracts/Checkout/Commands/V1/StartCheckout.cs
  </read_first>
  <behavior>
    - `POST /orders/checkout` with a valid `StartCheckout` body and a non-empty cart returns 202 with `{ orderId }` where `orderId == request.CheckoutId`.
    - `POST /orders/checkout` with an empty cart returns 400.
    - `GET /orders/{id}` for an order with a non-null `FailureReason` includes it in the JSON response body under `failureReason`.
    - `GET /orders/{id}` for an order with `FailureReason == null` includes `failureReason: null` (never omits the field).
    - `POST /orders/test-create-from-cart` no longer exists (404/405 on any request to that route).
  </behavior>
  <action>
    In `OrderDto.cs`, add `string? FailureReason` as the final positional parameter of the `OrderDto` record (after `CreatedAt`) — Mapperly's `OrderMapper.ToDto` auto-maps by property name against `OrderReadModel.FailureReason` (added in plan 04-02), no change needed to `OrderMapper.cs` itself. Do not add `FailureReason` to `OrderSummaryDto` (list view stays as-is).

    In `OrdersEndpoints.cs`: delete the entire `POST /orders/test-create-from-cart` handler (lines currently ~63-106). Add `POST /orders/checkout` in its place, accepting `[Microsoft.AspNetCore.Mvc.FromBody] StartCheckout request` plus the same `HttpContext httpContext, ClaimsPrincipal user, ICartClient cartClient, IPublishEndpoint publishEndpoint, OrdersDbContext db, CancellationToken ct` parameters as the old handler. Body: extract `userId`/`token` exactly as before; call `cartClient.GetCartAsync(token, ct)`; if null/empty return `Results.BadRequest(new { error = "Cart is empty." })`; set `var orderId = request.CheckoutId;` (reuse the caller-minted id — do NOT call `Guid.NewGuid()`); publish `new OrderCreated(MessageId: Guid.NewGuid(), CorrelationId: orderId, CausationId: request.MessageId, OccurredAt: now, OrderId: orderId, UserId: userId, LineItems: <mapped from cart>, TotalAmount: <cart sum>, CreatedAt: now, SimulatePaymentFailure: request.SimulatePaymentFailure)`; `await db.SaveChangesAsync(ct)` (flush outbox BEFORE clearing cart, preserving CART-04 ordering); `await cartClient.ClearCartAsync(token, ct)`; return `Results.Accepted($"/orders/{orderId}", new { orderId })`. Keep `.RequireAuthorization()`. Reuse `GetUserId`/`ExtractBearerToken` helpers verbatim (already private static in this file).

    `GET /orders/{id}` itself needs no logic change — once `OrderDto` has `FailureReason` and `OrderMapper.ToDto` is Mapperly source-generated, the field flows through automatically. Confirm by rebuilding (Mapperly regenerates at compile time).
  </action>
  <acceptance_criteria>
    - `dotnet build src/services/orders/Orders.sln` succeeds (confirms Mapperly regenerated `OrderMapper` cleanly against the new `FailureReason` property).
    - `grep -c "test-create-from-cart" src/services/orders/ECommerce.Orders.API/Features/Orders/OrdersEndpoints.cs` returns 0.
    - `grep -c "orders/checkout" src/services/orders/ECommerce.Orders.API/Features/Orders/OrdersEndpoints.cs` returns at least 1.
    - `grep -c "request.CheckoutId" src/services/orders/ECommerce.Orders.API/Features/Orders/OrdersEndpoints.cs` returns at least 1 (proves the caller-minted id is reused, not regenerated).
    - `grep -c "FailureReason" src/services/orders/ECommerce.Orders.API/Features/Orders/OrderDto.cs` returns at least 1.
  </acceptance_criteria>
  <verify>
    <automated>dotnet build src/services/orders/Orders.sln</automated>
  </verify>
  <done>POST /orders/checkout exists and reuses the caller-minted checkoutId as OrderId; GET /orders/{id} response includes FailureReason; the Phase 3 test-trigger endpoint is fully removed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update Orders integration test suite for the checkout endpoint</name>
  <files>
    src/services/orders/ECommerce.Orders.Tests/Integration/OrdersEndpointSteps.cs,
    src/services/orders/ECommerce.Orders.Tests/Integration/OrdersEndpointTests.cs
  </files>
  <read_first>
    src/services/orders/ECommerce.Orders.Tests/Integration/OrdersEndpointSteps.cs,
    src/services/orders/ECommerce.Orders.Tests/Integration/OrdersEndpointTests.cs
  </read_first>
  <action>
    In `OrdersEndpointSteps.cs`: replace `When_TestCreateFromCartIsCalled(string userId)` with `When_CheckoutIsCalled(string userId, Guid checkoutId, bool simulatePaymentFailure = false)` — POSTs to `/orders/checkout` with a JSON body `new { messageId = Guid.NewGuid(), correlationId = checkoutId, causationId = Guid.Empty, occurredAt = DateTimeOffset.UtcNow, checkoutId, simulatePaymentFailure }` (matching `StartCheckout`'s property names case-insensitively per System.Text.Json defaults) and the `TestAuthHandler.TestUserIdHeader` header. Update `Then_ResponseContainsOrderId` usage sites accordingly (still reads `body["orderId"]`, unchanged). Add `Then_ResponseOrderHasFailureReason(OrderDto? body, string expectedReason)` asserting `body!.FailureReason.Should().Be(expectedReason)`.

    In `OrdersEndpointTests.cs`, locate every test currently calling `When_TestCreateFromCartIsCalled` and update it to call `When_CheckoutIsCalled` with an explicit `Guid.NewGuid()` checkoutId, asserting the returned `orderId` equals that same checkoutId. Keep existing pagination/list/detail/404/IDOR tests unchanged (they operate on `Given_OrderReadModelExistsForUser`/`Given_UserHasOrders`, independent of this task).
  </action>
  <acceptance_criteria>
    - `dotnet test src/services/orders/Orders.sln --filter "FullyQualifiedName~OrdersEndpointTests"` passes with 0 failures.
    - `grep -c "When_TestCreateFromCartIsCalled" src/services/orders/ECommerce.Orders.Tests/Integration/OrdersEndpointSteps.cs src/services/orders/ECommerce.Orders.Tests/Integration/OrdersEndpointTests.cs` returns 0 across both files.
    - `grep -c "When_CheckoutIsCalled" src/services/orders/ECommerce.Orders.Tests/Integration/OrdersEndpointTests.cs` returns at least 1.
  </acceptance_criteria>
  <verify>
    <automated>dotnet test src/services/orders/Orders.sln --filter "FullyQualifiedName~OrdersEndpointTests" --logger "console;verbosity=normal"</automated>
  </verify>
  <done>All Orders integration tests pass against the new POST /orders/checkout endpoint; no reference to the retired test-trigger endpoint remains.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Angular/Checkout.API → POST /orders/checkout | Authenticated but attacker-controlled `checkoutId`/`simulatePaymentFailure` in the request body |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-04-07 | Spoofing | `checkoutId` supplied in `StartCheckout` body | accept | A caller could POST an arbitrary GUID as `checkoutId`; worst case they create an order under their own account with an id of their choosing — no cross-user impact since `UserId` is still derived from the JWT claim, not the body |
| T-04-08 | Information Disclosure | `GET /orders/{id}` `FailureReason` field | mitigate | Only the 3 curated strings from plan 04-02's saga are ever persisted — never raw exception/stack-trace text (ASVS V7) |
</threat_model>

<verification>
`dotnet build src/services/orders/Orders.sln` succeeds. `dotnet test src/services/orders/Orders.sln --filter "FullyQualifiedName~OrdersEndpointTests"` passes with 0 failures.
</verification>

<success_criteria>
Orders.API exposes a real checkout-driven order-creation endpoint using the caller-minted checkoutId as the order/saga id, and GET /orders/{id} surfaces FailureReason — the two Orders-side HTTP primitives Checkout.API needs.
</success_criteria>

<output>
Create `.planning/phases/04-checkout-saga-payments/04-03-SUMMARY.md` when done
</output>
