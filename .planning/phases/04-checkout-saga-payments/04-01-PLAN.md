---
phase: 04-checkout-saga-payments
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/building-blocks/Contracts/Checkout/Commands/V1/StartCheckout.cs
  - src/building-blocks/Contracts/Checkout/Commands/V1/Placeholder.cs
  - src/building-blocks/Contracts/Payments/Commands/V1/AuthorisePayment.cs
  - src/building-blocks/Contracts/Payments/Commands/V1/RefundPayment.cs
  - src/building-blocks/Contracts/Payments/Commands/V1/Placeholder.cs
  - src/building-blocks/Contracts/Payments/Events/V1/PaymentAuthorised.cs
  - src/building-blocks/Contracts/Payments/Events/V1/PaymentFailed.cs
  - src/building-blocks/Contracts/Payments/Events/V1/PaymentRefunded.cs
  - src/building-blocks/Contracts/Payments/Events/V1/Placeholder.cs
  - src/building-blocks/Contracts/Fulfillment/Events/V1/FulfillmentFailed.cs
  - src/building-blocks/Contracts/Fulfillment/Events/V1/Placeholder.cs
  - src/building-blocks/Contracts/Orders/Events/V1/OrderCreated.cs
  - src/building-blocks/Contracts/Orders/Events/V1/OrderStatusChanged.cs
  - src/ecommerce.AppHost/Program.cs
  - src/services/gateway/ECommerce.Gateway.API/appsettings.json
autonomous: true
requirements: [CHK-01, PAY-01]

must_haves:
  truths:
    - "The shared Contracts library defines every message shape the saga, Payments, and Checkout façade need before any of those services are implemented — no downstream plan has to invent a contract shape mid-implementation."
    - "Checkout.API, Payments.API, and the Gateway are wired into the AppHost/YARP topology so services started in later plans are immediately reachable without further infra edits."
  artifacts:
    - path: "src/building-blocks/Contracts/Checkout/Commands/V1/StartCheckout.cs"
      provides: "Internal HTTP request contract Checkout.API sends to Orders.API to mint a saga instance"
    - path: "src/building-blocks/Contracts/Payments/Commands/V1/AuthorisePayment.cs"
      provides: "Command the Orders saga publishes to trigger simulated payment processing"
    - path: "src/building-blocks/Contracts/Payments/Events/V1/PaymentFailed.cs"
      provides: "Event Payments publishes on deterministic/forced failure, consumed by the Orders saga for CHK-03"
    - path: "src/building-blocks/Contracts/Fulfillment/Events/V1/FulfillmentFailed.cs"
      provides: "Event Checkout.API's demo trigger publishes directly, consumed by the Orders saga for CHK-04"
  key_links:
    - from: "src/ecommerce.AppHost/Program.cs"
      to: "ECommerce.Payments.API"
      via: ".WithReference(postgres).WaitFor(postgres)"
      pattern: "payments.*WithReference\\(postgres\\)"
    - from: "src/services/gateway/ECommerce.Gateway.API/appsettings.json"
      to: "http://checkout"
      via: "checkout-route ReverseProxy cluster"
      pattern: "checkout-route"
---

<objective>
Define every new cross-service message contract this phase needs and wire the AppHost/Gateway topology so Checkout.API and Payments.API are reachable and provisioned correctly. This is the interface-first foundation every other Phase 4 plan (Orders saga, Payments service, Checkout façade, Angular) builds against — no other plan should need to invent a message shape.

Purpose: Establish a single source of truth for contract shapes and service wiring before any consumer/producer code is written, per Interface-First Task Ordering.
Output: Compiling `Contracts.csproj` with all Phase 4 message records; `ecommerce.AppHost/Program.cs` and Gateway `appsettings.json` correctly wired for `checkout` and `payments`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-checkout-saga-payments/04-CONTEXT.md
@.planning/phases/04-checkout-saga-payments/04-RESEARCH.md
@.planning/phases/04-checkout-saga-payments/04-PATTERNS.md
@docs/adr/0005-saga-orchestration.md
@docs/adr/0006-masstransit-outbox-inbox.md

<interfaces>
Envelope every new contract MUST implement (from `src/building-blocks/Contracts/IMessageEnvelope.cs`):

```
public interface IMessageEnvelope
{
    Guid MessageId { get; }
    Guid CorrelationId { get; }
    Guid CausationId { get; }
    DateTimeOffset OccurredAt { get; }
}
```

Existing envelope-record shape to copy verbatim (from `src/building-blocks/Contracts/Orders/Events/V1/OrderStatusChanged.cs`):

```
public record OrderStatusChanged(
    Guid MessageId, Guid CorrelationId, Guid CausationId, DateTimeOffset OccurredAt,
    Guid OrderId, string PreviousStatus, string NewStatus, DateTimeOffset ChangedAt
) : IMessageEnvelope;
```

`OrderCreated.cs` current shape (extend, do not replace):

```
public record OrderCreated(
    Guid MessageId, Guid CorrelationId, Guid CausationId, DateTimeOffset OccurredAt,
    Guid OrderId, string UserId, IReadOnlyList<OrderLineItemData> LineItems,
    decimal TotalAmount, DateTimeOffset CreatedAt
) : IMessageEnvelope;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author Phase 4 message contracts, extend OrderCreated/OrderStatusChanged</name>
  <files>
    src/building-blocks/Contracts/Checkout/Commands/V1/StartCheckout.cs,
    src/building-blocks/Contracts/Checkout/Commands/V1/Placeholder.cs,
    src/building-blocks/Contracts/Payments/Commands/V1/AuthorisePayment.cs,
    src/building-blocks/Contracts/Payments/Commands/V1/RefundPayment.cs,
    src/building-blocks/Contracts/Payments/Commands/V1/Placeholder.cs,
    src/building-blocks/Contracts/Payments/Events/V1/PaymentAuthorised.cs,
    src/building-blocks/Contracts/Payments/Events/V1/PaymentFailed.cs,
    src/building-blocks/Contracts/Payments/Events/V1/PaymentRefunded.cs,
    src/building-blocks/Contracts/Payments/Events/V1/Placeholder.cs,
    src/building-blocks/Contracts/Fulfillment/Events/V1/FulfillmentFailed.cs,
    src/building-blocks/Contracts/Fulfillment/Events/V1/Placeholder.cs,
    src/building-blocks/Contracts/Orders/Events/V1/OrderCreated.cs,
    src/building-blocks/Contracts/Orders/Events/V1/OrderStatusChanged.cs
  </files>
  <read_first>
    src/building-blocks/Contracts/IMessageEnvelope.cs,
    src/building-blocks/Contracts/Orders/Events/V1/OrderCreated.cs,
    src/building-blocks/Contracts/Orders/Events/V1/OrderStatusChanged.cs,
    src/building-blocks/Contracts/Checkout/Commands/V1/Placeholder.cs,
    src/building-blocks/Contracts/Payments/Events/V1/Placeholder.cs
  </read_first>
  <action>
    Delete the four now-superseded placeholder files (`Checkout/Commands/V1/Placeholder.cs`, `Payments/Commands/V1/Placeholder.cs`, `Payments/Events/V1/Placeholder.cs`, `Fulfillment/Events/V1/Placeholder.cs`) and replace with real contracts, namespace `ECommerce.{Area}.{Commands|Events}.V1` matching the existing convention exactly:

    - `Checkout/Commands/V1/StartCheckout.cs` — `public record StartCheckout(Guid MessageId, Guid CorrelationId, Guid CausationId, DateTimeOffset OccurredAt, Guid CheckoutId, bool SimulatePaymentFailure = false) : IMessageEnvelope;`. This is NOT published on the bus — it is the JSON request body Checkout.API POSTs synchronously to Orders.API's new `POST /orders/checkout` endpoint (built in plan 04-03). `CorrelationId` and `CheckoutId` are the same GUID.
    - `Payments/Commands/V1/AuthorisePayment.cs` — `public record AuthorisePayment(Guid MessageId, Guid CorrelationId, Guid CausationId, DateTimeOffset OccurredAt, Guid CheckoutId, decimal Amount, bool SimulatePaymentFailure = false) : IMessageEnvelope;`. Published by the Orders saga, consumed by Payments (plan 04-04).
    - `Payments/Commands/V1/RefundPayment.cs` — `public record RefundPayment(Guid MessageId, Guid CorrelationId, Guid CausationId, DateTimeOffset OccurredAt, Guid CheckoutId, decimal Amount, string Reason) : IMessageEnvelope;`. Published by the Orders saga on the CHK-04 fulfillment-failure path.
    - `Payments/Events/V1/PaymentAuthorised.cs` — `public record PaymentAuthorised(Guid MessageId, Guid CorrelationId, Guid CausationId, DateTimeOffset OccurredAt, Guid CheckoutId, decimal Amount, DateTimeOffset AuthorisedAt) : IMessageEnvelope;`.
    - `Payments/Events/V1/PaymentFailed.cs` — `public record PaymentFailed(Guid MessageId, Guid CorrelationId, Guid CausationId, DateTimeOffset OccurredAt, Guid CheckoutId, decimal Amount, string Reason, DateTimeOffset FailedAt) : IMessageEnvelope;`. Use exact reason string `"Payment declined"` when publishing from Payments (locked copy, D-09/UI-SPEC).
    - `Payments/Events/V1/PaymentRefunded.cs` — `public record PaymentRefunded(Guid MessageId, Guid CorrelationId, Guid CausationId, DateTimeOffset OccurredAt, Guid CheckoutId, decimal Amount, DateTimeOffset RefundedAt) : IMessageEnvelope;`.
    - `Fulfillment/Events/V1/FulfillmentFailed.cs` — `public record FulfillmentFailed(Guid MessageId, Guid CorrelationId, Guid CausationId, DateTimeOffset OccurredAt, Guid CheckoutId, string Reason, DateTimeOffset FailedAt) : IMessageEnvelope;`. Published directly by Checkout.API's demo-only trigger endpoint (plan 04-05), consumed by the Orders saga.

    Do NOT touch `Fulfillment/Commands/V1/Placeholder.cs`, `Checkout/Events/V1/Placeholder.cs`, `Identity/**`, `Notifications/**`, `Cart/**`, `Catalog/**`, or `Orders/Commands/V1/Placeholder.cs` — out of scope this phase.

    Extend (do not break) the two existing Orders contracts using trailing optional parameters so every existing named-argument call site keeps compiling:
    - `OrderCreated.cs` — append `bool SimulatePaymentFailure = false` as the final parameter.
    - `OrderStatusChanged.cs` — append `string? FailureReason = null` as the final parameter.

    Per RESEARCH.md Pitfall 6, never add a bearer-token/access-token field to any contract in this task.
  </action>
  <acceptance_criteria>
    - `dotnet build src/building-blocks/Contracts/Contracts.csproj` succeeds with zero errors.
    - `grep -rl "IMessageEnvelope" src/building-blocks/Contracts/Checkout/Commands/V1/StartCheckout.cs src/building-blocks/Contracts/Payments` returns all 5 new files.
    - `Placeholder.cs` no longer exists in `Checkout/Commands/V1/`, `Payments/Commands/V1/`, `Payments/Events/V1/`, `Fulfillment/Events/V1/`.
    - `grep -c "SimulatePaymentFailure" src/building-blocks/Contracts/Orders/Events/V1/OrderCreated.cs` returns at least 1.
    - `grep -c "FailureReason" src/building-blocks/Contracts/Orders/Events/V1/OrderStatusChanged.cs` returns at least 1.
    - `dotnet build src/services/orders/Orders.sln` still succeeds (proves the two extended contracts didn't break existing named-argument call sites in `OrderStateMachineSteps.cs`/`OrdersEndpoints.cs`).
  </acceptance_criteria>
  <verify>
    <automated>dotnet build src/building-blocks/Contracts/Contracts.csproj && dotnet build src/services/orders/Orders.sln</automated>
  </verify>
  <done>All 7 new contract files exist and compile; the 4 superseded placeholders are deleted; OrderCreated and OrderStatusChanged compile with their new trailing optional fields and every existing call site still builds.</done>
</task>

<task type="auto">
  <name>Task 2: Wire AppHost and Gateway for Checkout/Payments</name>
  <files>
    src/ecommerce.AppHost/Program.cs,
    src/services/gateway/ECommerce.Gateway.API/appsettings.json
  </files>
  <read_first>
    src/ecommerce.AppHost/Program.cs,
    src/services/gateway/ECommerce.Gateway.API/appsettings.json
  </read_first>
  <action>
    In `src/ecommerce.AppHost/Program.cs`:
    - Move the `checkout` project declaration to AFTER the `orders` variable declaration (currently `checkout` is declared before `orders`, at line ~33; `orders` is declared at line ~38). Assign it to a variable (`var checkout = ...`) since `gateway` will need `.WithReference(checkout)`.
    - Remove `.WithReference(postgres)` from `checkout` (Checkout.API is a thin façade with no database this phase — do not provision an unused DB reference). Keep `.WithReference(serviceBus)` (needed for the producer-only `FulfillmentFailed` publish) and add `.WithReference(orders)` (needed for the internal `IOrdersClient` HTTP calls).
    - Add `.WithReference(postgres).WaitFor(postgres)` to the `payments` project declaration (Payments gets its first database this phase — mirror the `catalog` project's existing `.WithReference(postgres).WaitFor(postgres)` pattern).
    - Add `.WithReference(checkout)` to the `gateway` project declaration's chain (alongside its existing `.WithReference(catalog)`, `.WithReference(identity)`, etc.).
    - Do not change `fulfillment`'s declaration — out of scope this phase.

    In `src/services/gateway/ECommerce.Gateway.API/appsettings.json`:
    - Add a `checkout-route` entry to `ReverseProxy.Routes` identical in shape to the existing `orders-route` block: `"ClusterId": "checkout"`, `"Match": { "Path": "/api/checkout/{**catch-all}" }`, `"Transforms": [{ "PathRemovePrefix": "/api/checkout" }]`.
    - Add a `checkout` cluster entry to `ReverseProxy.Clusters` identical in shape to the existing `orders` cluster: `"Destinations": { "checkout": { "Address": "http://checkout" } }`.
    - Do NOT add routes/clusters for `payments` or `fulfillment` — both stay internal-only per RESEARCH.md (no public Angular access).
  </action>
  <acceptance_criteria>
    - `grep -n "var checkout = builder.AddProject" src/ecommerce.AppHost/Program.cs` matches, and its line number is greater than the line number matched by `grep -n "var orders = builder.AddProject" src/ecommerce.AppHost/Program.cs`.
    - `grep -A5 "var checkout = " src/ecommerce.AppHost/Program.cs` contains `.WithReference(orders)` and does NOT contain `.WithReference(postgres)`.
    - `grep -A5 "ECommerce_Payments_API" src/ecommerce.AppHost/Program.cs` contains `.WithReference(postgres)` and `.WaitFor(postgres)`.
    - `grep -A10 "var gateway = " src/ecommerce.AppHost/Program.cs` contains `.WithReference(checkout)`.
    - `grep -c "checkout-route" src/services/gateway/ECommerce.Gateway.API/appsettings.json` returns 1, and `grep -c '"checkout":' src/services/gateway/ECommerce.Gateway.API/appsettings.json` returns at least 1.
    - `dotnet build src/ecommerce.AppHost/ecommerce.AppHost.sln` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>dotnet build src/ecommerce.AppHost/ecommerce.AppHost.sln</automated>
  </verify>
  <done>AppHost declares checkout after orders with correct references (orders, serviceBus, no postgres); payments references postgres; gateway references checkout; Gateway appsettings.json routes /api/checkout/** to the checkout cluster.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Angular client → YARP Gateway | Untrusted input crosses here (JWT-authenticated but attacker-controlled request bodies) |
| Checkout.API → Orders.API (internal HTTP) | Bearer token forwarded; not a public trust boundary but still service-to-service |
| Orders saga → Azure Service Bus | Message payloads persisted (outbox/DLQ) and logged via OTel |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-04-01 | Information Disclosure | Any new Contracts record | mitigate | Never include a bearer/access token field in any `Contracts/**/V1/*.cs` record (RESEARCH.md Pitfall 6) — enforced by code review of this task's diff |
| T-04-02 | Tampering | Payments/Fulfillment routes | mitigate | Gateway `appsettings.json` intentionally omits routes for `payments`/`fulfillment` — no public ingress exists for a client to directly command payment/fulfillment outcomes |
| T-04-03 | Elevation of Privilege | Payments getting its first DB reference | accept | `payments` DB is provisioned via the same shared Aspire `postgres` resource other services already use (DB-per-service isolation enforced at schema/migration level in plan 04-04, not at this infra-wiring step) |
</threat_model>

<verification>
`dotnet build src/building-blocks/Contracts/Contracts.csproj`, `dotnet build src/services/orders/Orders.sln`, and `dotnet build src/ecommerce.AppHost/ecommerce.AppHost.sln` all succeed with zero errors. Every new contract file implements `IMessageEnvelope`. AppHost declaration order and references match the acceptance criteria exactly.
</verification>

<success_criteria>
All Phase 4 message contracts exist, compile, and follow the established envelope/namespace convention. AppHost and Gateway are wired so that when Checkout.API and Payments.API gain real logic in later plans, no further infrastructure wiring is needed.
</success_criteria>

<output>
Create `.planning/phases/04-checkout-saga-payments/04-01-SUMMARY.md` when done
</output>
