---
phase: 04-checkout-saga-payments
plan: 06
type: execute
wave: 5
depends_on: ["04-05"]
files_modified:
  - src/frontend/ecommerce-app/src/app/shared/models/checkout.model.ts
  - src/frontend/ecommerce-app/src/app/shared/models/order.model.ts
  - src/frontend/ecommerce-app/src/app/core/services/checkout.service.ts
  - src/frontend/ecommerce-app/src/app/core/services/orders.service.ts
  - src/frontend/ecommerce-app/src/app/features/checkout/checkout-page/checkout-page.component.ts
  - src/frontend/ecommerce-app/src/app/features/checkout/checkout-page/checkout-page.component.html
  - src/frontend/ecommerce-app/src/app/features/checkout/checkout-page/checkout-page.component.scss
  - src/frontend/ecommerce-app/src/app/features/orders/order-detail/order-detail.component.ts
  - src/frontend/ecommerce-app/src/app/features/orders/order-detail/order-detail.component.html
  - src/frontend/ecommerce-app/src/app/features/orders/order-detail/order-detail.component.scss
  - src/frontend/ecommerce-app/src/app/app.routes.ts
  - src/frontend/ecommerce-app/src/app/features/cart/cart-page/cart-page.component.html
  - src/frontend/ecommerce-app/src/app/features/cart/cart-page/cart-page.component.ts
autonomous: true
requirements: [FE-03, CHK-01, CHK-02]

must_haves:
  truths:
    - "A user can click through Cart -> Checkout -> Place Order -> watch a live-updating step indicator -> land on the order detail page automatically, with zero manual page refreshes (FE-03)."
    - "The .99 deterministic failure and the manual demo toggle are both discoverable and functional directly from the /checkout page (D-10, D-11)."
    - "A cancelled/failed order's detail page shows the exact human-readable failure reason the backend produced — never the raw saga status string."
  artifacts:
    - path: "src/frontend/ecommerce-app/src/app/features/checkout/checkout-page/checkout-page.component.ts"
      provides: "mat-stepper, ~1.5s polling loop that stops on terminal state, hint text, demo toggle, auto-redirect to /orders/:id"
    - path: "src/frontend/ecommerce-app/src/app/features/orders/order-detail/order-detail.component.ts"
      provides: "First Angular view of Orders data — route param -> GET /api/orders/{id} -> status + failureReason display"
    - path: "src/frontend/ecommerce-app/src/app/app.routes.ts"
      provides: "/checkout and /orders/:id routes"
  key_links:
    - from: "cart-page.component.html"
      to: "/checkout"
      via: "routerLink, enabled button (replaces disabled placeholder)"
      pattern: "routerLink=\"/checkout\""
    - from: "checkout-page.component.ts"
      to: "GET /api/checkout/{id}"
      via: "interval(1500).pipe(switchMap(...))"
      pattern: "interval\\(1500\\)"
    - from: "checkout-page.component.ts terminal state"
      to: "/orders/:id"
      via: "router.navigate(['/orders', id])"
      pattern: "router.navigate\\(\\['/orders'"
---

<objective>
Build the two new Angular pages this phase requires — `/checkout` (step indicator, polling, demo affordances) and `/orders/:id` (status + failure reason) — and wire `/cart`'s existing disabled placeholder button into the flow. No polling UI or `/orders/:id` view exists anywhere in the app yet (confirmed by RESEARCH.md Pitfall 4) — this is genuinely new Angular surface area, not an incremental addition to an existing page.

Purpose: Deliver FE-03 — the only user-visible proof that the whole Phase 4 saga (04-01 through 04-05) actually works end-to-end.
Output: `checkout.service.ts`, `orders.service.ts`, `CheckoutPageComponent`, `OrderDetailComponent`, updated routes, updated cart-page CTA.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-checkout-saga-payments/04-CONTEXT.md
@.planning/phases/04-checkout-saga-payments/04-UI-SPEC.md
@.planning/phases/04-checkout-saga-payments/04-PATTERNS.md

<interfaces>
Backend response shapes this plan's services consume (from plan 04-05):

```typescript
// GET /api/checkout/{id}
interface CheckoutStatus {
  checkoutId: string;
  status: 'Started' | 'AwaitingPayment' | 'Paid' | 'Cancelled' | 'Failed' | 'Fulfilled';
  failureReason: string | null;
}

// GET /api/orders/{id}
interface OrderDetail {
  id: string;
  status: 'Pending' | 'Paid' | 'Fulfilled' | 'Cancelled' | 'Failed';
  totalAmount: number;
  lineItems: { productId: string; productName: string; unitPrice: number; quantity: number }[];
  createdAt: string;
  failureReason: string | null;
}
```

Existing `Cart`/`CartLineItem` models (`shared/models/cart.model.ts`) and `CartService` (`core/services/cart.service.ts`) — read_first below, reuse the exact thin-HttpClient-wrapper pattern for the two new services.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Models, services, and routes</name>
  <files>
    src/frontend/ecommerce-app/src/app/shared/models/checkout.model.ts,
    src/frontend/ecommerce-app/src/app/shared/models/order.model.ts,
    src/frontend/ecommerce-app/src/app/core/services/checkout.service.ts,
    src/frontend/ecommerce-app/src/app/core/services/orders.service.ts,
    src/frontend/ecommerce-app/src/app/app.routes.ts
  </files>
  <read_first>
    src/frontend/ecommerce-app/src/app/core/services/cart.service.ts,
    src/frontend/ecommerce-app/src/app/shared/models/cart.model.ts,
    src/frontend/ecommerce-app/src/app/app.routes.ts
  </read_first>
  <action>
    Create `shared/models/checkout.model.ts`: `export type CheckoutStatusValue = 'Started' | 'AwaitingPayment' | 'Paid' | 'Cancelled' | 'Failed' | 'Fulfilled'; export interface CheckoutStatus { checkoutId: string; status: CheckoutStatusValue; failureReason: string | null; }`.

    Create `shared/models/order.model.ts`: `export interface OrderLineItem { productId: string; productName: string; unitPrice: number; quantity: number; } export interface OrderDetail { id: string; status: string; totalAmount: number; lineItems: OrderLineItem[]; createdAt: string; failureReason: string | null; }`.

    Create `core/services/checkout.service.ts` mirroring `cart.service.ts`'s exact `@Injectable({ providedIn: 'root' })` + `inject(HttpClient)` shape: `startCheckout(simulatePaymentFailure: boolean): Observable&lt;{ checkoutId: string }&gt;` → `this.http.post&lt;{ checkoutId: string }&gt;('/api/checkout', { simulatePaymentFailure })`; `getStatus(checkoutId: string): Observable&lt;CheckoutStatus&gt;` → `this.http.get&lt;CheckoutStatus&gt;(`/api/checkout/${checkoutId}`)`.

    Create `core/services/orders.service.ts`: `getOrder(id: string): Observable&lt;OrderDetail&gt;` → `this.http.get&lt;OrderDetail&gt;(`/api/orders/${id}`)`.

    In `app.routes.ts`, import `CheckoutPageComponent` from `./features/checkout/checkout-page/checkout-page.component` and `OrderDetailComponent` from `./features/orders/order-detail/order-detail.component`; add two routes BEFORE the `{ path: '**', ... }` wildcard, after the existing `cart` route: `{ path: 'checkout', component: CheckoutPageComponent, title: 'Checkout — eCommerce' }` and `{ path: 'orders/:id', component: OrderDetailComponent, title: 'Order — eCommerce' }`.
  </action>
  <acceptance_criteria>
    - `grep -c "CheckoutStatus" src/frontend/ecommerce-app/src/app/shared/models/checkout.model.ts` returns at least 1.
    - `grep -c "post<{ checkoutId" src/frontend/ecommerce-app/src/app/core/services/checkout.service.ts` returns at least 1.
    - `grep -c "path: 'checkout'" src/frontend/ecommerce-app/src/app/app.routes.ts` returns 1.
    - `grep -c "path: 'orders/:id'" src/frontend/ecommerce-app/src/app/app.routes.ts` returns 1.
    - `cd src/frontend/ecommerce-app && npx tsc --noEmit -p tsconfig.app.json` succeeds (or equivalent `ng build --configuration development` if `tsc --noEmit` is not wired — verify which command the existing project uses first via `package.json` scripts).
  </acceptance_criteria>
  <verify>
    <automated>cd src/frontend/ecommerce-app && npx tsc --noEmit -p tsconfig.app.json</automated>
  </verify>
  <done>checkout.service.ts and orders.service.ts exist as thin HttpClient wrappers matching cart.service.ts's convention; both new routes are registered before the wildcard route.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CheckoutPageComponent — stepper, polling, hint text, demo toggle</name>
  <files>
    src/frontend/ecommerce-app/src/app/features/checkout/checkout-page/checkout-page.component.ts,
    src/frontend/ecommerce-app/src/app/features/checkout/checkout-page/checkout-page.component.html,
    src/frontend/ecommerce-app/src/app/features/checkout/checkout-page/checkout-page.component.scss
  </files>
  <read_first>
    .planning/phases/04-checkout-saga-payments/04-UI-SPEC.md,
    src/frontend/ecommerce-app/src/app/features/cart/cart-page/cart-page.component.ts,
    src/frontend/ecommerce-app/src/app/features/cart/cart-page/cart-page.component.html,
    src/frontend/ecommerce-app/src/app/features/cart/cart-page/cart-page.component.scss,
    src/frontend/ecommerce-app/src/app/features/catalog/product-detail/product-detail.component.ts
  </read_first>
  <behavior>
    - On init: loads the cart via `CartService.getCart()`; if `items.length === 0`, immediately `router.navigate(['/cart'])` (per UI-SPEC's copy contract — no separate empty state on this page).
    - Clicking "Place Order" calls `CheckoutService.startCheckout(simulatePaymentFailure)`, disables the button, shows a loading spinner on it until the 202 response returns (per UI-SPEC's Copywriting Contract).
    - After receiving `checkoutId`, polls `CheckoutService.getStatus(checkoutId)` every 1500ms (D-07) via `interval(1500).pipe(switchMap(...))`, updating a `currentStatus` signal each tick.
    - Polling stops (`takeWhile` with `inclusive: true`) the moment `currentStatus()` is one of `'Paid' | 'Cancelled' | 'Failed' | 'Fulfilled'` (terminal), then `router.navigate(['/orders', checkoutId])` (D-08).
    - The `mat-stepper` highlights the step matching `currentStatus()`: `Started` = step 0, `AwaitingPayment` = step 1, `Paid`/terminal = step 2 (per D-06/UI-SPEC's exact 3-label set: "Started" → "Awaiting Payment" → "Paid" — never render the raw backend status string directly in the stepper).
    - The demo toggle (`mat-checkbox`, label exactly `"Simulate payment failure"`) and hint text (exactly `"Tip: cart totals ending in .99 simulate a payment failure."`) are both visible above/near the Place Order button before it is clicked (D-10/D-11).
  </behavior>
  <action>
    Build `CheckoutPageComponent` as a standalone component combining `cart-page.component.ts`'s signal-based loading/error pattern (`cart = signal&lt;Cart | null&gt;(null)`, `isLoading`, `hasError`) with a NEW polling mechanism (no existing analog — RESEARCH.md confirms this is genuinely new). Inject `CartService`, `CheckoutService`, `Router`. State signals: `cart`, `isLoading`, `hasError`, `simulatePaymentFailure = signal(false)`, `checkoutId = signal&lt;string | null&gt;(null)`, `currentStatus = signal&lt;CheckoutStatusValue | null&gt;(null)`, `isPlacingOrder = signal(false)`. A `currentStepIndex = computed(...)` mapping `currentStatus()` to `0 | 1 | 2` per the `&lt;behavior&gt;` mapping. A `isTerminal(status: CheckoutStatusValue) => ['Paid','Cancelled','Failed','Fulfilled'].includes(status)` helper.

    Poll implementation in `onPlaceOrder()`: `this.checkoutService.startCheckout(this.simulatePaymentFailure()).subscribe({ next: ({checkoutId}) => { this.checkoutId.set(checkoutId); this.startPolling(checkoutId); }, error: (err) => { this.isPlacingOrder.set(false); this.hasError.set(true); } })`. `startPolling(id: string)`: `interval(1500).pipe(switchMap(() => this.checkoutService.getStatus(id)), takeWhile(s => { this.currentStatus.set(s.status); return !this.isTerminal(s.status); }, true)).subscribe({ next: (s) => { if (this.isTerminal(s.status)) { this.router.navigate(['/orders', id]); } }, error: () => this.hasError.set(true) });` — import `interval`, `switchMap`, `takeWhile` from `rxjs`/`rxjs/operators`.

    Template (`checkout-page.component.html`) per UI-SPEC exactly: `mat-stepper` (horizontal, `linear="false"`, `[selectedIndex]="currentStepIndex()"`) with 3 `mat-step` labels `"Started"`, `"Awaiting Payment"`, `"Paid"`; a `mat-card` order-summary section reusing `cart-page`'s `.order-summary`/`.summary-total` class names and structure; a `<p class="hint-text">Tip: cart totals ending in .99 simulate a payment failure.</p>`; a `mat-checkbox [(ngModel)]="simulatePaymentFailure"` labeled `"Simulate payment failure"` (or signal-compatible two-way binding via `(change)`/`[checked]` if `ngModel` requires `FormsModule` import — add `FormsModule` to the component's `imports` array if used); a `mat-raised-button color="primary"` labeled `"Place Order"`, `[disabled]="isPlacingOrder()"`, showing a `mat-spinner` (16-20px) inline when `isPlacingOrder()` is true; error state reusing `cart-page`'s exact `.error-state` heading/body/Retry-button shape with heading `"Unable to load checkout status"` and body `"Something went wrong while checking your order. Please refresh the page or try again."` (UI-SPEC Copywriting Contract).

    Apply spacing/typography/color tokens exactly as UI-SPEC specifies (reuse `--space-*` CSS custom properties from `src/styles.scss`, `color="primary"` for all CTAs on this page per UI-SPEC's explicit "follows the cart-page precedent" note — never `color="accent"`).
  </action>
  <acceptance_criteria>
    - `grep -c "mat-stepper" src/frontend/ecommerce-app/src/app/features/checkout/checkout-page/checkout-page.component.html` returns at least 1.
    - `grep -c "interval(1500)" src/frontend/ecommerce-app/src/app/features/checkout/checkout-page/checkout-page.component.ts` returns 1.
    - `grep -c "Tip: cart totals ending in .99 simulate a payment failure." src/frontend/ecommerce-app/src/app/features/checkout/checkout-page/checkout-page.component.html` returns 1.
    - `grep -c "Simulate payment failure" src/frontend/ecommerce-app/src/app/features/checkout/checkout-page/checkout-page.component.html` returns at least 1.
    - `grep -c "router.navigate(\['/orders'" src/frontend/ecommerce-app/src/app/features/checkout/checkout-page/checkout-page.component.ts` returns 1.
    - `grep -c 'color="accent"' src/frontend/ecommerce-app/src/app/features/checkout/checkout-page/checkout-page.component.html` returns 0.
    - `cd src/frontend/ecommerce-app && npx tsc --noEmit -p tsconfig.app.json` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>cd src/frontend/ecommerce-app && npx tsc --noEmit -p tsconfig.app.json</automated>
  </verify>
  <done>CheckoutPageComponent redirects empty carts to /cart, places orders with the demo toggle honored, polls every 1.5s, drives a 3-step mat-stepper matching the checkout vocabulary, and auto-navigates to /orders/:id on any terminal state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: OrderDetailComponent and cart-page CTA wiring</name>
  <files>
    src/frontend/ecommerce-app/src/app/features/orders/order-detail/order-detail.component.ts,
    src/frontend/ecommerce-app/src/app/features/orders/order-detail/order-detail.component.html,
    src/frontend/ecommerce-app/src/app/features/orders/order-detail/order-detail.component.scss,
    src/frontend/ecommerce-app/src/app/features/cart/cart-page/cart-page.component.html,
    src/frontend/ecommerce-app/src/app/features/cart/cart-page/cart-page.component.ts
  </files>
  <read_first>
    src/frontend/ecommerce-app/src/app/features/catalog/product-detail/product-detail.component.ts,
    src/frontend/ecommerce-app/src/app/features/catalog/product-detail/product-detail.component.html,
    src/frontend/ecommerce-app/src/app/features/cart/cart-page/cart-page.component.html,
    src/frontend/ecommerce-app/src/app/features/cart/cart-page/cart-page.component.ts,
    .planning/phases/04-checkout-saga-payments/04-UI-SPEC.md
  </read_first>
  <behavior>
    - Route param `id` missing or `OrdersService.getOrder(id)` errors → `notFound` state shown (mirrors `ProductDetailComponent`'s exact pattern).
    - Order loads successfully with `status` in `['Cancelled','Failed']` and a non-null `failureReason` → the exact `failureReason` string is rendered verbatim in a `<p class="failure-reason">`, using a `mat-chip` in the destructive/warn color for the status badge.
    - Order loads with `status` in `['Pending','Paid','Fulfilled']` → status chip renders in a neutral/primary color, no failure-reason paragraph shown.
    - Cart page's existing disabled "Proceed to Checkout — Coming Soon" button is replaced with an enabled control that navigates to `/checkout`.
  </behavior>
  <action>
    Build `OrderDetailComponent` mirroring `ProductDetailComponent`'s exact structure: `route = inject(ActivatedRoute)`, `ordersService = inject(OrdersService)`, signals `order = signal&lt;OrderDetail | null&gt;(null)`, `isLoading`, `notFound`. `ngOnInit()`: read `this.route.snapshot.params['id']`; if missing set `notFound` and return; else `getOrder(id).subscribe({ next: (order) => { this.order.set(order); this.isLoading.set(false); }, error: () => { this.isLoading.set(false); this.notFound.set(true); } })`. Add `statusColor = computed(() => ['Cancelled','Failed'].includes(this.order()?.status ?? '') ? 'warn' : 'primary')` and `showFailureReason = computed(() => ['Cancelled','Failed'].includes(this.order()?.status ?? '') && !!this.order()?.failureReason)`.

    Template per UI-SPEC: not-found state reuses `product-detail`'s exact `.not-found-state` shape with heading `"Order not found"` and a `mat-button` "Back to Catalog" (`routerLink="/catalog"`); order display shows a Display-role (28px/500) grand total, a Heading-role (20px/500) "Order Summary" section listing line items (product name, qty, unit price, line total via `CurrencyPipe`), a `mat-chip` status badge colored via `statusColor()`, and — only `@if (showFailureReason())` — a `<p class="failure-reason">{{ order()!.failureReason }}</p>` rendering the backend string VERBATIM (no rewording, per UI-SPEC's explicit instruction not to reword the timeout/failure strings). Include a `mat-raised-button color="primary"` "Back to Catalog" link (`routerLink="/catalog"`) at the bottom.

    In `cart-page.component.html`, replace the `<button mat-raised-button color="primary" disabled>Proceed to Checkout — Coming Soon</button>` with `<a mat-raised-button color="primary" routerLink="/checkout">Proceed to Checkout</a>` (enabled, navigates to the new `/checkout` page — the actual "Place Order" confirm action lives on `/checkout` itself per UI-SPEC's Component Inventory, which scopes "Place Order" copy specifically to that page). `RouterLink` is already imported in `cart-page.component.ts`'s `imports` array (confirm via read_first; add if missing).
  </action>
  <acceptance_criteria>
    - `grep -c "failure-reason" src/frontend/ecommerce-app/src/app/features/orders/order-detail/order-detail.component.html` returns at least 1.
    - `grep -c "Coming Soon" src/frontend/ecommerce-app/src/app/features/cart/cart-page/cart-page.component.html` returns 0.
    - `grep -c 'routerLink="/checkout"' src/frontend/ecommerce-app/src/app/features/cart/cart-page/cart-page.component.html` returns 1.
    - `grep -c "disabled" src/frontend/ecommerce-app/src/app/features/cart/cart-page/cart-page.component.html` returns 0 (the Proceed-to-Checkout control is no longer disabled; confirm no other unrelated `disabled` attributes were introduced elsewhere in this file — re-check manually if any match is unrelated).
    - `cd src/frontend/ecommerce-app && npx tsc --noEmit -p tsconfig.app.json` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>cd src/frontend/ecommerce-app && npx tsc --noEmit -p tsconfig.app.json</automated>
  </verify>
  <done>OrderDetailComponent renders status + verbatim failure reason for terminal-failed orders; cart-page's placeholder button is replaced with a working /checkout navigation link.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Angular runtime (browser) → YARP Gateway | All requests carry the user's own JWT (existing OIDC wiring from Phase 2); this plan adds no new auth surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-04-15 | Information Disclosure | `failureReason` rendered verbatim in the DOM | accept | Backend only ever produces 3 curated strings (plan 04-02); Angular does no server-error passthrough, so no stack trace or internal detail can reach the DOM via this field |
| T-04-16 | Tampering | `simulatePaymentFailure` toggle sent from an untrusted client | accept | By design (D-11) — the toggle only forces the SAME deterministic-failure code path PAY-02 already exposes via `.99` amounts; it grants no capability beyond what a user could already trigger by shaping their own cart total |
</threat_model>

<verification>
`cd src/frontend/ecommerce-app && npx tsc --noEmit -p tsconfig.app.json` succeeds with zero type errors across all new/modified files. Manual smoke check (documented for the developer, not automatable in this environment): run the full AppHost + `ng serve`, add an item to cart, click through Cart → Checkout → Place Order, confirm the stepper advances and the page redirects to `/orders/:id` showing `Paid`; repeat with a `.99` cart total or the demo toggle checked and confirm the redirect lands on a `Cancelled` order showing `"Payment declined"`.
</verification>

<success_criteria>
FE-03 is fully satisfied: a user can complete checkout and watch order status update in real time via polling, using only the UI (no direct API calls needed), with both the natural `.99` trigger and the explicit demo toggle discoverable and functional.
</success_criteria>

<output>
Create `.planning/phases/04-checkout-saga-payments/04-06-SUMMARY.md` when done
</output>
